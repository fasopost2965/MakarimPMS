{
  "name": "pms-makarim",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo (backend NestJS + frontend React) du PMS Hôtel Makarim.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run start:dev --workspace=backend",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1"
  },
  "lint-staged": {
    "backend/**/*.ts": "bash -c 'cd backend && npx eslint --fix'",
    "frontend/**/*.{ts,tsx}": "bash -c 'cd frontend && npx eslint --fix'"
  }
}
