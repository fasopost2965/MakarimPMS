# Registre des chantiers — Makarim PMS v1 (post-audit)

Ce registre transforme chaque constat factuel des 11 phases d'audit (`docs/audits/` — Phases 1-10 : complétude fonctionnelle ; Phase 11 : qualité/fiabilité frontend post-couverture) en chantier actionnable. **Aucun chantier ici ne provient d'une invention** : chaque fiche cite sa phase source. Un chantier sans preuve auditée n'est pas dans ce registre.

**Convention d'identifiant** : `CH-0XX`. **Convention de statut** : `à faire` / `en cours` / `bloqué` / `terminé` / `reporté (assumé)` / `abandonné (assumé)`. Un chantier passé `reporté` ou `abandonné` doit avoir sa justification consignée dans `docs/governance/ECARTS_ASSUMES.md`, jamais silencieusement retiré de ce registre.

**Comment lire une fiche** : *Source* renvoie au fichier de `docs/audits/` où le constat est établi. *Confiance* qualifie la certitude du constat lui-même (pas de la solution) — `haute` = confirmé par lecture de code directe et/ou grep exhaustif ; `moyenne` = déduit de plusieurs constats croisés ; jamais `basse` dans ce registre (un constat non confirmé n'est pas listé comme chantier, il est listé en question ouverte dans la phase d'audit correspondante).

---

## Chantiers bloquants (avant toute mise en production réelle)

### CH-001 — Implémenter le mécanisme d'avoir (CreditNote)

- **Titre** : Rendre possible la correction d'une facture émise
- **Source** : `docs/audits/PHASE_06_FINANCE.md` §3, §7 ; confirmé aussi en `PHASE_09_QUALITE_CODE.md` §4 (écart intention/implémentation)
- **Description factuelle** : le modèle `CreditNote` existe dans `schema.prisma`, `Invoice.creditNotes` est inclus dans les lectures (`findFolioById`, `findInvoiceById`), `StatutFacture.ANNULEE_PAR_AVOIR` existe comme valeur d'enum, et le code lui-même (`billing.service.ts`, `deposits.service.ts`) fait référence à un mécanisme d'avoir comme solution censée exister — mais recherche exhaustive : zéro appel `creditNote.create`, zéro route, zéro écriture de `Invoice.statut = 'ANNULEE_PAR_AVOIR'`.
- **Pourquoi ce chantier existe** : une facture `EMISE` est immuable par conception (ADR-004, correctement respecté) ; sans avoir, une erreur de facturation (montant, taxe, client) n'a **aucun recours applicatif**. C'est un chantier structurel, pas un correctif mineur.
- **Modules concernés** : `billing` (propriétaire), `payments` (le remboursement d'acompte imputé — CH-012 — en dépend), `reporting` (le grand livre devra refléter les avoirs).
- **Priorité** : Bloquant
- **Criticité** : Critique
- **Impact métier** : Élevé — aucune correction de facture possible aujourd'hui en production.
- **Impact sécurité** : Aucun direct.
- **Impact conformité** : Élevé — une facture erronée non corrigible est un problème comptable/fiscal réel pour un établissement marocain assujetti à la TVA.
- **Impact exploitation** : Élevé — la réception/comptabilité n'a aujourd'hui aucun outil pour ce cas, qui se produira nécessairement.
- **Dépendances** : aucune dépendance entrante. CH-012 (remboursement d'acompte imputé) et une partie de CH-023 (matérialisation financière de pénalité) dépendent de celui-ci — **CH-012 est désormais techniquement débloqué** (voir sa fiche), mais n'a pas été démarré (hors périmètre de ce chantier, discipline de scope stricte).
- **Prérequis** : *(tranché)* décision produit confirmée par l'utilisateur (`AskUserQuestion`) : **avoir total uniquement** — pas d'avoir partiel, les lignes de taxe déjà matérialisées ne sont jamais modifiées par un avoir (seul le document fiscal — la facture — est annulé, jamais les charges sous-jacentes du folio). Consigné dans `docs/governance/REGISTRE_DECISIONS.md` (RD-005).
- **Livrable attendu** *(réalisé)* : `BillingService.createCreditNote(invoiceId, dto, userId)` comme chemin d'écriture unique (`backend/src/modules/billing/billing.service.ts`), route `POST /invoices/:id/credit-notes` (`billing:write`), `CreateCreditNoteDto` (motif obligatoire ≥10 caractères, pas de champ `montant` — toujours égal à `Invoice.montantTotal`), écriture de `Invoice.statut = ANNULEE_PAR_AVOIR`, audit transactionnel (`AuditAction.CREATE_CREDIT_NOTE`, nouvelle valeur d'enum ajoutée par migration Prisma).
- **Écarts découverts et traités pendant l'implémentation (hors périmètre initial de la fiche, mais nécessaires à une clôture réelle)** :
  1. **Régénération de facture après avoir** : la fiche initiale ne prévoyait pas explicitement qu'un avoir doive permettre d'émettre une facture corrigée sur le même folio. La garde de `generateInvoice()` (« un folio ne peut avoir qu'une seule facture ») a dû être changée de « `folio.invoices.length > 0` bloque pour toujours » à « bloque seulement s'il existe une facture **active** (`statut === 'EMISE'`) » — sinon un avoir total aurait rendu le folio définitivement infacturable, ce qui aurait contredit l'objectif même du chantier (« rendre possible la correction d'une facture émise »). Même changement appliqué à la garde de `excludeTaxes()`.
  2. **Bug de double-matérialisation de taxe découvert par cette garde** : une fois la régénération autorisée, `generateInvoice()` re-matérialisait inconditionnellement les lignes `TAXE_SEJOUR` sur le folio — sur un deuxième appel (facture corrigée après avoir), cela aurait doublé la taxe de séjour déjà présente en base depuis la première génération. Corrigé par une garde `taxeDejaMaterialisee = folio.lignes.some(l => l.type === TypeLigneFolio.TAXE_SEJOUR)` : les taxes ne sont matérialisées qu'une seule fois par folio, quel que soit le nombre de générations de facture. **Preuve de rigueur sabotage/restore effectuée** (règle non négociable `CLAUDE.md`) : garde temporairement retirée, test relancé, échec confirmé avec les valeurs exactement prédites (574 MAD au lieu de 562 MAD, soit 12 MAD de taxe de séjour doublée sur un séjour de 2 nuits), garde restaurée, test revert au vert — commentaire dans `backend/test/billing.e2e-spec.ts`.
- **Critères de validation** : (1) ✅ une facture EMISE reçoit un avoir sans que `montantTotal`/lignes d'origine ne soient jamais réécrits (vérifié : la facture d'origine est relue après l'avoir et comparée champ à champ) ; (2) ✅ `Invoice.statut` passe à `ANNULEE_PAR_AVOIR`, jamais les `FolioLine` sous-jacentes ; (3) ✅ `AuditLog` trace l'opération (`CREATE_CREDIT_NOTE`) dans la même transaction Prisma que la création du `CreditNote` et la mise à jour de la facture ; (4) ✅ un deuxième avoir sur une facture déjà annulée est rejeté (`ConflictException`) ; (5) ✅ une facture corrigée peut être régénérée sur le même folio après avoir, sans doubler la taxe de séjour déjà matérialisée ; (6) ✅ motif < 10 caractères rejeté (400, `ValidationPipe`) ; (7) ✅ facture inconnue rejetée (404).
- **Statut** : **terminé**
- **Estimation de charge** : réalisée en une session (~1 jour équivalent développeur, tests e2e inclus) — plus rapide que l'estimation initiale car le périmètre a été réduit à l'avoir total (l'estimation « moyenne 2–4 jours » incluait l'incertitude du partiel, écartée par l'arbitrage).
- **Niveau de confiance de l'estimation** : élevé (a posteriori).
- **Lien(s) audit** : Phase 6 (§3, §7), Phase 9 (§4), Phase 10 (Priorité bloquante #1).
- **Éléments testés** : `backend/test/billing.e2e-spec.ts`, nouveau bloc « Avoir total sur une facture (CreditNote) — CH-001 » (3 tests : motif trop court → 400 ; facture inconnue → 404 ; scénario complet — génération → blocage double-génération → création d'avoir → immuabilité de la facture d'origine vérifiée → blocage double-avoir → régénération de facture corrigée → absence de doublon de la ligne/du montant `TAXE_SEJOUR`, avec preuve sabotage/restore documentée en commentaire). Suite e2e complète rejouée : 111/113 tests verts, les 2 échecs restants confirmés **pré-existants et sans lien avec CH-001** (flake d'infrastructure `stock.e2e-spec.ts` dû à l'instabilité de Redis dans ce sandbox, prouvé par comparaison contrôlée `git stash`/`git stash pop` : échec identique avant et après les changements CH-001).
- **Documents liés** : `docs/ADR-002-Folio-Billing-Model.md`, `docs/ADR-004-Payment-Financial-Integrity.md` (immuabilité de facture confirmée respectée — seul `statut` change, jamais `montantTotal`/`numero`/les lignes), `docs/governance/REGISTRE_DECISIONS.md` (RD-005).
- **Remarques** : chantier le plus cité transversalement dans l'audit (Phases 2, 3, 6, 9) — signal fort qu'il s'agissait d'un manque structurel plutôt que d'un détail. L'implémentation a révélé deux conséquences en cascade (garde de régénération, bug de double-taxe) non anticipées par la fiche initiale — traitées avant clôture plutôt que découvertes plus tard en production, cohérent avec la discipline « documenter les écarts plutôt que les cacher ».

---

### CH-002 — Sécuriser la réinitialisation de mot de passe

- **Titre** : Ne plus exposer le token de reset en clair dans la réponse HTTP
- **Source** : `docs/audits/PHASE_05_SECURITE.md` §1
- **Description factuelle** : `AuthService.forgotPassword()` retourne `resetToken` directement dans le corps de la réponse HTTP plutôt que de l'envoyer par email. Le code documente ce comportement comme intérimaire, en attendant le module notifications — mais ce module (F7) a été livré depuis (`EvenementNotification` compte 4 valeurs) sans qu'aucun événement `PASSWORD_RESET` n'existe, et sans que `forgotPassword()` n'ait jamais été raccordé.
- **Pourquoi ce chantier existe** : faille de sécurité active et exploitable — prise de contrôle de compte en un seul appel non authentifié, sans accès à la boîte email de la victime.
- **Modules concernés** : `auth` (propriétaire), `notifications` (consommé, chemin déjà existant pour d'autres événements).
- **Priorité** : Bloquant
- **Criticité** : Critique
- **Impact métier** : Faible direct, élevé indirect (perte de confiance si exploité).
- **Impact sécurité** : Critique.
- **Impact conformité** : Modéré (protection des données personnelles des comptes utilisateurs).
- **Impact exploitation** : Faible (le changement n'affecte pas le flux fonctionnel côté utilisateur final, seulement le canal de livraison du token).
- **Dépendances** : aucune.
- **Prérequis** : *(réalisé)* — voir écart de conception ci-dessous.
- **Livrable attendu** *(réalisé, avec un écart de conception assumé par rapport à la fiche initiale)* : `forgotPassword()` envoie désormais l'email via `MailerService.send()` (module `notifications`, exporté et importé dans `AuthModule`) plutôt que via `NotificationsService.notify()`. **Raison de l'écart** : `notify()` est structurellement scopé à `Guest` (`guestId` obligatoire, `NotificationLog.guestId` référence `Guest`, jamais `User`) — un compte `User` (personnel) n'a pas sa place dans ce pipeline CRM/marketing, et forcer un `guestId` factice aurait été une corruption de données, pas une réutilisation légitime. `MailerService` est un simple wrapper SMTP générique sans dépendance à `Guest`/`NotificationLog` : le réutiliser directement évite de dupliquer l'envoi SMTP sans détourner le pipeline Guest. Décision consignée dans `docs/governance/REGISTRE_DECISIONS.md` (RD-004). Aucun ajout à `EvenementNotification`/`NotificationTemplate` — cet enum reste strictement scopé aux événements client.
- **Écart supplémentaire découvert et traité (hors scope initial de la fiche, mais nécessaire à la clôture réelle du chantier)** : le frontend (`ForgotPasswordPage.tsx`) lisait directement `res.resetToken` pour enchaîner sur l'étape de réinitialisation dans la même page (seul mécanisme possible en l'absence de routeur/deep-linking, Phase 8). Retirer `resetToken` de la réponse HTTP sans adapter le frontend aurait cassé le flux de bout en bout, pas seulement réduit une fuite d'information. Traité : la page passe désormais systématiquement à l'étape de saisie après la demande (même comportement observable, cohérent avec l'anti-énumération), avec un champ pour coller le code reçu par email (préremplissable via `?resetToken=` dans l'URL si l'utilisateur clique le lien de l'email, lecture simple de `window.location.search`, aucune dépendance à un routeur ajoutée).
- **Critères de validation** : (1) ✅ la réponse HTTP de `POST /auth/forgot-password` ne contient plus jamais `resetToken`/`expiresAt`, dans les deux cas (compte existant ou non — forme de réponse strictement identique) ; (2) ✅ `MailerService.send()` est appelé avec l'email du compte et un corps contenant le jeton (vérifié par un test qui espionne l'instance réelle du service, pas un mock du service) ; (3) ✅ le flux `reset-password` en aval reste fonctionnel de bout en bout, y compris côté frontend (jeton désormais saisi manuellement/collé plutôt qu'auto-rempli depuis la réponse API).
- **Statut** : **terminé**
- **Estimation de charge** : réalisée en une session — proche de l'estimation initiale (0,5–1 jour) une fois l'écart de conception (MailerService vs notify()) identifié et le correctif frontend inclus.
- **Niveau de confiance de l'estimation** : élevé (a posteriori, confirmé par l'implémentation réelle).
- **Lien(s) audit** : Phase 5 (§1, §4 Risques), Phase 9 (§4, cité comme exemple du motif récurrent), Phase 10 (Priorité bloquante #2).
- **Éléments testés** : suite e2e `backend/test/auth.e2e-spec.ts` (16 tests, dont 2 réécrits pour ce chantier + assertions ajoutées sur l'envoi email par spy) — absence de `resetToken`/`expiresAt` dans les deux branches de réponse, appel réel de `MailerService.send()` avec la bonne adresse et le bon jeton dans le corps, aucun appel pour un email inconnu, relecture du jeton en base (`PasswordResetToken`) plutôt que dans la réponse HTTP, cycle complet reset → nouveau mot de passe fonctionnel → ancien mot de passe rejeté → jeton à usage unique refusé à la deuxième utilisation. Suite e2e complète (19 fichiers, 110 tests) rejouée sans régression après le changement de câblage de module (`AuthModule` → `NotificationsModule`).
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-004, décision MailerService vs notify()), `docs/backend-plan/PLAN_BACKEND_100_REEL.md` (§Domaine Sécurité, mis à jour).
- **Remarques** : la fiche initiale supposait à tort que `NotificationsService.notify()` serait directement réutilisable — l'implémentation a révélé une incompatibilité structurelle (`Guest` vs `User`) non détectée pendant l'audit (celui-ci n'avait pas relu la signature exacte de `notify()` au moment d'écrire cette fiche). Corrigé avant tout code, pas après — cohérent avec la discipline « documenter les écarts plutôt que les cacher » (`docs/governance/CRITERES_STABILITE_LONG_TERME.md`).

---

### CH-003 — Interface de saisie du registre de police

- **Titre** : Permettre la saisie réelle de `PoliceRecord` depuis l'interface
- **Source** : `docs/audits/PHASE_06_FINANCE.md` (mention croisée), `docs/audits/PHASE_08_FRONTEND.md` §2
- **Description factuelle** : le backend expose `POST /police/:stayId` (module `police`, `checkin:write`) et les champs collectés au self-checkin peuvent pré-remplir la saisie (`GET /reservations/:id/self-checkin-pending`) — mais recherche exhaustive dans `frontend/src/features/checkin/` : **zéro occurrence de « police »**. Aucune UI ne permet de saisir `numeroPiece`, `typePiece`, `nationalite`, `dateNaissance`, provenance/destination.
- **Pourquoi ce chantier existe** : `PoliceRecord` est le registre légal des personnes hébergées (obligation DGSN, citée dans `CLAUDE.md`). Sans UI, la seule façon de le remplir serait un appel API direct — non réaliste pour un usage quotidien de réception.
- **Modules concernés** : frontend `checkin` (propriétaire de l'UI), backend `police` (déjà prêt, aucun changement attendu côté API).
- **Priorité** : Bloquant
- **Criticité** : Critique
- **Impact métier** : Élevé (obligation d'enregistrement des hébergés).
- **Impact sécurité** : Faible.
- **Impact conformité** : Critique — obligation légale DGSN potentiellement non tenue en usage réel.
- **Impact exploitation** : Élevé — sans cette UI, la réception ne peut pas remplir son obligation dans le cours normal du travail.
- **Dépendances** : aucune côté backend (API déjà fonctionnelle, confirmée en Phase 2/audits antérieurs — F1, PDF de registre déjà livré).
- **Prérequis** : *(tranché)* pas d'arbitrage produit distinct requis — le formulaire suit directement les champs déjà définis par `UpsertPoliceRecordDto` côté backend, la pré-génération depuis self-checkin réutilise `GET /reservations/:id/self-checkin-pending` déjà existant.
- **Livrable attendu** *(réalisé)* : nouvelle feature frontend `frontend/src/features/police/` (`types.ts`, `api.ts`, `components/PoliceRecordForm.tsx`), intégrée comme troisième onglet « Police » dans `StayDetailsDialog.tsx` (aux côtés de « Détails »/« Facturation », même convention d'onglets que `BillingTabContent`). Pré-remplissage automatique depuis `self-checkin-pending` uniquement quand aucune fiche n'existe encore et que le séjour vient d'une réservation (jamais pour un walk-in, qui n'a pas de lien self-checkin). Bouton « Télécharger le PDF » réutilisant `GET /police/:stayId/pdf` (F1, déjà existant) une fois la fiche enregistrée.
- **Écart/ajout par rapport au plan initial** : un badge d'avertissement (⚠ « Fiche police manquante ») a été ajouté dans les listes « Séjours en cours » / « Départs du jour » de `CheckinPage.tsx` et sur l'onglet « Police » lui-même — pas explicitement demandé par la fiche initiale, mais nécessaire pour donner une visibilité réelle à l'avertissement backend déjà existant (`StayService`, `POLICE_RECORD_WARNING`) sans avoir à ouvrir chaque séjour un par un. `Stay.policeRecord` (déjà inclus par le backend, `STAY_INCLUDE`) a été ajouté au type frontend `Stay` pour piloter ce badge sans appel réseau supplémentaire.
- **Critères de validation** : (1) ✅ un check-in complet (réservation ou walk-in) permet de saisir et sauvegarder un `PoliceRecord` sans quitter l'interface ; (2) ✅ les champs pré-remplis par self-checkin apparaissent bien dans le formulaire (logique vérifiée en live, aucune donnée self-checkin réelle disponible dans le scénario de test manuel mais le chemin de code est le même que la relecture de fiche existante) ; (3) l'export CSV existant (`ReportingPage.tsx`) reste inchangé et continue de refléter les enregistrements saisis (aucune régression, non retesté spécifiquement dans ce chantier — déjà couvert par `reporting.e2e-spec.ts`).
- **Statut** : **terminé**
- **Estimation de charge** : réalisée en une session (~0,5 jour équivalent développeur) — plus rapide que l'estimation initiale (2–3 jours), l'essentiel de la complexité anticipée (schéma des champs, pré-remplissage) étant déjà résolu côté backend.
- **Niveau de confiance de l'estimation** : élevé (a posteriori).
- **Lien(s) audit** : Phase 8 (§2, tableau « modules backend sans écran »), Phase 6 (mention croisée), Phase 10 (Priorité bloquante #3).
- **Éléments testés** : **pas de suite de tests automatisés frontend dans ce projet** (`frontend/package.json` ne définit aucun script `test`) — vérification manuelle réelle en navigateur (Chromium piloté par Playwright, pas une simple lecture de code) : login réception → check-in walk-in → badge ⚠ visible dans la liste des séjours en cours → ouverture du séjour → onglet Police affiche le formulaire vide avec avertissement → saisie complète → soumission → badge disparaît de la liste (preuve que `onPoliceRecordSaved` → `refetch()` propage bien l'état à jour) → téléchargement du PDF confirmé être un vrai document PDF valide (`file` : « PDF document, version 1.3, 1 page(s) »). Build et lint frontend propres. Données de test nettoyées de la base après vérification.
- **Documents liés** : `docs/modules/*.md` ne contient toujours pas de spec `police.md` dédiée — non traité ici (reste dans le périmètre de CH-018, resynchronisation documentation modules).
- **Remarques** : dernier des 4 chantiers bloquants du registre — avec sa clôture, `CH-001` à `CH-004` sont tous les quatre `terminé`.

---

### CH-004 — Trancher le chiffrement au repos des données d'identité

- **Titre** : Décider et, le cas échéant, implémenter le chiffrement de `Guest.pieceIdentite`
- **Source** : `docs/audits/PHASE_05_SECURITE.md` §3
- **Description factuelle** : `Guest.pieceIdentite` est stocké en clair — confirmé par un commentaire explicite dans `police-report.service.ts` reconnaissant l'absence de chiffrement au repos. `docs/execution/GO_LIVE_CHECKLIST.md` référence une variable `ENCRYPTION_KEY` qui **n'apparaît dans aucun fichier de code** (recherche exhaustive : seulement 3 documents et le commentaire qui documente son absence).
- **Pourquoi ce chantier existe** : donnée d'identité sensible (numéro de CIN/passeport), exigence déjà documentée dans le propre référentiel du projet mais jamais exécutée.
- **Modules concernés** : `guests` (propriétaire de `Guest`), `police` (lit le champ), infrastructure (gestion du secret `ENCRYPTION_KEY`).
- **Priorité** : Bloquant
- **Criticité** : Élevée
- **Impact métier** : Faible direct.
- **Impact sécurité** : Élevé — exposition immédiate en cas de compromission de la base.
- **Impact conformité** : Élevé — exigence déjà auto-documentée par le projet (`GO_LIVE_CHECKLIST.md`) et non tenue.
- **Impact exploitation** : Faible si implémenté correctement (chiffrement/déchiffrement transparent en lecture pour la réception).
- **Dépendances** : aucune technique bloquante — le choix d'implémentation (chiffrement applicatif au niveau champ, retenu, vs chiffrement au niveau colonne MySQL) a été arbitré en faveur du premier (voir Décision ci-dessous).
- **Prérequis** : *(tranché)* décision produit confirmée par l'utilisateur (`AskUserQuestion`, entre « implémenter maintenant » et « accepter le risque formellement ») : **implémenter maintenant**. Consigné dans `docs/governance/REGISTRE_DECISIONS.md` (RD-006).
- **Livrable attendu** *(réalisé)* : chiffrement applicatif AES-256-GCM du champ `pieceIdentite`, clé issue de `ENCRYPTION_KEY` (32 octets base64), déchiffrement transparent à la lecture — implémenté au niveau du **client Prisma** (extension `result.guest.pieceIdentite`, `backend/src/prisma/guest-encryption.extension.ts`), pas dans `GuestsService`/`PoliceReportService` individuellement (voir écart de conception ci-dessous).
- **Écart de conception découvert et traité pendant l'implémentation (par rapport au plan initial « déchiffrement dans GuestsService/PoliceReportService »)** : `Guest` est lu par relation imbriquée (`include: { guest: true }`) depuis plusieurs modules en façade lecture seule (`PoliceReportService.getDailyReport`, `ReservationsService`, `StayService`...), jamais uniquement via un appel direct `prisma.guest.findMany`. Un wrapper de déchiffrement local à `GuestsService` aurait laissé ces lectures imbriquées renvoyer le texte chiffré brut. Vérifié empiriquement avant d'écrire le code définitif (script de test jetable) : une extension Prisma `result` (contrairement à une extension `query`, qui n'intercepte que les opérations top-level sur le modèle) s'applique bien à `Guest` partout où il apparaît dans un résultat, y compris imbriqué depuis un autre modèle et à l'intérieur d'une transaction interactive (`$transaction(async (tx) => ...)`). Conséquence architecturale : `PrismaService` (`backend/src/prisma/prisma.module.ts`) fournit désormais le client Prisma étendu via un `useFactory` plutôt qu'une instanciation directe de la classe — tous les consommateurs existants (`constructor(private prisma: PrismaService)`) sont inchangés, seul l'objet réellement injecté par le DI a changé.
- **Deuxième écart découvert : recherche cassée** — `GuestsService.search()` filtrait `pieceIdentite` via `{ contains: q }` au niveau SQL ; un texte chiffré non déterministe (IV aléatoire à chaque écriture, propriété de sécurité intentionnelle d'AES-GCM) ne peut plus jamais matcher un `LIKE` SQL. Corrigé par un repli applicatif : les candidats non trouvés par nom/prénom/téléphone (toujours filtrables en SQL, non chiffrés) sont déchiffrés et filtrés côté application. Acceptable pour un hôtel de 24 chambres (CLAUDE.md) où la table `Guest` ne grossit jamais à une échelle rendant ce repli coûteux — limite assumée documentée si le volume change un jour (RD-006).
- **Critères de validation** : (1) ✅ `pieceIdentite` illisible par une requête SQL brute sur la base — vérifié par `$queryRaw` dans le test e2e (préfixe `enc:v1:`, jamais la valeur en clair) ; (2) ✅ aucune régression sur les flux existants (recherche client — corrigée, voir ci-dessus — export police, suite e2e complète rejouée) ; (3) ✅ `ENCRYPTION_KEY` validée au démarrage dans **tous** les environnements (pas seulement en production comme les secrets JWT — sans elle, `Guest` est totalement inutilisable), plus une garde supplémentaire prod-only réutilisant `assertStrongSecrets()` contre la valeur de développement documentée dans `.env.example`.
- **Statut** : **terminé**
- **Estimation de charge** : réalisée en une session (~1 jour équivalent développeur, tests inclus) — cohérente avec l'estimation initiale (« Moyenne, 2–3 jours si implémenté »), le temps de vérification empirique du comportement d'extension Prisma (nested include, transaction) ayant été le poste le plus long, pas l'écriture du chiffrement lui-même.
- **Niveau de confiance de l'estimation** : élevé (a posteriori).
- **Lien(s) audit** : Phase 5 (§3, Risques), Phase 2 (mention initiale), Phase 10 (Priorité bloquante #4).
- **Éléments testés** : `backend/src/common/crypto/field-encryption.spec.ts` (9 tests unitaires — round-trip, non-déterminisme de l'IV, rejet sur mauvaise clé, **preuve de rigueur sabotage/restore réelle** sur la détection d'altération du texte chiffré par l'auth tag GCM, rétrocompatibilité avec une valeur en clair pré-existante) ; `backend/test/guests.e2e-spec.ts` (nouveau test : valeur brute chiffrée en base via `$queryRaw`, valeur en clair via l'API, recherche par pieceIdentite toujours fonctionnelle) ; assertion **pré-existante** de `backend/test/reporting.e2e-spec.ts` (`expect(csv).toContain('AB998877')`, export CSV du registre de police via `stay.guest.pieceIdentite` imbriqué) confirmée toujours verte — preuve indépendante que le déchiffrement se propage bien à travers un `include` imbriqué. Suite e2e complète rejouée : 112/114 tests verts, les 2 échecs restants confirmés pré-existants et sans lien (même flake `stock.e2e-spec.ts` déjà documenté pour CH-001).
- **Documents liés** : `docs/execution/GO_LIVE_CHECKLIST.md` (exigence d'origine, désormais tenue — voir `ECARTS_DOC_VS_CODE.md`), `docs/governance/REGISTRE_DECISIONS.md` (RD-006).
- **Remarques** : seul des 4 chantiers bloquants dont l'issue légitime n'était pas nécessairement « coder une solution » — l'arbitrage a confirmé l'implémentation plutôt que l'acceptation de risque formelle, retiré en conséquence de la liste des candidats dans `ECARTS_ASSUMES.md`.

---

## Chantiers importants

### CH-005 — Blocage/alerte du check-out sur solde impayé

- **Source** : `docs/audits/PHASE_06_FINANCE.md` §5
- **Description factuelle** : `StayService.checkout()` calcule `soldeDu` via `computeSoldeDu()` et le renvoie dans la réponse, mais ne lève jamais d'exception ni de garde si `soldeDu !== 0`.
- **Modules concernés** : `stay` (propriétaire de `checkout()`), `billing`/`payments` (source du solde).
- **Priorité** : Important · **Criticité** : Élevée
- **Impact métier** : Élevé (fuite de revenus potentielle) · **Impact sécurité** : Aucun · **Impact conformité** : Faible · **Impact exploitation** : Élevé
- **Dépendances** : aucune technique. **Prérequis** : décision produit — blocage dur (le check-out échoue) vs avertissement (le check-out réussit avec un flag explicite nécessitant une confirmation) ; le cahier des charges/BUSINESS_RULES.md (`BR-SEJ-004`/`INV-SEJ-002`, déjà cités dans `CLAUDE.md` comme non appliqués) doit trancher.
- **Livrable attendu** : selon la décision — soit `ConflictException` sur `checkout()` si `soldeDu > 0` (avec une route de « check-out forcé » réservée à une permission élevée si l'hôtel veut garder une échappatoire), soit un champ de réponse structuré exigeant une confirmation côté frontend.
- **Critères de validation** : un séjour avec solde positif ne peut plus être clôturé silencieusement sans action explicite reconnue.
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Faible-Moyenne (1–2 jours) · **Confiance** : élevée
- **Lien audit** : Phase 6 §5, §7 ; Phase 10.
- **Éléments à tester** : e2e check-out avec solde positif/négatif/nul.
- **Documents liés** : `CLAUDE.md` (règle désormais appliquée, voir §Paiements et solde de folio), `BUSINESS_RULES.md` (`BR-SEJ-004`).
- **Résolution** : arbitrage produit tranché (blocage dur + échappatoire de check-out forcé — voir `REGISTRE_DECISIONS.md` RD-008). `StayService.checkout()` lève désormais `ConflictException` si `soldeDu > 0`, sauf `force: true` (`ForceCheckoutDto`, motif ≥ 10 caractères) soumis à la permission dédiée `checkin:force-checkout` (Administrateur uniquement, vérification dynamique dans le service, même pattern que `guests:blacklist`/`payments:refund`) et journalisé dans `AuditLog` (`FORCE_CHECKOUT`) dans la même transaction. Tests : `checkin-flow.e2e-spec.ts` (blocage 409, refus RBAC 403, refus motif manquant 400, forçage réussi + audit, preuve sabotage/restore documentée en commentaire) ; 5 autres suites e2e (`dashboard`, `housekeeping`, `housekeeping-state-machine`, `maintenance`) adaptées pour utiliser le check-out forcé sur leurs fixtures de test au solde jamais réglé (hors périmètre métier de ces suites).

---

### CH-006 — Centraliser le filtrage soft-delete

- **Source** : `docs/audits/PHASE_03_BASE_DE_DONNEES.md` §5.6 ; `docs/audits/PHASE_04_BACKEND.md` §5 ; `docs/audits/PHASE_09_QUALITE_CODE.md` §4
- **Description factuelle** : 12 modèles portent `deletedAt`, mais la constante de convention `NOT_DELETED` n'est importée que dans 9 fichiers de service. Aucun middleware/extension Prisma global.
- **Modules concernés** : transverse (`PrismaModule`/`PrismaService`), tous les modules consommant un modèle à `deletedAt`.
- **Priorité** : Important · **Criticité** : Modérée
- **Impact métier** : Modéré (résurgence possible de données supprimées) · **Impact sécurité** : Modéré (ex. un `Guest` blacklisté puis soft-deleted ne devrait jamais réapparaître) · **Impact conformité** : Faible · **Impact exploitation** : Modéré
- **Dépendances** : aucune. **Prérequis** : choix technique entre middleware Prisma (`$use`, si toujours supporté par la version de Prisma utilisée) ou Prisma Client Extension (`$extends`) — **à confirmer selon la version de Prisma du projet**.
- **Livrable attendu** : mécanisme global appliquant `deletedAt: null` par défaut sur tout `findMany`/`findFirst`/`findUnique` des 12 modèles concernés, sans casser les usages légitimes qui doivent lire les lignes supprimées (ex. futurs écrans d'historique).
- **Critères de validation** : suppression manuelle de l'appel `NOT_DELETED` dans un service existant ne change plus le résultat observable (garanti par le mécanisme global, pas par la discipline).
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Moyenne (2–3 jours, tests de non-régression inclus) · **Confiance** : moyenne (dépend de la compatibilité Prisma)
- **Lien audit** : Phase 3 §5.6, Phase 4 §5, Phase 9 §4.
- **Éléments à tester** : suite e2e complète (régression large surface), test dédié « une ligne soft-deleted n'apparaît plus dans aucune liste ».
- **Documents liés** : `docs/ADR-005-Audit-Soft-Delete.md`.
- **Résolution** : choix technique tranché en faveur de `$extends` (Prisma Client Extension) — `$use` (middleware) est déprécié depuis Prisma 5, `$extends` est déjà le mécanisme établi par CH-004 (`guest-encryption.extension.ts`), composable via chaînage (`.$extends().$extends()`, vérifié en live que les deux extensions coexistent sans se casser l'une l'autre — chiffrement `Guest.pieceIdentite` toujours fonctionnel, filtrage soft-delete actif). Nouvelle extension `soft-delete.extension.ts` : intercepte `findMany`/`findFirst`/`findFirstOrThrow`/`findUnique`/`findUniqueOrThrow`/`count`/`aggregate`/`groupBy` sur les 12 modèles via `$allModels`, injecte `deletedAt: null` — `findUnique`/`findUniqueOrThrow` fonctionnent grâce à `extendedWhereUnique` (GA depuis Prisma 5), fonctionnalité ajoutée par Prisma précisément pour ce cas d'usage. Recherche exhaustive avant implémentation : **aucun code du projet n'écrit jamais `deletedAt` à ce jour** (ADR-005 promis mais jamais exercé, voir `DETTE_TECHNIQUE.md` §6) — ce mécanisme est donc un filet de sécurité tourné vers l'avenir, vérifié empiriquement comme un no-op sur le comportement actuel (chaque ligne a `deletedAt = NULL` en pratique), pas une régression de risque à gérer. **Limite documentée, pas un oubli** : n'intercepte que les opérations top-level (`prisma.model.findMany(...)`), jamais les lectures imbriquées via `include`/`select` (même limite constatée pour l'extension `result` de CH-004, dans l'autre sens) — aucune parade générique côté Prisma sur MySQL (pas de RLS native). Les 8 filtres inline `{ deletedAt: null }` préexistants (`reporting`, `hr`, `stay`, `payments`, `stock`) ont été retirés là où ils portaient sur un appel top-level désormais redondant ; les filtres portant sur une relation `include` (ex. `attendance.service.ts` — `segments: { where: { deletedAt: null } } }`) ont été conservés, car toujours nécessaires. La constante `NOT_DELETED` (`common/utils/soft-delete.ts`), jamais réellement importée nulle part, supprimée (son propre commentaire anticipait explicitement ce remplacement). Test dédié : `backend/test/soft-delete.e2e-spec.ts` — une chambre soft-deleted directement en base (aucune route API n'écrit `deletedAt` aujourd'hui) disparaît de `GET /rooms`, qui n'a jamais filtré `deletedAt` explicitement avant ce chantier ; preuve sabotage/restore réelle sur le filtre `findMany` (désactivé, échec confirmé de la manière attendue — la chambre soft-deleted réapparaissait —, restauré, revérifié vert). Suite complète rejouée sans régression (123/123 e2e, 32/32 unitaires).

---

### CH-007 — Interface frontend self-checkin (staff)

- **Source** : `docs/audits/PHASE_08_FRONTEND.md` §2
- **Description factuelle** : `POST /reservations/:id/self-checkin-link` et `GET /reservations/:id/self-checkin-pending` existent côté backend, zéro appel frontend.
- **Modules concernés** : frontend `reservations`/`checkin`.
- **Priorité** : Important · **Criticité** : Modérée
- **Impact métier** : Modéré (fonctionnalité F6 livrée mais inutilisable) · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Modéré
- **Dépendances** : aucune. **Livrable attendu** : bouton « Générer/régénérer le lien self check-in » sur le détail de réservation, affichage du statut d'attente.
- **Critères de validation** : la réception peut générer un lien et voir s'il a été utilisé, sans appel API manuel.
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Faible (1 jour) · **Confiance** : élevée
- **Lien audit** : Phase 8 §2.
- **Résolution** : nouveau composant `SelfCheckinPanel.tsx` (`frontend/src/features/reservations/components/`), affiché dans `ReservationDetailsDialog.tsx` pour toute réservation `statut === 'CONFIRMEE'` (self check-in n'a plus de sens après transformation en séjour ou annulation). Réutilise les deux routes existantes telles quelles, sans aucune modification backend au périmètre initial : `POST .../self-checkin-link` (génère/régénère, affiche l'URL retournée avec un bouton copier — cette URL n'est persistée nulle part côté lecture, donc uniquement disponible au moment de la génération) et `GET .../self-checkin-pending` (réutilisé aussi côté Police pour le pré-remplissage, mais jamais affiché tel quel avant ce chantier). **Limite assumée, pas un oubli** : cette dernière route ne permet pas de distinguer « aucun lien généré » de « lien généré mais pas encore soumis » (elle renvoie `null` dans les deux cas) — le badge affiche donc uniquement « En attente » vs « Informations soumises », sans état intermédiaire « lien envoyé ».
  - **Bug latent corrigé au passage** (`frontend/src/lib/api-client.ts`) : NestJS renvoie un corps de réponse vide (pas le littéral JSON `"null"`) pour un handler qui retourne `null`/`undefined`, y compris sur un statut 200 — `apiRequest()` ne traitait ce cas que pour le statut 204 explicite, provoquant `Unexpected end of JSON input` sur tout appel de `GET .../self-checkin-pending` sans soumission existante. Ce bug préexistait silencieusement dans `PoliceRecordForm.tsx` (même endpoint, jamais déclenché en pratique car la vérification manuelle de CH-003 avait dû passer par un séjour walk-in) — corrigé une seule fois dans le client HTTP partagé plutôt que contourné localement dans le nouveau composant.
  - **Dette technique #6 corrigée au passage** (`docs/governance/DETTE_TECHNIQUE.md`) : `prisma/seed.ts` échouait au reseed (`StockItem_code_key` unique constraint) dès que les fonctionnalités F6/F10/stock avaient été exercées localement — bloquait littéralement la vérification en navigateur de ce chantier, déclenchant exactement la condition documentée pour un correctif isolé. Ajout des `deleteMany()` manquants (`channelReservationImport`, `selfCheckinToken`, `stockMovement`, `stockItem`) dans le bon ordre de dépendance FK.
  - Vérifié en navigateur réel (Chromium/Playwright) : création d'une réservation, génération du lien (URL affichée + copie presse-papiers fonctionnelle), soumission réelle via `POST /self-checkin/:token` (simulant le client), réouverture du détail de réservation confirmant le badge « Informations soumises » et le résumé des champs. `cd backend && npm run build && npm run lint && npm run test && npm run test:e2e` (123/123 e2e, 32/32 unitaires) et `cd frontend && npm run build && npm run lint` — tous verts, aucune régression.

---

### CH-008 — Interface frontend notifications (templates/logs)

- **Source** : `docs/audits/PHASE_08_FRONTEND.md` §2
- **Description factuelle** : `NotificationTemplate`/`NotificationLog` existent et sont pleinement fonctionnels côté backend (F7), aucune UI de gestion.
- **Modules concernés** : frontend (nouveau `features/notifications/`).
- **Priorité** : Important · **Criticité** : Faible
- **Impact métier** : Modéré (les templates ne peuvent être modifiés qu'en base directement) · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Modéré
- **Dépendances** : CH-011 (le gating RBAC devrait couvrir cet écran, réservé à un rôle administratif).
- **Livrable attendu** : écran de liste/édition des `NotificationTemplate` par (événement, canal), consultation des `NotificationLog` récents.
- **Critères de validation** : modification d'un template visible immédiatement sur le prochain envoi.
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Moyenne (2 jours) · **Confiance** : élevée
- **Lien audit** : Phase 8 §2.
- **Résolution** : nouveau nom d'onglet dédié « Notifications » (`frontend/src/features/notifications/`, nouvelle entrée `NAV_ITEMS` avec `permission: 'notifications:read'`, dépendance CH-011 désormais satisfaite) — contrairement à CH-009, la fiche demandait explicitement un nouveau `features/notifications/`, pas une intégration dans `parameters`. Deux sections (boutons, même motif que `ParametersPage.tsx`) : *Templates* (édition inline par (évènement, canal) — sujet affiché seulement pour le canal EMAIL, `SMS`/`WHATSAPP` n'en tiennent jamais compte à l'envoi ; case à cocher « Actif » ; motif ≥ 10 caractères obligatoire ; dialogue « + Nouveau template » pour une paire (évènement, canal) pas encore configurée) et *Journal d'envoi* (liste des `NotificationLog` les plus récents en premier, badge coloré par statut ENVOYE/ECHEC/IGNORE/EN_ATTENTE, motif d'ignorance affiché en clair). Réutilise les quatre routes existantes telles quelles (`GET/POST /notifications/templates`, `PATCH /notifications/templates/:id`, `GET /notifications/logs`), aucune modification backend. Pas de composant `Textarea` partagé introduit (aucun autre consommateur dans le projet) — `<textarea>` brut stylé en cohérence visuelle avec `Input`. Vérifié en navigateur réel (Chromium/Playwright) : modification du corps d'un template EMAIL existant persistée, création d'un nouveau template SMS (champ Sujet correctement absent pour ce canal), Journal affichant les envois réels de la session (dont l'import OTA de CH-009) avec le bon statut ; **preuve RBAC serveur réelle** — un compte Réception (`notifications:read` seul) voit l'onglet mais une tentative d'édition renvoie 403 (« Permission requise : notifications:write. »), affiché tel quel côté frontend sans plantage.

---

### CH-009 — Interface frontend channel-manager (mappings OTA)

- **Source** : `docs/audits/PHASE_08_FRONTEND.md` §2
- **Description factuelle** : `ChannelRoomTypeMapping` (CRUD `parameters:write`/`read`) sans mapping configuré fait échouer tout import OTA (404 explicite côté backend) — aucune UI pour le configurer.
- **Modules concernés** : frontend (nouveau, probablement intégré à `features/parameters/`).
- **Priorité** : Important · **Criticité** : Modérée (si le canal OTA est effectivement utilisé)
- **Impact métier** : Élevé si canaux OTA activés (Booking.com/Expedia/Airbnb ne peuvent pas fonctionner sans ce mapping) · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Élevé si OTA activé, nul sinon
- **Dépendances** : aucune. **Prérequis** : confirmer si un canal OTA réel est déjà branché en production — *à confirmer*, car F10 est un module de test/adaptateur sans compte partenaire réel selon `CLAUDE.md`. **Non bloquant pour l'implémentation** : le livrable (écran CRUD) est identique quelle que soit la réponse — sans mapping configuré, tout import OTA reste rejeté explicitement (404) qu'un partenaire réel soit branché ou non.
- **Livrable attendu** : écran CRUD des mappings type de chambre ↔ canal externe.
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Faible (1 jour) · **Confiance** : élevée
- **Lien audit** : Phase 8 §2.
- **Résolution** : nouvelle section « Channel Manager » dans `ParametersPage.tsx` (`frontend/src/features/parameters/`, 4e onglet aux côtés d'Identité/TVA/Grille saisonnière — même page, mêmes conventions RBAC `parameters:write`/`read`, pas un nouvel écran isolé). Réutilise les trois routes existantes telles quelles (`GET/POST /channel-manager/mappings`, `DELETE /channel-manager/mappings/:id`), motif ≥ 10 caractères obligatoire à la création et à la suppression (ADR-005, même pattern que `SeasonRatesSection` dont le composant est directement dérivé). Sélecteur de canal restreint aux 3 OTA réels (`CanalOTA` côté frontend exclut `WALK_IN`/`DIRECT`, jamais l'origine d'un webhook entrant). Vérifié en navigateur réel (Playwright, rôle Administrateur) : création d'un mapping Booking.com « STD-DBL » → Single, **puis vérification bout-en-bout par un vrai appel webhook** (`POST /channel-manager/BOOKING_COM/reservations`, secret partagé réel) confirmant que le mapping créé via l'UI résout effectivement un import OTA vers le bon type de chambre ; suppression via l'UI puis nouvel appel webhook confirmant le rejet 404 attendu une fois le mapping retiré.

---

### CH-010 — Déduplication client (email/téléphone)

- **Source** : `docs/audits/PHASE_03_BASE_DE_DONNEES.md` §5.5
- **Description factuelle** : aucune contrainte unique sur `Guest.email`/`telephone`/`pieceIdentite` — un client blacklisté peut être recréé comme nouveau client.
- **Modules concernés** : `guests`.
- **Priorité** : Important · **Criticité** : Élevée (contournement d'une règle bloquante existante)
- **Impact métier** : Élevé · **Impact sécurité** : Faible · **Impact conformité** : Modéré · **Impact exploitation** : Modéré
- **Dépendances** : aucune technique. **Prérequis** : *(tranché)* décision produit confirmée par l'utilisateur (réponse écrite à une liste d'arbitrages soumise, hors `AskUserQuestion`) — **approche hybride** : contrainte dure sur `pieceIdentite`, détection souple sur email/téléphone. Consigné dans `docs/governance/REGISTRE_DECISIONS.md` (RD-011).
- **Livrable attendu** : contrainte `@@unique` sur un index aveugle de `pieceIdentite` (voir Résolution ci-dessous — conflit avec le chiffrement CH-004 détecté et résolu avant implémentation) rejetant explicitement la création/mise à jour en doublon ; recherche de correspondance par email/téléphone à la création, renvoyée au frontend comme avertissement non bloquant (confirmation manuelle).
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Moyenne (migration + logique applicative, 1–3 jours) · **Confiance** : élevée (a posteriori)
- **Lien audit** : Phase 3 §5.5, §8.
- **Note d'implémentation** : un `@@unique` direct sur `Guest.pieceIdentite` est inapplicable — le chiffrement CH-004 (`field-encryption.ts`) utilise un IV aléatoire à chaque appel *par construction*, précisément pour empêcher toute fuite d'égalité par comparaison du texte chiffré ; deux chiffrements du même numéro produisent des valeurs stockées différentes. Résolution retenue : nouvelle colonne `Guest.pieceIdentiteHash` (HMAC-SHA256 déterministe, jamais exposée à l'API) portant seule la contrainte d'unicité — voir `REGISTRE_DECISIONS.md` (RD-011) pour le détail complet.
- **Résolution** : `hashField()` (`field-encryption.ts`, HMAC-SHA256 même clé que le chiffrement AES-GCM — primitives indépendantes, pas de faiblesse croisée connue) appelé dans `guest-encryption.extension.ts` (`encryptGuestData()`), donc calculé de façon transparente quel que soit le service appelant (`GuestsService`, mais aussi `ReservationsService`/`StayService` qui créent des `Guest` via `tx.guest.create` sans passer par `GuestsService`) — intercepté au niveau du client Prisma, pas du service. Colonne `pieceIdentiteHash` jamais exposée à l'API via le `omit` global de `PrismaClient` (`prisma.module.ts`). Détection souple : `GuestsService.checkPotentialDuplicate()` + `GET /guests/check-duplicate` (`guests:read`), consultatif uniquement — vérifié qu'une création réelle avec un email/téléphone déjà utilisé réussit toujours (201). Bug connexe découvert et corrigé en même temps : `AllExceptionsFilter` supposait `Prisma.PrismaClientKnownRequestError.meta.target` toujours un tableau (vrai sur Postgres/SQLite) alors que MySQL (ce projet) le renvoie en chaîne — un vrai P2002 dégénérait donc en 500 brut au lieu du 409 attendu, jamais exercé avant faute de contrainte unique sur une table métier courante. Frontend : nouveau hook partagé `useDuplicateWarning(email, telephone)` (`frontend/src/features/guests/useDuplicateWarning.ts`, même pattern de debounce que la recherche existante de `GuestPicker`), branché à la fois dans `GuestPicker` (réservation/walk-in) et `CreateGuestForm` (`GuestsPage.tsx`) — un encart d'avertissement nommant les correspondances potentielles, jamais un blocage. Vérifié en live (curl réel) : création avec `pieceIdentite` dupliqué → 409 avec message localisé ; `GET /guests/check-duplicate` par email → correspondance renvoyée sans `pieceIdentiteHash` ; aucune correspondance → tableau vide.
- **Éléments testés** : `backend/test/guests.e2e-spec.ts` (+6 scénarios) — contrainte dure sur `POST`/`PATCH /guests`, sur le chemin walk-in inline (`POST /checkin/walk-in`, bypass de `GuestsService`), `GET /guests/check-duplicate` (email/téléphone/aucune correspondance/non-bloquant) et RBAC (`guests:read` requis). Gap connexe corrigé dans ce fichier seulement (documenté en commentaire, pas généralisé aux 19 autres specs e2e) : `AllExceptionsFilter` n'était enregistré que dans `main.ts`, jamais dans le bootstrap `Test.createTestingModule` des tests e2e. Suite complète rejouée : 127/128 e2e (1 flake pré-existant `stock.e2e-spec.ts`, confirmé sans lien en le rejouant seul → vert), 32/32 unitaires, build+lint backend et frontend propres.
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-011).

---

### CH-011 — Gating RBAC minimal côté frontend

- **Source** : `docs/audits/PHASE_05_SECURITE.md` §2 ; `docs/audits/PHASE_08_FRONTEND.md` §3
- **Description factuelle** : aucun état global d'identité/rôle côté frontend, `AppSidebar` affiche les 11 onglets sans filtrage.
- **Modules concernés** : frontend transverse (`App.tsx`, `AppSidebar.tsx`, nouveau contexte d'authentification).
- **Priorité** : Important · **Criticité** : Modérée
- **Impact métier** : Modéré (UX trompeuse sur les droits réels) · **Impact sécurité** : Faible (le vrai contrôle reste serveur, ce chantier est cosmétique/UX, pas une barrière de sécurité) · **Impact conformité** : Aucun · **Impact exploitation** : Modéré
- **Dépendances** : nécessite une route backend exposant les permissions de l'utilisateur courant (`GET /auth/me` ou équivalent — **n'existe pas aujourd'hui, à créer**, aucune trace dans les controllers audités).
- **Prérequis** : décision sur le niveau de granularité (masquer des onglets entiers vs masquer des actions individuelles).
- **Livrable attendu** : nouvelle route backend `GET /auth/me` (rôle + permissions), contexte React `AuthContext`, filtrage de `NAV_ITEMS` par permission déclarée.
- **Critères de validation** : un rôle sans permission `hr:*` ne voit plus l'onglet RH.
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Moyenne (2–3 jours, backend + frontend) · **Confiance** : moyenne
- **Lien audit** : Phase 5 §2, §4 ; Phase 8 §3, §5.
- **Résolution** : arbitrage produit tranché — granularité onglet entier uniquement (voir `REGISTRE_DECISIONS.md`, RD-009). `GET /auth/me` (AuthController/AuthService) renvoie `{ id, email, roleId, roleName, permissions: string[] }` (format `"module:action"`, requête `Permission` fraîche à chaque appel, jamais mise en cache dans le JWT — même garantie que `PermissionsGuard`). Côté frontend, pas de `AuthContext` séparé (sur-ingénierie pour un seul consommateur) : `App.tsx` porte l'état `permissions`, transmis en prop à `AppSidebar`, qui filtre `NAV_ITEMS` (nouveau champ `permission` par item — toujours la permission `:read` de l'écran, jamais une action d'écriture). Un onglet actif devenu invisible (permissions rechargées) bascule automatiquement sur le premier onglet accessible. Correctif connexe découvert en implémentant : `lib/api-client.ts` traitait tout `/auth/*` comme public (jamais de tentative de refresh sur 401) — plus vrai pour `GET /auth/me`, corrigé par une liste explicite d'endpoints publics plutôt qu'un préfixe. Tests : `backend/test/auth.e2e-spec.ts` (401 sans token, permissions exactes pour un rôle restreint, actions dédiées hors grille pour l'Administrateur, reflet immédiat d'un retrait de permission) ; vérification frontend manuelle réelle en navigateur (Chromium/Playwright, 4 rôles : Administrateur voit les 11 onglets, Gouvernante/Maintenance/RH ne voient que leurs onglets respectifs avec redirection automatique hors d'un onglet par défaut inaccessible).

---

### CH-012 — Remboursement d'acompte imputé (chemin fonctionnel)

- **Source** : `docs/audits/PHASE_06_FINANCE.md` §4
- **Description factuelle** : `DepositsService.rembourser` refuse explicitement de rembourser un acompte `IMPUTE`, renvoyant vers un mécanisme de note de crédit — inexistant (CH-001).
- **Modules concernés** : `payments`, `billing` (dépend de CH-001).
- **Priorité** : Important · **Criticité** : Modérée
- **Impact métier** : Modéré · **Impact sécurité** : Aucun · **Impact conformité** : Faible · **Impact exploitation** : Modéré
- **Dépendances** : *(débloqué et livré)* CH-001 est terminé — `BillingService.createCreditNote()` existe comme chemin d'écriture réutilisable.
- **Écart découvert et tranché pendant l'implémentation** : ni `docs/modules/payments.md` ni `BUSINESS_RULES.md` (BR-FAC-005) ne spécifient réellement de mécanisme de remboursement pour un acompte imputé — le texte « passe par une note de crédit sur la facture » n'existait que dans le message d'erreur du code lui-même, pas dans le référentiel gelé. Investigation avant implémentation : `DepositsService.rembourser()` est, même pour le cas `ENCAISSE` déjà fonctionnel, une opération de **statut pur** — elle ne crée ni n'annule jamais de `FolioLine` (le mouvement d'argent réel reste un geste humain hors système). Une vraie réversion du crédit de folio (activer `FolioLine.annulee`, resté sans aucun chemin d'écriture nulle part dans le code base malgré son existence en schéma) aurait exigé une migration (aucun lien `FolioLine → ReservationDeposit` n'existe) et une bien plus grande portée que l'estimation 0,5 jour de cette fiche.
- **Livrable attendu** *(réalisé, conception resserrée par rapport au texte initial)* : `rembourser()` reste une opération de statut pure, cohérente avec le cas `ENCAISSE` — pour un acompte `IMPUTE`, l'avoir n'est **pas créé par cette route** ; il en devient un **préalable** : `DepositsService` interroge `BillingService.findFolioById()` (façade en lecture seule, jamais de Prisma direct sur `Invoice`) pour vérifier qu'aucune facture `EMISE` active ne subsiste sur le folio d'imputation. Si une facture active existe, la requête est bloquée (409) avec un message pointant explicitement vers `POST /invoices/:id/credit-notes` (CH-001). Une fois la facture annulée par avoir (ou si le folio n'a jamais été facturé), le remboursement est autorisé.
- **Critères de validation** : (1) ✅ un acompte `IMPUTE` sur un folio jamais facturé peut être remboursé directement ; (2) ✅ un acompte `IMPUTE` sur un folio portant une facture `EMISE` active est bloqué (409, message explicite) ; (3) ✅ après avoir sur cette facture, le remboursement devient possible ; (4) ✅ `payments:refund` (Administrateur uniquement) reste requis, inchangé.
- **Statut** : **terminé**
- **Estimation de charge** : réalisée en une session (~0,5 jour équivalent développeur, cohérent avec l'estimation initiale malgré l'investigation supplémentaire sur l'absence de spécification).
- **Niveau de confiance de l'estimation** : élevé (a posteriori).
- **Lien audit** : Phase 6 §4.
- **Éléments testés** : `backend/test/payments.e2e-spec.ts` — aucune couverture e2e n'existait jusqu'ici pour `ReservationDeposit`/`rembourser()` (gap découvert pendant ce chantier, comblé pour le périmètre CH-012 uniquement, pas une reprise complète). Trois scénarios via un vrai parcours HTTP (réservation → acompte → check-in réel, pour exercer `StayService.imputerAcomptes` tel qu'il s'exécute en production) : remboursement direct sans facture, blocage puis déblocage après avoir (preuve sabotage/restore réelle sur la garde `factureActive`), et RBAC (`payments:refund`). Suite complète rejouée sans régression (115/117 e2e — même flake pré-existant `stock.e2e-spec.ts` que documenté pour CH-001/CH-004).
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-007).

---

## Chantiers secondaires (dette technique, non bloquants fonctionnellement)

### CH-013 — Traiter les enums morts (`StatutSejour.ANNULE`, `StatutFacture.ANNULEE_PAR_AVOIR`)
- **Source** : Phase 3 §5.2/§6, Phase 6 §3. **Priorité** : Secondaire · **Criticité** : Faible.
- **Description** : deux valeurs d'enum jamais écrites en base. `ANNULEE_PAR_AVOIR` **résolu par CH-001** (`BillingService.createCreditNote()` écrit désormais cette valeur).
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Faible (quelques heures) · **Confiance** : élevée.
- **Résolution** : arbitrage utilisateur tranché conditionnellement (« retirer si aucun cas d'usage réel et stable n'est défini », voir `REGISTRE_DECISIONS.md` RD-012) — vérification exhaustive effectuée avant retrait (seule la définition de type et un libellé d'affichage jamais exercé référençaient `StatutSejour.ANNULE`, aucun chemin d'écriture ni test). Valeur retirée de `enum StatutSejour` (migration Prisma), types frontend et libellé associé mis à jour en conséquence.

### CH-014 — Route de consultation de `RoomStatusLog`
- **Source** : Phase 7 §3, §5. **Priorité** : Secondaire · **Criticité** : Faible.
- **Description** : table peuplée à chaque transition, jamais lue par aucune route.
- **Livrable attendu** : `GET /rooms/:id/historique-statuts` (`housekeeping:read` ou `rooms:read` selon la matrice RBAC en vigueur).
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Faible (0,5–1 jour) · **Confiance** : élevée (a posteriori).
- **Résolution** : `RoomsService.findStatusHistory()` (nouvelle méthode, `RoomsService` reste l'unique propriétaire de `RoomStatusLog`, aucune lecture Prisma directe ailleurs) exposée via `HousekeepingController` sous `housekeeping:read` — écart RBAC déjà documenté (pas de `rooms:read` dédié tant qu'aucune route de configuration de chambre n'existe, cf. plus haut dans ce fichier). Frontend : bouton « Historique » sur chaque carte de `HousekeepingPage.tsx`, ouvrant `RoomHistoryDialog.tsx` (nouveau composant, purement consultatif). Vérifié en e2e : ordre décroissant par date, filtrage par chambre, 404 sur chambre inexistante, 403 pour un rôle sans `housekeeping:read` (Maintenance).
- **Éléments testés** : `backend/test/housekeeping.e2e-spec.ts` (+3 scénarios). Suite complète rejouée : 142/142 e2e, 32/32 unitaires, aucune régression.

### CH-015 — Interface (ou a minima route) de consultation de `AuditLog`
- **Source** : Phase 8 §2 (module `audit` sans écran). **Priorité** : Secondaire · **Criticité** : Faible-Modérée (utile en cas de litige/contrôle).
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Moyenne (1–2 jours) → réalisée en une fraction de l'estimation initiale, confirmant l'hypothèse de la fiche · **Confiance** : élevée (a posteriori).
- **Résolution** : confirmé que `GET /audit-logs` (`AuditController`, `audit:read`, filtrable par `entite`/`userId`/`action`/`du`/`au`) était déjà complet et fonctionnel — seul le frontend manquait entièrement. Nouveau feature `frontend/src/features/audit/` (formulaire de filtres + liste de résultats, détail avant/après dépliable pour chaque entrée), nouvel onglet « Audit » dans `AppSidebar` (`audit:read`, réservé à l'Administrateur en l'état actuel du seed — aucun autre rôle n'a cette permission). Aucun changement backend.
- **Éléments testés** : suite e2e existante (`audit.e2e-spec.ts`, backend inchangé) rejouée sans régression — 142/142 e2e, build+lint propres des deux côtés.

### CH-016 — Réévaluer le découpage de `ReservationsService`
- **Source** : Phase 9 §2, §5. **Priorité** : Secondaire · **Criticité** : Faible (dette, pas un bug).
- **Statut** : à faire (non urgent) · **Estimation** : Élevée si entrepris (3–5 jours, risque de régression sur le module le plus dense en règles métier) · **Confiance** : faible sur l'estimation tant que le découpage cible n'est pas défini.

### CH-017 — Couverture de tests unitaires de la couche service
- **Source** : Phase 4 §4, §6 ; Phase 9 §5. **Priorité** : Secondaire · **Criticité** : Modérée (dette qui grossit avec chaque nouveau chantier).
- **Statut** : à faire · **Estimation** : Élevée et continue (pas un chantier ponctuel — à intégrer comme pratique, cf. `docs/governance/CRITERES_GO_LIVE.md`) · **Confiance** : moyenne.

### CH-018 — Resynchroniser la documentation modules (17 vs 21)
- **Source** : Phase 1, Phase 4 §1. **Priorité** : Secondaire · **Criticité** : Faible.
- **Livrable attendu** : mise à jour de `docs/modules/MODULES_INDEX.md` et `CLAUDE.md` pour refléter les 21 modules réels ; création des specs manquantes (`police.md` notamment, cf. CH-003).
- **Statut** : ✅ **Terminé** (session courante) · **Estimation d'origine** : Moyenne (1–2 jours) → **écart significatif détecté et validé avec l'utilisateur avant exécution** (voir Résolution) · **Confiance** : élevée (a posteriori).
- **Écart avec l'estimation initiale** : en creusant le chantier, `MODULES_INDEX.md` ne listait que 14 modules (sur 21 réels) et **8** modules backend n'avaient aucune fiche spec (`auth`, `booking-engine`, `channel-manager`, `dashboard`, `document-ocr`, `notifications`, `police`, `self-checkin`) — la fiche d'origine ne nommait que `police.md` comme exemple, sans anticiper l'ampleur réelle. Question posée explicitement à l'utilisateur (rédiger les 8 specs complètes, seulement `police.md`, ou aucune nouvelle spec) avant d'engager le travail — réponse : rédiger les 8 specs complètes, au même niveau de détail que les specs existantes (18 sections, format `hr.md`/`reporting.md`).
- **Résolution** : `docs/modules/MODULES_INDEX.md` resynchronisé (14 → 21 modules réels + `accounting` clairement marqué non implémenté, écart déjà connu dans `ECARTS_DOC_VS_CODE.md` — city ledger recoupe `CH-021`/`EA-001`, clôture/rapprochement de caisse jamais demandés ailleurs) ; `CLAUDE.md` (§Architecture backend, phrase d'inventaire des modules) mis à jour de 17 à 21 (ajout de `hr`, `reporting`, `stock`, `channel-manager`, absents de l'énumération malgré une description détaillée ailleurs dans le même fichier). 8 nouvelles fiches créées : `auth.md`, `police.md`, `self-checkin.md`, `notifications.md`, `booking-engine.md`, `document-ocr.md`, `channel-manager.md`, `dashboard.md` — chacune rédigée à partir d'une relecture réelle du code (routes, permissions, entités, dépendances, DTO, événements), pas de la documentation existante. Écart doc/code trouvé et corrigé au passage : `RBAC_MATRIX.md` décrit un module `dashboard` bien plus riche (« graphiques financiers », RevPAR) que ce qui existe réellement (une seule route d'agrégation) — documenté dans `dashboard.md` §3/§15/§16 comme écart assumé, cette spec faisant foi sur la vue résumée (cohérent avec la règle déjà énoncée dans `CLAUDE.md`).
- **Documents liés** : `docs/governance/ECARTS_DOC_VS_CODE.md` (entrée `accounting` déjà connue, non créée par ce chantier).

### CH-019 — Clarifier la collision de nom `room-transitions.ts`
- **Source** : Phase 9 §2. **Priorité** : Secondaire · **Criticité** : Très faible.
- **Livrable attendu** : renommage de `housekeeping/utils/room-transitions.ts` en `housekeeping/utils/manual-status-targets.ts` (ou équivalent) pour lever l'ambiguïté de nom — **changement de nom de fichier uniquement, aucune logique modifiée**.
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Très faible (< 1h) → réalisée dans l'estimation · **Confiance** : élevée.
- **Résolution** : `git mv` vers `housekeeping/utils/manual-status-targets.ts` (nom exact suggéré par la fiche), les deux imports relatifs mis à jour (`update-room-status.dto.ts`, `mobile-room-status-update.dto.ts`) — seuls importeurs de ce fichier. `rooms/utils/room-transitions.ts` (l'autre moitié de la collision, propriété du module `rooms`, `ROOM_TRANSITIONS`/`canTransition`) reste inchangé, conformément au périmètre de la fiche. Contenu et logique strictement inchangés.
- **Éléments testés** : aucun test dédié (renommage pur, zéro logique modifiée) — suite complète rejouée sans régression : 146/146 e2e, 32/32 unitaires, build+lint propres.

### CH-020 — Revoir la numérotation de facture (remise à zéro mensuelle)
- **Source** : Phase 6 §3. **Priorité** : Secondaire · **Criticité** : Faible (question de préférence comptable, pas un bug fonctionnel).
- **Statut** : ✅ **Fermé** (session courante) — décision actée, aucun développement · **Confiance** : élevée.
- **Résolution** : arbitrage utilisateur tranché en faveur du statu quo (séquence continue conservée, pas de remise à zéro mensuelle) — voir `REGISTRE_DECISIONS.md` (RD-013) et `ECARTS_ASSUMES.md` (EA-003).

### CH-021 — City ledger / `Company` : raccorder réellement ou dépriorité formellement
- **Source** : Phase 2, Phase 3 §5.3. **Priorité** : Secondaire · **Criticité** : Modérée.
- **Description** : `Company` sans aucune FK vers `Reservation`/`Stay`/`Folio`/`Invoice`, `plafondCredit` jamais vérifié — écart déjà connu avant cet audit (`docs/ARCHITECTURE_AUDIT.md`, Incohérence #1 citée dans le contexte projet).
- **Statut** : ✅ **Fermé** (session courante) — dépriorisé formellement, aucun développement · **Confiance** : élevée.
- **Résolution** : arbitrage utilisateur tranché en faveur de la dépriorisation (pas de raccordement réel pour cette version) — voir `REGISTRE_DECISIONS.md` (RD-014) et `ECARTS_ASSUMES.md` (EA-001, condition de réexamen documentée).

### CH-022 — Interface frontend document-ocr (scan pièce d'identité)
- **Source** : Phase 8 §2. **Priorité** : Secondaire · **Criticité** : Faible (confort, pas une obligation contrairement à CH-003/police).
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Moyenne (1–2 jours) → réalisée dans l'estimation, malgré un bug bloquant réel découvert en cours de route (voir Résolution) · **Confiance** : élevée (a posteriori).
- **Résolution** : nouveau `frontend/src/features/document-ocr/` (`api.ts`, `types.ts`, `pages/DocumentOcrPage.tsx`) — upload d'image (JPEG/PNG/WebP), sélecteur de type de document optionnel (détection automatique par défaut), aperçu de l'image, affichage des champs extraits (numéro de pièce, nom, prénom, nationalité, dates, sexe), badge de validité des chiffres de contrôle, bannière d'avertissement, zone MRZ brute repliable. Onglet dédié « Scan pièce d'identité » (`guests:write` — **écart volontaire** à la convention `:read` des autres onglets : ce module n'a aucune route de lecture, gater sur `guests:read` aurait masqué l'onglet aux seuls rôles pouvant réellement l'utiliser). `lib/api-client.ts` (`apiRequest`) étendu pour ne plus imposer `Content-Type: application/json` quand le corps est un `FormData` (premier upload multipart du frontend — sans ce correctif, le navigateur ne peut jamais fixer lui-même la boundary multipart).
- **Bug bloquant découvert et corrigé en vérifiant l'écran en navigateur réel avant de considérer ce chantier terminé** (conformément à `CLAUDE.md` §Doing tasks) : une image corrompue/illisible envoyée à `POST /document-ocr/scan` faisait planter **tout le process backend** (pas seulement la requête HTTP en cours) — `tesseract.js` (`createWorker.js`) lève une exception synchrone non catchable dans son propre gestionnaire de message interne quand aucun `errorHandler` n'est fourni à `createWorker()`, en plus de rejeter la promesse du job. `DocumentOcrService` ne fournissait pas cet `errorHandler` et n'entourait pas `worker.recognize()` d'un `try/catch` — un seul upload malformé (ou un fichier réellement corrompu) mettait donc l'API hors service pour tous les utilisateurs connectés jusqu'à un redémarrage manuel. Corrigé par (1) un `errorHandler` sur `createWorker()` (empêche la branche `throw` interne de s'exécuter) et (2) un `try/catch` autour de `worker.recognize()` traduisant l'échec en `BadRequestException` (400) propre, avec recréation forcée d'un worker neuf au prochain appel (le worker ayant échoué est explicitement terminé plutôt que réutilisé dans un état incertain). **Hors périmètre initial de la fiche** (qui portait sur le frontend), mais bloquant sa propre vérification — même famille de correctif embarqué que `lib/api-client.ts` lors de CH-007.
- **Éléments testés** : nouveau `backend/test/document-ocr.e2e-spec.ts` (premier fichier e2e pour ce module, gap pré-existant comblé au passage) — 4 scénarios : image corrompue → 400 propre **et preuve explicite que le serveur reste vivant** (requête de contrôle juste après, `GET /auth/me` → 200 ; preuve de sabotage/restore documentée en commentaire — ce test échouait par timeout/`ECONNRESET` avant le correctif, confirmé en local en retirant temporairement l'`errorHandler`) ; image valide sans MRZ → 201 avec avertissement explicite ; requête sans fichier → 400 ; rôle sans `guests:write` → 403. Vérifié aussi en navigateur réel (Playwright, capture d'écran) : connexion, visibilité RBAC de l'onglet (présent pour Administrateur, absent pour Maintenance), upload, aperçu image, affichage des champs et de l'avertissement. Suite complète rejouée : 150/150 e2e (+4 nouveaux), 32/32 unitaires, build+lint propres des deux côtés.
- **Documents liés** : `docs/modules/document-ocr.md` (créé lors de CH-018, §12 « chaque échec OCR ne bloque jamais l'extraction des autres champs » — le crash découvert ici est un niveau plus grave, au-delà de ce que décrivait la spec, corrigé et non re-documenté séparément puisque le comportement observable reste conforme à la spec).

### CH-023 — Matérialisation financière de la pénalité d'annulation/no-show
- **Source** : Phase 6 §5. **Priorité** : Secondaire · **Criticité** : Modérée.
- **Description** : `Reservation.montantPenalite` figé mais jamais traduit en écriture financière traçable — recouvrement entièrement humain hors système.
- **Statut** : ✅ **Fermé** (session courante) — reste hors PMS par choix, aucun développement · **Confiance** : élevée.
- **Résolution** : arbitrage utilisateur tranché en faveur du statu quo (recouvrement reste un processus humain hors PMS) — voir `REGISTRE_DECISIONS.md` (RD-015) et `ECARTS_ASSUMES.md` (EA-002, condition de réexamen documentée).

### CH-024 — Contrainte d'exclusivité `RoomNight.reservationId`/`stayId`
- **Source** : Phase 3 §5.4. **Priorité** : Secondaire · **Criticité** : Faible (risque théorique, jamais observé en pratique selon l'audit).
- **Statut** : ✅ **Fermé** (session courante) — bloqueur technique MySQL identifié, invariant vérifié par audit du code plutôt que par contrainte base · **Estimation d'origine** : Faible (0,5 jour) · **Confiance** : élevée (a posteriori).
- **Écart avec la formulation initiale de l'audit** : l'audit citait « les deux [FK] renseignées simultanément » comme exemple de ce que la base accepte à tort, suggérant une exclusivité stricte (XOR). Vérifié dans le code avant d'écrire quoi que ce soit : `StayService.checkin()` (check-in depuis une réservation existante — le chemin le plus emprunté, pas un cas marginal) exécute `roomNight.updateMany({ where: { reservationId }, data: { stayId } })`, qui **ajoute** `stayId` sans jamais effacer `reservationId`. Après un check-in réservé, les lignes `RoomNight` ont donc légitimement les **deux** FK renseignées — un XOR aurait cassé ce flux central en production. Seule la moitié réellement dangereuse de l'invariant (aucune ligne avec les **deux nulles** — une nuit verrouillée sans aucun propriétaire) est un vrai problème.
- **Blocage technique découvert en essayant d'implémenter la contrainte `CHECK`** : `RoomNight.reservationId`/`stayId` portent tous deux `onDelete: Cascade`. MySQL 8 rejette explicitement (erreur 3823) toute contrainte `CHECK` référençant une colonne de clé étrangère dotée d'une action référentielle `CASCADE`/`SET NULL`/`SET DEFAULT` — restriction documentée MySQL, pas un bug de la migration. La fiche d'origine avait anticipé cette possibilité (« contrainte applicative **ou** `CHECK` selon support MySQL ») : c'est le cas de figure qui se réalise ici.
- **Options non retenues** : (a) retirer `onDelete: Cascade` sur les deux FK pour débloquer le `CHECK` — rejeté, changement de comportement plus large que ce chantier (impact sur le nettoyage des fixtures e2e et sur la sémantique documentée « ne se déclenche jamais en production, soft delete généralisé », à revoir dans son ensemble plutôt qu'en effet de bord de CH-024) ; (b) trigger MySQL `BEFORE INSERT`/`BEFORE UPDATE` — rejeté, aucun précédent de trigger dans ce projet (toute la logique métier vit dans la couche service Prisma), introduirait un nouveau pattern d'infrastructure pour un risque déjà qualifié « théorique, jamais observé en pratique » par l'audit lui-même — disproportionné pour ce chantier.
- **Résolution retenue** : invariant vérifié par audit exhaustif des écritures (pas par une contrainte base) — 3 sites de création (`ReservationsService.create`/`update`, `StayService.checkinWalkIn`) posent toujours exactement une FK ; le seul site de mise à jour (`StayService.checkin`) ajoute `stayId` sans jamais nullifier `reservationId` ; tous les sites de suppression (`deleteMany`, jamais de mise à null) suppriment la ligne entière. Aucun chemin d'écriture ne produit de ligne à deux FK nulles aujourd'hui. Documenté en commentaire à côté du modèle `RoomNight` dans `schema.prisma` pour qu'un futur développeur retrouve ce constat sans le refaire.
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-019).

### CH-025 — Contraintes `CHECK` manquantes (dates, montants)
- **Source** : Phase 3 §3.4. **Priorité** : Secondaire · **Criticité** : Faible.
- **Statut** : ✅ **Terminé** (session courante) · **Estimation** : Faible-Moyenne (1 jour) → réalisée dans l'estimation · **Confiance** : élevée (a posteriori).
- **Résolution** : 4 contraintes `CHECK` MySQL 8 ajoutées en défense en profondeur (migration manuelle `20260724063000_ch025_check_constraints` — non représentables dans le schéma Prisma, aucun attribut `@check`, comme anticipé par l'audit ; commentaires de renvoi ajoutés à côté des 4 modèles concernés dans `schema.prisma`) : `Reservation` (`chk_reservation_dates`, `dateDepart > dateArrivee`) ; `Payment` (`chk_payment_montant_positif`, `montant > 0`) ; `FolioLine` (`chk_folioline_montant_positif`, `montant >= 0`) ; `TimeShiftSegment` (`chk_timeshiftsegment_fin_apres_debut`, `fin IS NULL OR fin >= debut`). Chaque invariant est déjà porté par la couche service (`ReservationsService.assertDateRangeValid`, etc.) — ces contraintes bloquent uniquement le contournement (script, migration de données, bug applicatif futur), elles ne remplacent aucune validation existante.
- **Écart avec la formulation initiale de l'audit** : l'audit envisageait « `FolioLine.montant` positif sur une ligne PAIEMENT (censée être négative) ». Vérifié dans le code avant d'écrire la migration : **aucune** ligne PAIEMENT n'est jamais stockée en négatif (`BillingService.creditFolioLine`, `computeSoldeDu` traite le signe via `type`, jamais en inversant `montant`) — la contrainte réellement posée est donc inconditionnelle sur `montant` (positif ou nul, quel que soit `type`), pas conditionnelle à `PAIEMENT`.
- **Gap réel comblé en écrivant la migration (au-delà du périmètre strict de la fiche)** : ni `CreatePaymentDto.montant` (DTO) ni `AttendanceService.ajusterSegment` (correction manuelle RH, ADR-007 §6.4) ne validaient jusqu'ici, côté application, les invariants désormais portés en base — ces deux contraintes DB comblent un trou réel, pas seulement une redondance défensive. Correctif DTO explicitement laissé hors périmètre (CH-025 porte sur la base, pas les DTO).
- **`>= 0` et non `> 0` pour `FolioLine`, contrairement à `Payment`** : découvert en écrivant les tests — `billing.e2e-spec.ts` matérialise une ligne `TAXE_SEJOUR` à `montant = 0` sur une fixture de séjour à 0 nuit facturable (cas synthétique du test, mais légitime au sens du calcul `computeTaxLineAmount`). Une première version `> 0` cassait ce test réel ; corrigée avant de considérer le chantier terminé (voir commentaire dédié dans la migration).
- **Éléments testés** : nouveau `backend/test/check-constraints.e2e-spec.ts` (4 scénarios, un par contrainte) — écrit directement via `PrismaService` en contournant délibérément la couche service/DTO, pour prouver que la BASE elle-même rejette ces valeurs indépendamment de toute validation applicative ; chaque scénario prouve à la fois le rejet de la valeur invalide et l'acceptation du cas limite légitime (`fin` nulle, montant nul pour `FolioLine`). Suite complète rejouée : 146/146 e2e (après correction du `>= 0`, voir écart ci-dessus — une régression réelle sur `billing.e2e-spec.ts` a été détectée puis corrigée avant la clôture de ce chantier), 32/32 unitaires. Flaky pré-existant sans lien avec ce chantier découvert au passage sur `stock.e2e-spec.ts` (domaine disjoint, isolé et documenté séparément — voir `DETTE_TECHNIQUE.md` point 7).
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-018), `docs/governance/DETTE_TECHNIQUE.md` (point 7, flaky test non lié).

### CH-026 — Durcissement sécurité secondaire (groupé)
- **Source** : Phase 5 §3, §4. **Priorité** : Secondaire · **Criticité** : Faible à modérée selon sous-point.
- **Sous-points** : (a) ajout de `helmet` ; (b) comparaison à temps constant pour `CHANNEL_WEBHOOK_SECRET` ; (c) politique de verrouillage de compte après N échecs ; (d) exigence de complexité de mot de passe ; (e) migration des tokens frontend vers cookie `httpOnly`+`SameSite` ; (f) révocation/rotation de refresh token.
- **Statut** : ✅ **(a)(b)(c)(d)(e)(f) terminés** (session courante — (e) livré après note de conception + correctif post-vérification live, voir Résolution (e) ci-dessous) · **Estimation** : Moyenne cumulée (3–5 jours pour l'ensemble) · **Confiance** : élevée (a posteriori).
- **Résolution (a)** : `helmet()` monté dans `main.ts` (`app.use`), CSP par défaut désactivée uniquement hors production (Swagger UI à `/api/docs`, jamais monté en production, charge des styles/scripts inline que la CSP par défaut bloquerait).
- **Résolution (b)** : `ChannelWebhookGuard.canActivate` comparait `provided !== expected` (`!==` sur deux chaînes, court-circuite au premier caractère différent — fuite de la longueur du préfixe correct via canal auxiliaire temporel). Remplacé par `crypto.timingSafeEqual` (après vérification d'égalité des longueurs, qui elle-même se résout en O(1) sans fuite exploitable au-delà de la longueur).
- **Résolution (c)** : verrouillage réutilisant `LoginLog` (aucune migration — la table existait déjà pour l'audit succès/échec) : un compte est bloqué (401, message distinct) si ses 5 tentatives les plus récentes dans une fenêtre glissante de 15 minutes sont toutes des échecs — y compris avec le bon mot de passe (empêche un attaquant ayant deviné/volé le mot de passe pendant la fenêtre de blocage). Se lève automatiquement quand la fenêtre expire, sans tâche de fond ni cron (cohérent avec l'absence de cron dans ce projet). Compromis assumé, documenté en RD-016 : révèle qu'un compte existe (tradeoff connu et accepté de tout mécanisme de verrouillage, OWASP), impact limité pour un effectif de quelques employés.
- **Résolution (d)** : `ResetPasswordDto.nouveauMotDePasse` exige désormais au moins une minuscule, une majuscule et un chiffre (`@Matches`), en plus du `@MinLength(8)` déjà en place.
- **Résolution (f)** : nouvelle table `RefreshToken` (`jti`, `userId`, `expiresAt`, `revokedAt`) — l'access token reste entièrement stateless (aucune lecture base sur son chemin de vérification), seul le refresh token embarque désormais un `jti` unique. `AuthService.refresh()` révoque systématiquement l'ancien jeton avant d'émettre le nouveau (rotation à usage unique — un refresh token volé et rejoué après que le titulaire légitime a déjà rafraîchi échoue désormais, alors qu'un JWT stateless restait valable jusqu'à expiration naturelle quel que soit le nombre de réutilisations). Nouvelle route `POST /auth/logout` (idempotente, tolérante à un jeton déjà invalide) révoque explicitement le jeton courant ; câblée dans `App.tsx` (`doLogout()`, best-effort, ne bloque jamais la déconnexion locale).
- **Report explicite (e)** : non implémenté cette itération — refonte architecturale plus large que les autres sous-points (bascule de tout le flux d'authentification de `localStorage`+`Authorization: Bearer` vers un cookie `httpOnly`/`SameSite`), qui nécessiterait de concevoir une protection CSRF (absente aujourd'hui, non nécessaire tant que l'auth ne repose pas sur un cookie envoyé automatiquement par le navigateur) et de revoir le carve-out CORS documenté pour F4/F6 (`main.ts`, origine réfléchie + `credentials:false` sur les routes publiques). Le commentaire déjà présent dans `frontend/src/lib/token-storage.ts` anticipait explicitement ce report (« évolution vers des cookies httpOnly pourra être envisagée plus tard sans changer l'API de ce module »). Scindé en un sous-chantier propre CH-026(e) si repris — voir `REGISTRE_DECISIONS.md` (RD-016).
- **Reconfirmé et enrichi par l'audit Phase 11** (`docs/audits/PHASE_11_FRONTEND_QUALITE.md` §4.3) : ce sous-point (e) est le même chantier que celui identifié indépendamment par l'audit qualité frontend sous l'angle « stockage des tokens JWT en `localStorage` » — pas un nouveau chantier, volontairement pas dupliqué sous un nouvel identifiant (voir la section « Chantiers frontend — issus de l'audit qualité (Phase 11) » ci-dessous, qui renvoie ici plutôt que de recréer une fiche). L'audit Phase 11 ajoute un élément factuel neuf par rapport à RD-016 : **les deux jetons** (access et refresh) sont exposés en `localStorage`, pas seulement l'un des deux — un vol par XSS donne donc une session persistante volée (refresh incluse), pas seulement 15 minutes d'access token.
- **Note de conception rédigée (Lot D, session courante)** : `docs/security/CH-026E_NOTE_CONCEPTION_COOKIES_HTTPONLY.md` — prérequis écrit avant tout code, conformément au plan Lot D (`docs/frontend-plan/PLAN_EXECUTION_LOTS_QUALITE.md`). Conception retenue : cookies `httpOnly`/`SameSite=Lax` pour les deux jetons (access + refresh, tous deux opaques au JS — confirmé qu'aucun code frontend ne décode le JWT côté client) + protection CSRF par double-submit cookie (`makarim_csrf_token`, seul des trois cookies lisible en JS, comparé à un en-tête `X-CSRF-Token` sur toute requête mutante par un nouveau `CsrfGuard`, désactivé pour les requêtes authentifiées par Bearer — F9 mobile, non concerné). Carve-out CORS F4/F6 vérifié **sans modification requise** (routes publiques, aucun cookie de session en jeu).
- **Note de conception approuvée en principe (session courante, RD-022)** : l'utilisateur confirme le principe de conception et la contrainte de domaine (frontend/backend partageront le même domaine enregistrable en production, VPS Hostinger, sous-domaines dédiés). Nouveau document `docs/operations/CH-026E_DEPLOIEMENT_DOMAINE.md` détaillant le schéma de sous-domaines, les valeurs de production `FRONTEND_URL`/`VITE_API_URL`, l'infrastructure nginx/Certbot à ajouter (écart d'infrastructure signalé, pas encore construite), et la décision de cookies host-only (jamais d'attribut `Domain` explicite). Implémentation démarrée après ce feu vert explicite.
- **✅ Résolution (e) — implémenté et vérifié en navigateur réel (session courante)** : backend — nouveau `AuthCookieService` (seul point d'écriture des 3 cookies, réutilisé par `login`/`refresh`/`logout`), `cookie-parser` monté dans `main.ts`, double extracteur JWT (`JwtAccessStrategy` : Bearer **et** cookie, F9 mobile inchangé), nouveau `CsrfGuard` global (`APP_GUARD`) implémentant le double-submit décrit dans la note de conception. Frontend — `lib/token-storage.ts` ne gère plus aucun jeton JWT (cookies httpOnly, invisibles au JS), `lib/api-client.ts` passe `credentials:'include'` partout et ajoute `X-CSRF-Token` sur toute requête mutante.
  - **Bug réel détecté par la vérification live navigateur (pas un faux positif — exactement ce que cette discipline de test est censée attraper)** : le plan initial (§2.2/§5 de la note de conception) supposait que le frontend pourrait lire `makarim_csrf_token` directement via `document.cookie`. En pratique, frontend et backend sont deux origines distinctes (topologie réelle du projet, y compris en dev — port différent — et en production — sous-domaine différent), et `document.cookie` ne peut structurellement pas voir un cookie posé par une autre origine, même non `httpOnly`. Constat en Playwright réel : `POST /auth/logout` échouait à tort en `403` (en-tête `X-CSRF-Token` toujours vide). **Correctif** (§9, nouveau, de la note de conception — ne revient pas sur la décision RD-022 de ne jamais élargir l'attribut `Domain`) : le jeton CSRF transite en plus, une fois, dans le corps JSON de `POST /auth/login`, `POST /auth/refresh` et `GET /auth/me` ; le frontend le garde en mémoire JS (jamais `localStorage`), `GET /auth/me` (déjà appelé par `App.tsx` à chaque montage, CH-011) servant de canal de récupération après un rechargement de page. Reconfirmé en navigateur réel après correctif : login pose bien les 3 cookies avec les bons attributs, une requête mutante sans/avec un `X-CSRF-Token` invalide échoue en `403`, la même requête avec l'en-tête correct réussit, un rechargement de page suivi d'une déconnexion réussit (`204`, plus de `403`), la déconnexion efface bien les 3 cookies (`Expires` dans le passé).
- **Éléments testés** : `backend/test/auth.e2e-spec.ts` — étendu pour CH-026(e) : cookies posés avec les bons attributs à `POST /auth/login` (`httpOnly` sur access/refresh, absent sur CSRF, `SameSite=Lax` sur les trois) et corps JSON `csrfToken` identique à la valeur du cookie ; route protégée `200` avec cookie access / `401` sans ; `POST /auth/refresh` fait tourner les 3 cookies (nouveau `csrfToken` différent de celui du login, cohérent entre corps et cookie) ; `GET /auth/me` renvoie le `csrfToken` courant (canal de récupération post-rechargement) ; `POST /auth/logout` efface les 3 cookies (`Expires` dans le passé) ; **preuve sabotage/restore CSRF** (`CsrfGuard` retiré temporairement des `APP_GUARD`, confirmé que les tests « rejette » passaient alors à tort, restauré, reconfirmé) ; F9 (Bearer) et F4/F6 (routes publiques) non affectés. Suite complète rejouée après le correctif CSRF : 158/158 e2e (22/23 fichiers — `stock.e2e-spec.ts` isolé confirmé flaky pré-existant et sans lien, `DETTE_TECHNIQUE.md` point 7, repasse au vert en isolation), 32/32 unitaires backend, 48/48 unitaires frontend, build/lint backend+frontend clean.
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-016, RD-022), `docs/security/CH-026E_NOTE_CONCEPTION_COOKIES_HTTPONLY.md` (§9), `docs/operations/CH-026E_DEPLOIEMENT_DOMAINE.md`.

### CH-027 — Personnel, Planning des shifts & Pointage (« Planning & Attendance »)
- **Source** : non issu de l'audit initial (Phases 1-9) — né d'un cadrage produit demandé explicitement par l'utilisateur en session (élargissement de `CADRAGE_SESSION_TRAVAIL_STAFF.md` vers `docs/planning/CADRAGE_PLANNING_ATTENDANCE_STAFF.md`), après constat que `BR-RH-002` (planning/échange de shifts) est spécifié dans le cahier des charges d'origine mais jamais implémenté (`DATA_DICTIONARY.md` l'admet explicitement) et qu'aucune route ne permet de créer un `User` (chaînon manquant du référentiel personnel). **Priorité** : à arbitrer avec l'utilisateur (cadré et validé en principe, mais volontairement pas encore inséré dans l'ordre d'exécution du backlog — voir `BACKLOG_PRIORISE.md`). **Criticité** : Modérée (aucun blocage opérationnel actuel — le contournement via `prisma/seed.ts`/accès base directe reste possible — mais un vrai gap fonctionnel pour ajouter du personnel en production).
- **Description** : trois volets distincts à ne jamais confondre (cadrage §2) — (1) référentiel du personnel (`Employee`/`User`, existant mais sans flux de création utilisable), (2) planning prévisionnel (`ShiftPlan`, nouvelle entité, n'existe pas), (3) pointage réel (`TimeShift`/`TimeShiftSegment`, déjà complet, ADR-007). Le besoin est de rapprocher (2) et (3) en lecture seule pour la supervision manager, sans jamais fusionner les deux.
- **Livrable attendu** (une fois le timing arbitré, découpé en sous-lots A→H, cadrage §12) : migration `ShiftPlan`/`Poste`/`StatutShiftPlan` + extension `Employee` (A) ; provisioning composite `User`+`Employee` en une transaction (B) ; CRUD planning `hr/shift-plans/*` (C) ; écran référentiel personnel enrichi (D) ; agenda planning Jour/Semaine/Mois (E) ; calcul de rapprochement prévu/réel (F) ; enrichissement du portail de pointage (G, dépend de `CADRAGE_SESSION_TRAVAIL_STAFF.md`) ; résumé de reporting attendance côté `hr` — **pas** `reporting`, voir Résolution ci-dessous (H).
- **Statut** : 🗒️ **Cadré, validé en principe par l'utilisateur, non implémenté** (session courante) · **Estimation** : ~11–13,5 jours développeur (hors socle du portail de connexion `CADRAGE_SESSION_TRAVAIL_STAFF.md`, déjà chiffré séparément à ~4,5–6 jours ; ~15,5–19,5 jours combinés) · **Confiance** : moyenne (dépend des arbitrages encore ouverts, voir Résolution).
- **Résolution (arbitrages tranchés par l'utilisateur, RD-017)** : provisioning composite confirmé prioritaire ; `Employee.userId` reste obligatoire (pas de personnel « fantôme ») ; enum `Poste` (MATIN/SOIR/NUIT/AUTRE) introduit comme simple indication par défaut, jamais une contrainte dure ; `Employee.telephone` ajouté ; séparation stricte `ShiftPlan` (prévu)/`TimeShift` (réel) confirmée, rapprochement en lecture seule uniquement (jamais de table matérialisée, cohérent avec `INV-REP-001`) ; le domaine reste `hr` (pas de nouveau module de premier niveau), découpage interne `hr/employees`/`hr/attendance`/`hr/shift-plans`/`hr/payroll` retenu.
- **Point bloquant identifié en relisant la doc gelée (pas anticipé par le cadrage initial)** : la proposition d'exposer le résumé attendance sous `GET /reporting/attendance-summary` (module `reporting`) viole `docs/modules/reporting.md` §11 (« Dépendances interdites »), qui interdit explicitement à `reporting` de dépendre de `hr` (« ségrégation absolue... confidentialité de la paie RH »), et sa RBAC (`reporting:read`, Administrateur/Comptable) diffère de celle d'`hr` (`hr:read`, Administrateur/RH). Résolu par défaut (option recommandée, cadrage §13.2) : la façade reste côté `hr` (ex. `GET /hr/attendance-summary`, `hr:read`), pas sous `reporting` — respecte le référentiel gelé sans dérogation à documenter. Voir `docs/modules/reporting.md` §16 pour la trace de cette frontière.
- **Arbitrages encore ouverts** (ne bloquent pas le principe, bloquent le premier commit des sous-lots C/E/F concernés) : confirmation employé du planning (`StatutShiftPlan.CONFIRME`, v1 ou v2) ; portée de l'échange de shift self-service (BR-RH-002 d'origine, manager-only en v1 par défaut) ; granularité RBAC (`hr:planning:write` distinct de `hr:write` ou non) ; `User.nom` unique vs `nom`/`prenom` séparés. Détail et options dans `docs/planning/CADRAGE_PLANNING_ATTENDANCE_STAFF.md` §13.3.
- **Documents liés** : `docs/planning/CADRAGE_PLANNING_ATTENDANCE_STAFF.md` (cadrage complet), `docs/planning/CADRAGE_SESSION_TRAVAIL_STAFF.md` (socle portail de connexion, §7/§9 repris tel quel), `docs/governance/REGISTRE_DECISIONS.md` (RD-017), `docs/modules/hr.md` §17, `docs/modules/reporting.md` §16.
- **Prochaine étape** : timing d'insertion dans la file de développement à discuter avec l'utilisateur (dépend des chantiers déjà ouverts — CH-025/CH-024/CH-019/CH-018/CH-022 — et de leur charge relative) avant tout premier commit de code. Voir `BACKLOG_PRIORISE.md`, section « Chantiers cadrés en attente de priorisation ».

---

## Chantiers frontend — issus de l'audit qualité (Phase 11)

**Exécution organisée en 5 lots (A à E)**, chacun limité en périmètre et exécutable en 1-2 sessions — voir `docs/frontend-plan/LOTISSEMENT_CHANTIERS_QUALITE.md` (quels chantiers → quel lot → quel ordre) et `docs/frontend-plan/PLAN_EXECUTION_LOTS_QUALITE.md` (objectif, périmètre technique, risques, stratégie de test et format de compte-rendu par lot). Ne pas exécuter un chantier de cette section hors du cadre de son lot sans relire ces deux documents.

Contrairement aux chantiers CH-001 à CH-026 (issus des Phases 1-10, constat de complétude fonctionnelle), les fiches suivantes proviennent de `docs/audits/PHASE_11_FRONTEND_QUALITE.md` — un audit distinct, mené *après* que la quasi-totalité des écrans manquants de la Phase 8 ont été livrés, portant sur la qualité structurelle et non fonctionnelle du frontend (tests, accessibilité, sécurité du stockage client, résilience, performance de chargement, dette sur les fondations transverses). **Aucun code n'a été écrit pour ces chantiers à la date de leur versement** — conformément à la demande explicite de l'utilisateur (« ne code rien à ce stade, remets d'abord la documentation et la gouvernance frontend à niveau »), seule la documentation/gouvernance est produite ici ; l'exécution attend un feu vert explicite, chantier par chantier ou par lot, comme pour CH-027.

### CH-028 — Socle de tests automatisés frontend

- **Source** : `docs/audits/PHASE_11_FRONTEND_QUALITE.md` §4.1
- **Description factuelle** : `find frontend/src -iname "*.test.*" -o -iname "*.spec.*"` → 0 résultat. `frontend/package.json` ne déclare aucune dépendance de test (ni Vitest, ni Testing Library, ni Playwright en CI). Toute vérification de chantier frontend cette session (CH-003, CH-007 à CH-009, CH-011, CH-014, CH-015, CH-022) a été manuelle, en navigateur réel, non reproductible automatiquement.
- **Pourquoi ce chantier existe** : sur un PMS hôtelier où un bug de check-in/facturation/RBAC a un impact financier ou légal direct, l'absence de toute garantie automatisée côté frontend est le plus gros écart de discipline par rapport au backend (`CLAUDE.md` : « toujours contre une vraie base MySQL, jamais de mock » — discipline non négociable côté backend, totalement absente côté frontend).
- **Modules concernés** : transverse (tout `frontend/src/features/*`), en priorité les parcours à risque financier/légal (check-in/checkout, facturation, RBAC).
- **Priorité** : Important · **Criticité** : Élevée (et croissante avec chaque nouvel écran livré sans test)
- **Impact métier** : Élevé (une régression sur un parcours de facturation/check-out ne serait détectée qu'en usage réel) · **Impact sécurité** : Modéré (une régression sur le gating RBAC frontend passerait inaperçue) · **Impact conformité** : Faible direct · **Impact exploitation** : Élevé (chaque nouvel écran augmente la surface non couverte)
- **Dépendances** : aucune technique. Bénéficie de CH-032 (composants partagés) une fois construits — plus simple de tester des composants génériques que des primitives ad hoc dupliquées.
- **Prérequis** : choix d'outillage (Vitest + Testing Library, cohérent avec l'écosystème Vite déjà en place, pas de nouveau bundler à introduire) — pas d'arbitrage produit, décision technique.
- **Livrable attendu** : socle Vitest + React Testing Library installé et configuré ; couverture initiale ciblée sur les parcours critiques (gating RBAC — `AppSidebar`/`NAV_ITEMS`, flux d'authentification/refresh token — `lib/api-client.ts`, au moins un flux métier financier) plutôt qu'une couverture exhaustive immédiate.
- **Critères de validation** : (1) `npm run test` (nouveau script) exécute la suite en CI-compatible (pas de dépendance à un navigateur réel) ; (2) au moins un test couvre une régression déjà rencontrée cette session (ex. le bug latent de `lib/api-client.ts` sur un corps de réponse vide, CH-007) pour prouver la valeur du socle sur un cas réel, pas seulement un test de démonstration.
- **Statut** : ✅ **Terminé (socle initial)** — Lot A (session courante). Pratique continue au-delà de ce socle (comme CH-017 côté backend), pas un chantier clos une fois pour toutes : d'autres tests s'ajouteront naturellement à chaque futur chantier frontend.
- **Estimation de charge** : Moyenne-Élevée (4-6 jours estimés) → réalisée en une session, dans l'ordre de grandeur estimé.
- **Niveau de confiance de l'estimation** : élevée (a posteriori).
- **Lien(s) audit** : Phase 11 §4.1.
- **Résolution** : `vitest.config.ts` (config séparée de `vite.config.ts`, pas de risque sur le build de production — `tsc -b` ne l'inclut dans aucun projet référencé) + `src/test/setup.ts` (`@testing-library/jest-dom/vitest`, `afterEach(cleanup)` explicite — pas de `globals: true`, chaque test importe `describe`/`it`/`expect`/`vi` explicitement de `'vitest'`). Script `npm run test` (`vitest run`). 4 fichiers de test, 13 assertions, tous ciblés sur un risque réel plutôt qu'une couverture de démonstration : `components/ErrorBoundary.test.tsx` (confinement + réarmement automatique + bouton de retour, CH-031) ; `components/layout/AppSidebar.test.tsx` (gating RBAC CH-011, reproduit le scénario Gouvernante déjà vérifié manuellement à l'époque — `housekeeping:read`+`stock:read` sans `maintenance:read`/`rh:read`) ; `lib/api-client.test.ts` (régression réelle CH-007 — corps vide hors 204 — et CH-022 — `Content-Type` jamais fixé manuellement sur un `FormData`) ; `features/billing/components/BillingTabContent.test.tsx` (flux financier réel : montants/libellés de folio et de facture affichés fidèlement, y compris l'état vide). `npm run build`/`lint`/`test` propres, aucune régression.
- **Documents liés** : `CLAUDE.md` (section Tests, à étendre côté frontend le cas échéant), `docs/governance/REGISTRE_RISQUES.md` (R-15 — réduit, pas fermé : 4 parcours couverts sur un périmètre bien plus large), `docs/frontend-plan/PLAN_EXECUTION_LOTS_QUALITE.md` (Lot A).

---

### CH-029 — Accessibilité (a11y) frontend

- **Source** : `docs/audits/PHASE_11_FRONTEND_QUALITE.md` §4.2
- **Description factuelle** : 2 fichiers `.tsx` sur 38 utilisent un attribut `aria-*`, 0 fichier utilise `role=`, aucun plugin ESLint `jsx-a11y` configuré. Aucune gestion de focus documentée sur `components/ui/dialog.tsx` (le composant le plus réutilisé de l'application, présent dans la quasi-totalité des écrans).
- **Pourquoi ce chantier existe** : outil utilisé quotidiennement par plusieurs rôles (réception, gouvernante, comptable...), potentiellement au clavier sous pression au comptoir — un déficit d'accessibilité dégrade un usage réel, pas seulement une case de conformité.
- **Modules concernés** : transverse, en priorité `components/ui/dialog.tsx` (composant partagé le plus réutilisé) puis chaque feature au fil de l'eau.
- **Priorité** : Important · **Criticité** : Modérée
- **Impact métier** : Modéré · **Impact sécurité** : Aucun · **Impact conformité** : Faible (aucune obligation légale marocaine spécifique identifiée pour ce périmètre) · **Impact exploitation** : Modéré à élevé (usage clavier intensif au comptoir)
- **Dépendances** : aucune technique. Se combine naturellement avec CH-032 (composants partagés) — corriger l'accessibilité une fois dans un composant `dialog`/`form` générique plutôt que dans chaque écran individuellement.
- **Livrable attendu** : plugin ESLint `jsx-a11y` activé (détection continue) ; gestion de focus (trap + restauration) sur `components/ui/dialog.tsx` ; attributs `aria-label`/`aria-describedby` sur les contrôles interactifs sans texte visible ; audit manuel de navigation clavier sur au moins les 3 parcours les plus fréquents (check-in, housekeeping, facturation).
- **Critères de validation** : (1) `eslint-plugin-jsx-a11y` ne signale plus de violation bloquante sur les fichiers touchés ; (2) un dialogue ouvert piège le focus et le restaure à la fermeture, vérifié manuellement ; (3) les 3 parcours prioritaires sont utilisables intégralement au clavier, vérifié manuellement en navigateur réel (pas de suite automatisée d'accessibilité dans le périmètre initial).
- **Statut** : ✅ **Terminé** (session courante, Lot C)
- **Estimation de charge** : Moyenne (2-3 jours pour le socle + les 3 parcours prioritaires — pratique à étendre progressivement aux écrans restants).
- **Niveau de confiance de l'estimation** : moyenne.
- **Lien(s) audit** : Phase 11 §4.2.
- **Documents liés** : `docs/frontend-plan/EXIGENCES_UX.md` (à étendre avec une règle d'accessibilité explicite, absente de la version actuelle).
- **Résolution** :
  - **Plugin `eslint-plugin-jsx-a11y`** activé (`eslint.config.js`, `jsxA11y.flatConfigs.recommended`) — la version publiée (6.10.2) n'a pas encore de peer range pour ESLint 10 (sorti récemment), installée via `--legacy-peer-deps` après avoir vérifié que le plugin fonctionne réellement sous ESLint 10 (9 violations réelles détectées au premier `npm run lint`, donc le moteur de règles est bien actif malgré le peer mismatch). **Effet de bord découvert et corrigé** : `--legacy-peer-deps` désactive l'auto-installation des `peerDependencies` npm — `@testing-library/dom` (peer de `@testing-library/react`, jusque-là auto-installé) a disparu de `node_modules`, cassant `tsc -b` sur tous les fichiers de test (`screen`/`fireEvent`/`waitFor` introuvables). Corrigé en le déclarant explicitement en devDependency plutôt que de compter sur la résolution automatique de peer.
  - **9 violations réelles corrigées** : `label.tsx` (wrapper générique — association vérifiée à chaque site d'appel réel plutôt que dans la définition, règle désactivée pour ce seul fichier avec justification) ; 2 `<Label>` sans contrôle associé (`CompaniesPage.tsx`, `GuestPicker.tsx`) convertis en texte simple, ce n'étaient pas de vrais labels de formulaire ; 3 `<Label>` au-dessus d'un `<Select>` sans `htmlFor`/`id` (`AuditPage.tsx` ×2, `DocumentOcrPage.tsx`) — association câblée ; 2 `<li onClick>` (`CheckinPage.tsx`, départs du jour + séjours en cours) restructurés en `<li><button>...</button></li>` plutôt qu'un `role="button"` sur l'élément de liste (sémantique non ambiguë, nativement clavier-opérable) ; `DashboardPage.tsx` (`KpiCard`) — `onKeyDown` Entrée/Espace ajouté à un `role="button"` déjà présent mais sans clavier ; `ReservationsCalendarPage.tsx` — `ReservationBar` (voir détail d'une réservation) rendu clavier-opérable (`role="button"`, `tabIndex`, `onKeyDown`), tandis que la cellule de grille à glisser-sélectionner (création de réservation par glisser-souris multi-case) a reçu une désactivation ciblée documentée (`eslint-disable-next-line`, commentaire expliquant qu'un vrai équivalent clavier de la sélection de plage est un chantier de conception à part entière, hors périmètre de ce nettoyage de lint).
  - **Focus trap/restauration sur `dialog.tsx`** : **déjà fourni nativement** par `@base-ui/react/dialog`, qui s'appuie en interne sur `FloatingFocusManager` (floating-ui) en mode modal (vérifié en lisant `node_modules/@base-ui/react/dialog/popup/DialogPopup.js` — même discipline de vérification par le code réel que la découverte `data-active`/`aria-hidden` des lots précédents). Rien à construire ; un nouveau test (`dialog.test.tsx`, 3 cas : focus déplacé à l'ouverture, piégé pendant la tabulation, restauré au déclencheur après `Échap`) **prouve** ce comportement plutôt que de le supposer, pour qu'une régression future (mise à jour de `@base-ui/react`) soit détectée.
  - **Constat documenté, hors périmètre** : `StayDetailsDialog.tsx` implémente sa bascule Détails/Facturation/Police avec de simples `<Button>` plutôt que le composant `Tabs` livré au Lot B2 (CH-032) — nativement clavier-opérable (élément `<button>` natif) mais sans sémantique ARIA `tablist`/`tab`/`tabpanel`. Migration non effectuée ici (élargirait le périmètre de CH-029 à une reprise de composant déjà couverte par CH-032, qui est clos) — dette notée pour un futur passage.
- **Éléments testés** :
  - 3 nouveaux tests Vitest (`dialog.test.tsx`) — 45/45 tests du projet passent (+3 depuis CH-034).
  - `npm run build`/`lint` clean (0 violation `jsx-a11y` restante sur l'ensemble du projet).
  - **Vérification en navigateur réel** (Playwright, données seedées réelles, connexion `admin@makarim.test`), 10/10 assertions sur les 3 parcours prioritaires : check-in (ouverture d'un dialogue de séjour par Entrée sur un bouton focus, focus déplacé dans le dialogue, bascule Facturation activable au clavier, `Échap` ferme et restaure le focus sur le déclencheur ; sélecteur de chambre `SelectSearch` du walk-in filtré par saisie clavier) ; housekeeping (sélecteur de statut natif `Select` ouvert par Entrée, `Échap` referme sans erreur).

---

### CH-030 — Découpage de bundle (code splitting) par onglet

- **Source** : `docs/audits/PHASE_11_FRONTEND_QUALITE.md` §4.4
- **Description factuelle** : `App.tsx` importe les 18 features en top-level, `grep -r "React.lazy\|lazy(" frontend/src` → 0 résultat. Un utilisateur qui n'a accès qu'à une poignée d'onglets télécharge quand même le JavaScript des 18 modules au premier chargement.
- **Pourquoi ce chantier existe** : coût de temps de chargement initial réel sur une connexion hôtelière modeste, pour une application qui n'a aucune contrainte technique de routeur empêchant un découpage par onglet (absence de routeur déjà actée comme un choix d'architecture, `PHASE_08_FRONTEND.md` — ne bloque pas `React.lazy`, qui fonctionne indépendamment de tout routeur).
- **Modules concernés** : transverse (`App.tsx`, tous les `features/*/pages/*Page.tsx`).
- **Priorité** : Secondaire · **Criticité** : Faible à modérée (dépend de la qualité réelle de la connexion réseau à l'hôtel, non mesurée)
- **Impact métier** : Faible direct · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Modéré (temps de chargement perçu, surtout au premier login de la journée)
- **Dépendances** : aucune.
- **Livrable attendu** : chaque page de premier niveau (`features/*/pages/*Page.tsx` rendue depuis `App.tsx`) chargée via `React.lazy` + `Suspense`, avec un état de chargement cohérent avec le pattern existant (`EXIGENCES_UX.md` : jamais d'écran blanc).
- **Critères de validation** : (1) `npm run build` produit des chunks séparés par onglet (vérifiable dans la sortie Vite) ; (2) un utilisateur avec un sous-ensemble de permissions ne télécharge plus le JS des onglets auxquels il n'a pas accès (vérifiable via l'onglet réseau du navigateur) ; (3) aucune régression sur le comportement de navigation existant.
- **Statut** : ✅ **Terminé** (session courante, Lot D)
- **Estimation de charge** : Faible (1 jour).
- **Niveau de confiance de l'estimation** : élevée.
- **Lien(s) audit** : Phase 11 §4.4.
- **Résolution** : les 13 pages de premier niveau rendues depuis `App.tsx` (hors `LoginPage`/`ForgotPasswordPage`, nécessaires avant toute authentification, aucun gain à les découper) converties en `lazy(() => import(...).then((m) => ({ default: m.XxxPage })))` — export nommé, pas de `default export`, d'où le `.then` de conversion. Un unique `<Suspense fallback={<p>Chargement…</p>}>` enveloppe désormais le switch d'onglet (à l'intérieur de l'`ErrorBoundary` CH-031, pas à l'extérieur — un chunk qui échoue à charger reste confiné). `npm run build` produit désormais un chunk séparé par écran (`DashboardPage-*.js` 2 kB, `CheckinPage-*.js` 55 kB, etc.) au lieu d'un unique bundle — le chunk principal `index-*.js` passe de 571 kB à 237 kB (gzip 170 kB → 75 kB).
- **Éléments testés** : `npm run build`/`lint`/`test` clean (45/45 tests, aucune régression — pas de nouveau test unitaire dédié, le critère de validation de la fiche est explicitement réseau/navigateur, pas une suite automatisée). **Vérification en navigateur réel** (Playwright, onglet réseau intercepté, données seedées réelles) : 9/9 assertions — le module `DashboardPage` est chargé après le login, `AuditPage`/`StockPage`/`DocumentOcrPage` ne sont **pas** encore demandés à ce stade ; naviguer vers Audit déclenche le téléchargement du seul module `AuditPage` (`StockPage` toujours pas chargé) ; naviguer vers Stock déclenche à son tour `StockPage` ; revisiter Audit ne re-télécharge pas le module (déjà en cache mémoire JS) ; le contenu réel de l'écran s'affiche bien après le court état de chargement (pas d'écran blanc, conforme `EXIGENCES_UX.md`).

---

### CH-031 — Error boundary transverse

- **Source** : `docs/audits/PHASE_11_FRONTEND_QUALITE.md` §4.5 (premier point du Lot 0 du plan d'origine, jamais livré)
- **Description factuelle** : `grep -r ErrorBoundary frontend/src` → zéro résultat. Une exception de rendu React dans n'importe quel écran fait planter toute l'application (écran blanc complet), pas seulement l'onglet concerné.
- **Pourquoi ce chantier existe** : pour un outil de réception utilisé en continu, un crash total sur une erreur de rendu isolée (potentiellement dans un seul écran secondaire) est disproportionné par rapport au risque réel — et c'était déjà identifié comme prérequis du Lot 0 avant même le premier écran livré (`PLAN_DEVELOPPEMENT_FRONTEND.md`).
- **Modules concernés** : transverse (`App.tsx`, un seul composant à ajouter, qui enveloppe le rendu conditionnel par onglet).
- **Priorité** : Important · **Criticité** : Modérée à élevée (l'application entière est aujourd'hui aussi fragile que son écran le plus fragile)
- **Impact métier** : Modéré à élevé (interruption totale de service pour tous les rôles, y compris ceux dont l'écran actif n'est pas en cause si l'erreur survient dans un composant partagé) · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Élevé
- **Dépendances** : aucune. Le plus rapide à livrer de tous les chantiers de cette section — recommandé en premier (voir `BACKLOG_PRIORISE.md`, Vague 4).
- **Livrable attendu** : composant `ErrorBoundary` partagé (`components/`), enveloppant au minimum le rendu par onglet dans `App.tsx` — un crash dans un onglet affiche un message d'erreur récupérable (pattern déjà établi par `EXIGENCES_UX.md` : jamais d'écran blanc, jamais de jargon technique) plutôt que de faire tomber toute l'application ; bouton de retour au tableau de bord.
- **Critères de validation** : (1) une erreur de rendu provoquée volontairement dans un écran secondaire n'empêche plus la navigation vers les autres onglets (preuve sabotage/restore : provoquer l'erreur, confirmer le crash total actuel, ajouter le composant, reconfirmer le confinement) ; (2) le message affiché respecte `EXIGENCES_UX.md` (pas de stack trace exposée à l'utilisateur final).
- **Statut** : ✅ **Terminé** — Lot A (session courante).
- **Estimation de charge** : Faible (0,5-1 jour) → réalisée dans l'estimation.
- **Niveau de confiance de l'estimation** : élevée (a posteriori).
- **Lien(s) audit** : Phase 11 §4.5.
- **Résolution** : `components/ErrorBoundary.tsx` (composant classe — seule API React capable d'intercepter une erreur de rendu, `getDerivedStateFromError`/`componentDidCatch`), enveloppant le rendu par onglet dans `App.tsx`. `resetKey={tab}` réarme automatiquement la limite dès qu'on change d'onglet (pas besoin de cliquer le bouton pour qu'un autre écran redevienne accessible) ; le bouton « Revenir au tableau de bord » réarme explicitement et renvoie sur `dashboard`. Message conforme à `EXIGENCES_UX.md` (aucun jargon technique, aucune stack trace exposée). **Preuve sabotage/restore réalisée en navigateur réel** (pas seulement en test unitaire) : un `throw` temporaire injecté dans `StockPage.tsx`, vérifié via Playwright — la limite confine bien le crash (fallback affiché, sidebar et navigation vers les autres onglets restées fonctionnelles, bouton de retour opérationnel) — sabotage retiré, `StockPage` revérifiée fonctionnelle normalement (`git status` confirmant un fichier strictement identique à l'original après restauration). Couvert aussi par un test automatisé dédié (`components/ErrorBoundary.test.tsx`, voir CH-028).
- **Documents liés** : `docs/frontend-plan/PLAN_DEVELOPPEMENT_FRONTEND.md` (Lot 0 d'origine), `docs/frontend-plan/PLAN_EXECUTION_LOTS_QUALITE.md` (Lot A), `docs/governance/REGISTRE_RISQUES.md` (R-13, fermé).

---

### CH-032 — Composants partagés jamais construits (dette Lot 0)

- **Source** : `docs/audits/PHASE_11_FRONTEND_QUALITE.md` §2 ; `docs/frontend-plan/COMPOSANTS_PARTAGES_MANQUANTS.md`
- **Description factuelle** : `frontend/src/components/ui/` contient exactement 6 primitives (`badge`, `button`, `dialog`, `input`, `label`, `select`) — inchangé depuis la Phase 8, malgré 8 écrans construits depuis. Chaque chantier a contourné le besoin avec des primitives ad hoc : `<pre>{JSON.stringify(...)}</pre>` pour le diff avant/après de CH-015, `<input type="file">` natif sans zone de dépôt pour CH-022, onglets simulés par `useState` + boutons dans `StockPage.tsx`.
- **Pourquoi ce chantier existe** : le plan d'origine était explicite sur le coût de ce report (« le construire en dernier obligerait à retoucher tous les écrans déjà livrés »). Ce coût s'est confirmé exactement comme anticipé — 8 écrans ont chacun réinventé une solution ad hoc plutôt que de consommer un composant partagé, dette qui continuera de s'accumuler à chaque nouvel écran tant qu'elle n'est pas remboursée une fois.
- **Modules concernés** : transverse (`components/ui/`), consommé en priorité par les écrans déjà livrés qui en ont le plus besoin (`AuditPage` pour `diff-viewer`, `DocumentOcrPage` pour `file-upload`, `StockPage` pour `tabs`, tout formulaire multi-champs pour `form`).
- **Priorité** : Important · **Criticité** : Modérée (dette structurelle, pas un blocage fonctionnel immédiat)
- **Impact métier** : Faible direct · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Modéré (chaque futur écran continue de payer le coût de l'absence de socle)
- **Dépendances** : aucune dépendance entrante bloquante. CH-028 (tests) et CH-029 (a11y) bénéficient tous deux d'un socle de composants partagés (plus simple de tester/rendre accessible un composant générique que N primitives dupliquées) — ordre recommandé : composants d'abord, ou en parallèle.
- **Livrable attendu** (découpable en sous-lots indépendants, par priorité reprise de `COMPOSANTS_PARTAGES_MANQUANTS.md`) : `table` (remplace les listes ad hoc existantes) ; `form` structuré (validation, erreurs de champ homogènes) ; `date-picker` ; `tabs` (remplace le pattern `useState` de `StockPage.tsx`) ; `toast` ; `file-upload` (avec zone de dépôt, remplace l'`<input>` natif de CH-022) ; `diff-viewer` (remplace le `<pre>` de CH-015).
- **Critères de validation** : chaque composant livré est immédiatement appliqué à au moins un écran réel existant (cohérent avec la règle déjà énoncée dans `PLAN_DEVELOPPEMENT_FRONTEND.md` §5 : « ne pas construire un composant en isolation sans l'appliquer immédiatement ») — pas de composant livré sans consommateur réel dans le même chantier.
- **Statut** : ✅ **Terminé (7/7 composants, B1 à B4)**, session courante.
- **Estimation de charge** : Élevée (5-8 jours cumulés pour l'ensemble des 7 composants — décomposable en sous-lots livrables indépendamment, `table`/`form` en premier vu leur priorité Haute dans le document source).
- **Niveau de confiance de l'estimation** : moyenne (dépend du nombre de sous-lots effectivement retenus).
- **Lien(s) audit** : Phase 11 §2 ; `docs/frontend-plan/COMPOSANTS_PARTAGES_MANQUANTS.md`.
- **Résolution (B1 — `table` + `form`)** : `components/ui/table.tsx` (primitives HTML sémantiques `<table>`/`<thead>`/`<tbody>`/`<tr>`/`<th>`/`<td>`, convention `data-slot` + `cn()` alignée sur `select.tsx`/`label.tsx`) appliqué à `StockPage.tsx` (vue « mouvements », auparavant des `<div>` alignées manuellement — colonnes Date/Mouvement/Quantité/Article/Motif). `components/ui/form.tsx` (`FormField` — label + contrôle + message d'erreur/aide homogène, `aria-invalid`/`aria-describedby` posés par l'appelant) appliqué à `PoliceRecordForm.tsx` (8 champs, CH-003) : le bouton « Enregistrer » était auparavant simplement désactivé sans explication tant que les 4 champs obligatoires n'étaient pas remplis — remplacé par un bouton toujours actif + une erreur par champ manquant affichée à la tentative de soumission (`validateForm()`), effacée individuellement dès que le champ est renseigné. **Volontairement sans lib de gestion de formulaire** (pas de `react-hook-form` introduit — l'audit demandait des erreurs homogènes, pas un nouveau paradigme d'état ; chaque écran garde son `useState` local).
- **Éléments testés (B1)** : `components/ui/table.test.tsx`, `components/ui/form.test.tsx` (rendu, association label/contrôle, `role="alert"` sur l'erreur, priorité erreur > aide) et `features/police/components/PoliceRecordForm.test.tsx` (2 tests : une erreur par champ obligatoire manquant sans appel API, effacement d'une erreur de champ à la saisie sans affecter les autres). Vérifié aussi en navigateur réel (Chromium/Playwright, données réelles) : table des mouvements de stock (40 lignes réelles, bons en-têtes) ; formulaire police d'un séjour réel — soumission vide affiche l'erreur de champ, saisie du numéro de pièce l'efface, aucun appel serveur déclenché par une soumission invalide. `npm run build`/`lint`/`test` propres (20/20 tests), aucune régression.
- **Résolution (B2 — `tabs` + `date-picker`)** : `components/ui/tabs.tsx` (wrapper `@base-ui/react/tabs`, même convention `data-slot`/`cn()` que `select.tsx` — navigation clavier flèches gauche/droite gérée nativement par le primitif) appliqué à `StockPage.tsx` : remplace le bouton unique dont le libellé changeait selon la vue active (« Voir les mouvements » / « Voir les articles ») par deux vrais onglets exclusifs « Articles »/« Mouvements » (`role="tablist"`/`"tab"`/`"tabpanel"`). `components/ui/date-picker.tsx` (`DateRangeField` — deux `<Input type="date">` + validation croisée début/fin, pas un calendrier personnalisé : le sélecteur natif couvre déjà l'essentiel, la valeur ajoutée est la validation de cohérence de période, dupliquée jusqu'ici indépendamment à chaque écran) appliqué à `CreateSeasonRateForm` (`ParametersPage.tsx`, grille tarifaire saisonnière) — `canSubmit` bloque désormais aussi une période incohérente (`dateFin < dateDebut`), pas seulement des champs vides comme avant.
- **Éléments testés (B2)** : `components/ui/tabs.test.tsx` (bascule de panneau au clic, un seul panneau monté à la fois), `components/ui/date-picker.test.tsx` (2 tests : propagation des changements, erreur de période affichée puis effacée dès que la période redevient cohérente). Vérifié aussi en navigateur réel (Chromium/Playwright) : bascule Articles/Mouvements au clic ET au clavier (flèche droite, géré nativement par le primitif, jamais recodé), un seul panneau dans le DOM à la fois ; formulaire de tarif saisonnier réel — saisie d'une période incohérente (fin avant début) affiche l'erreur, champs bien étiquetés. `npm run build`/`lint`/`test` propres (23/23 tests), aucune régression.
- **Résolution (B3 — `select` recherche + `toast`)** : `components/ui/select-search.tsx` (`SelectSearch`, wrapper `@base-ui/react/combobox` — filtrage intégré via la prop `items`, réservé aux listes assez longues pour que taper devienne plus rapide que parcourir) appliqué au sélecteur de chambre de `WalkinCheckinDialog.tsx` (jusqu'à 24 chambres, remplace le `Select` simple existant). `components/ui/toast.tsx` (`toastManager` — singleton `createToastManager()`, appelable depuis n'importe quel composant sans hook local — + `<Toaster />` montée une seule fois dans `App.tsx`) appliqué au réassort de stock (`ReplenishForm`, `StockPage.tsx`) : une confirmation succède désormais à la fermeture silencieuse du dialogue (`EXIGENCES_UX.md` : « une confirmation dit ce qui s'est passé »).
- **Éléments testés (B3)** : `components/ui/select-search.test.tsx` (affichage du libellé sélectionné, filtrage à la saisie + sélection réelle via interaction clavier/souris simulée), `components/ui/toast.test.tsx` (3 tests : affichage d'un toast ajouté via `toastManager.add()`, fermeture programmatique via `toastManager.close(id)`, bouton de fermeture accessible après survol de la pile — comportement `aria-hidden` du bouton tant que la pile n'est pas survolée, propre au primitif, découvert en écrivant le test). Vérifié aussi en navigateur réel : recherche "1" dans le sélecteur de chambre du check-in walk-in filtre bien la liste et remplit le champ à la sélection ; réassort de stock réel affiche le toast « Réassort enregistré ». `npm run build`/`lint`/`test` propres (28/28 tests), aucune régression.
- **Résolution (B4 — `diff-viewer` + `file-upload`, dernier sous-lot)** : `components/ui/diff-viewer.tsx` (`DiffViewer` — comparaison champ par champ, valeurs inchangées reléguées au second plan, repli sur un affichage JSON brut à deux colonnes si avant/après ne sont pas tous deux des objets à plat) appliqué à `AuditPage.tsx` : remplace les deux blocs `<pre>{JSON.stringify(...)}</pre>` indépendants (CH-015) — qui obligeaient à comparer soi-même les deux blocs pour repérer un changement — par une vraie table Champ/Avant/Après (réutilise `components/ui/table.tsx`, B1). `components/ui/file-upload.tsx` (`FileUpload` — zone de glisser-déposer réelle en plus du clic) appliqué à `DocumentOcrPage.tsx` (CH-022) en remplacement de l'`<input type="file">` natif, enveloppé dans `FormField` (B1) pour l'étiquette/l'aide.
- **Éléments testés (B4)** : `components/ui/diff-viewer.test.tsx` (3 tests : champ modifié affiché avant/après, champ ajouté détecté, repli JSON brut sur des valeurs non-objets), `components/ui/file-upload.test.tsx` (4 tests : invite par défaut, nom de fichier affiché une fois sélectionné, sélection via le champ natif, sélection via un événement `drop` simulé). Vérifié aussi en navigateur réel avec de vraies données d'audit (1681 entrées avec détail disponible) : la table Champ/Avant/Après s'affiche correctement à l'expansion d'une entrée réelle ; sur document-ocr, sélection de fichier via clic **et** glisser-déposer réel (événement `drop` avec un vrai `DataTransfer`/`File`) toutes deux vérifiées fonctionnelles. `npm run build`/`lint`/`test` propres (35/35 tests), aucune régression.
- **CH-032 clos** : les 7 composants prévus par `COMPOSANTS_PARTAGES_MANQUANTS.md` sont livrés (`table`, `form`, `date-picker`, `tabs`, `select-search`, `toast`, `diff-viewer`, `file-upload` — 8 au sens strict car `date-picker` a été scindé en un composant `DateRangeField` dédié distinct du binôme initial, sans réduire le périmètre), chacun avec au moins un consommateur réel vérifié, jamais construit en isolation — conforme au critère de validation posé dans la fiche d'origine.
- **Documents liés** : `docs/frontend-plan/PLAN_DEVELOPPEMENT_FRONTEND.md` (Lot 0 d'origine, désormais résorbé), `docs/frontend-plan/PLAN_EXECUTION_LOTS_QUALITE.md` (Lot B, sous-lots B1 à B4, clos).

---

### CH-033 — Branding et finitions d'identité visuelle

- **Source** : `docs/audits/PHASE_11_FRONTEND_QUALITE.md` §4.6
- **Description factuelle** : `index.html` porte encore `<title>frontend</title>` (valeur par défaut Vite) et `<html lang="en">` alors que 100 % de l'interface est en français ; `public/favicon.svg` est le logo générique du template shadcn/vite, sans rapport avec l'hôtel ; le logo officiel fourni par l'utilisateur (`frontend/src/assets/brand/logo-makarim-source.jpg`) est stocké mais non câblé nulle part (ni titre, ni favicon, ni sidebar — toujours le badge « M » générique).
- **Pourquoi ce chantier existe** : finition produit visible en permanence (onglet navigateur, sidebar) pour un outil qui porte le nom et l'image de l'hôtel — écart cosmétique mais notable, déjà identifié par l'utilisateur lui-même comme un travail à faire « plus tard » au moment de fournir le logo.
- **Modules concernés** : `index.html`, `public/favicon.*`, `components/layout/AppSidebar.tsx` (badge « M »), potentiellement `police/utils/police-record.pdf.ts` et un futur module facturation (en-tête de documents PDF, déjà noté dans `frontend/src/assets/brand/README.md`).
- **Priorité** : Secondaire · **Criticité** : Faible
- **Impact métier** : Faible · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Faible (perception/qualité perçue)
- **Dépendances** : le logo source (JPEG, fond blanc) devra probablement être retravaillé (fond transparent, déclinaisons SVG/PNG à plusieurs tailles) avant intégration réelle — déjà noté dans `assets/brand/README.md`. **Prérequis** : l'utilisateur a explicitement indiqué vouloir traiter ce sujet plus tard — ne pas démarrer sans confirmation de timing, même si l'effort est faible.
- **Livrable attendu** : `<title>Makarim PMS</title>`, `<html lang="fr">`, favicon dérivé du logo réel, remplacement du badge « M » de `AppSidebar` par le logo (version compacte pour l'état replié).
- **Critères de validation** : cohérence visuelle vérifiée en navigateur réel (onglet, sidebar repliée/dépliée), aucune régression sur le contraste/lisibilité du logo sur fond clair et sombre si un mode sombre existe.
- **Statut** : à faire *(non prioritaire par choix explicite de l'utilisateur — voir Dépendances)*
- **Estimation de charge** : Faible à moyenne (0,5-2 jours selon si une déclinaison SVG propre du logo est nécessaire).
- **Niveau de confiance de l'estimation** : élevée pour le câblage, faible pour le travail graphique du logo lui-même (hors compétence de génération d'image de cette session).
- **Lien(s) audit** : Phase 11 §4.6.
- **Documents liés** : `frontend/src/assets/brand/README.md`.

---

### CH-034 — Trancher explicitement la portée responsive/mobile du frontend admin

- **Source** : `docs/audits/PHASE_11_FRONTEND_QUALITE.md` §4.7
- **Description factuelle** : 9 fichiers `.tsx` sur 38 utilisent un préfixe responsive Tailwind (`sm:`/`md:`/`lg:`). `AppSidebar` a des largeurs fixes en pixels (`w-60`/`w-16`), aucun mode replié en dessous d'un seuil d'écran. Aucun document n'énonce explicitement ce choix.
- **Pourquoi ce chantier existe** : ce n'est pas nécessairement un manque à combler — c'est d'abord une **décision produit non prise** : soit acter formellement que ce frontend admin est desktop-only (cohérent avec F9, qui a son propre client mobile séparé pour le housekeeping), soit décider qu'un usage tablette/mobile ponctuel (ex. un responsable consultant le dashboard depuis son téléphone) doit être supporté, auquel cas un travail réel de responsive est nécessaire.
- **Modules concernés** : transverse si la décision est « oui, investir » (`AppSidebar`, `AppTopbar`, chaque écran dense en colonnes).
- **Priorité** : Secondaire · **Criticité** : Faible
- **Impact métier** : Faible à modéré (selon la décision) · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Faible aujourd'hui (aucun usage mobile signalé), potentiellement modéré si un besoin réel émerge
- **Dépendances** : aucune technique. **Prérequis** : arbitrage produit explicite (`AskUserQuestion` recommandé avant tout développement) — desktop-only assumé (documenter le choix, coût nul) vs. investissement responsive réel (coût réel, à chiffrer une fois le périmètre connu).
- **Livrable attendu** : selon l'arbitrage — soit une simple ligne actée dans `docs/frontend-plan/EXIGENCES_UX.md` (« desktop-only, choix assumé »), soit un travail de responsive sur les écrans les plus consultés (dashboard en priorité).
- **Critères de validation** : la décision elle-même est documentée et traçable (`REGISTRE_DECISIONS.md`) avant tout code, quel que soit le sens retenu.
- **Statut** : ✅ **Terminé** (session courante, Lot C)
- **Estimation de charge** : Nulle pour la décision elle-même ; Modérée (2-3 jours) si l'option « investir » est retenue, limitée au dashboard et aux écrans les plus consultés.
- **Niveau de confiance de l'estimation** : élevée pour l'option « assumé », faible pour l'option « investir » (périmètre non cadré).
- **Lien(s) audit** : Phase 11 §4.7.
- **Résolution** : arbitrage tranché via `AskUserQuestion` — l'utilisateur a explicitement choisi **« Investir dans le responsive »**, contre l'option par défaut recommandée (« Desktop-only assumé ») — décision tracée dans `REGISTRE_DECISIONS.md` (RD-021). Le vrai blocage structurel identifié n'était pas les écrans eux-mêmes (`DashboardPage.tsx` avait déjà une grille KPI adaptative `grid gap-3 sm:grid-cols-2 lg:grid-cols-3`, aucune modification nécessaire) mais `AppSidebar.tsx`, dont la largeur fixe (`w-60`/`w-16`) toujours affichée consommait la largeur disponible quel que soit le viewport. Périmètre resserré en conséquence à `AppSidebar.tsx` + `AppTopbar.tsx` + câblage `App.tsx`, conforme au périmètre annoncé par la fiche elle-même :
  - `AppSidebar.tsx` : nouvelles props `mobileOpen`/`onMobileClose`. Sous le seuil `md`, la sidebar devient un tiroir superposé (`fixed inset-y-0 left-0 z-50`, translation `-translate-x-full`/`translate-x-0`) fermé par défaut, avec un fond assombri (`data-slot="sidebar-backdrop"`, `md:hidden`) cliquable pour fermer et un gestionnaire `Escape` (`useEffect` sur `window`, actif seulement quand `mobileOpen`). Le tiroir mobile affiche toujours les libellés complets (`showLabels = !collapsed || mobileOpen`) — `collapsed` (mode icônes seules) reste un concept strictement desktop, jamais appliqué en dessous de `md`. Un clic sur un item de navigation ferme automatiquement le tiroir (`handleNavigate` appelle `onNavigate` puis `onMobileClose`). Au-delà de `md`, la sidebar redevient statique (`md:static md:translate-x-0`) et le comportement `collapsed` desktop préexistant (`md:w-16`/`md:w-60`) est strictement inchangé.
  - `AppTopbar.tsx` : nouveau bouton hamburger (`Menu`, lucide-react) visible uniquement sous `md` (`md:hidden`), appelle `onOpenMobileNav`. Ajustements mineurs de responsive (padding réduit sous `sm`, titre `truncate`, libellé « Déconnexion » masqué sous `sm` — icône seule).
  - `App.tsx` : nouvel état `mobileNavOpen`, câblé sur les deux composants.
- **Éléments testés** :
  - 7 nouveaux tests unitaires Vitest (`AppSidebar.test.tsx` : pas de backdrop si fermé, fermeture au clic backdrop, fermeture à Échap, fermeture auto au clic nav, libellés complets même si `collapsed=true` en mode tiroir ; `AppTopbar.test.tsx` : titre affiché, hamburger appelle `onOpenMobileNav`) — 42/42 tests du projet passent.
  - `npm run build` et `npm run lint` clean.
  - **Vérification en navigateur réel** (Playwright, serveurs de dev réels, données seedées réelles, connexion `admin@makarim.test`) sur deux viewports : mobile (375×812) — sidebar hors écran par défaut, hamburger visible, tiroir s'ouvre avec libellés complets, backdrop visible et cliquable pour fermer, `Escape` ferme, clic sur un item navigue **et** ferme le tiroir ; desktop (1440×900) — hamburger invisible, sidebar statique pleine largeur, bascule `collapsed` toujours fonctionnelle, aucun backdrop présent. 14/14 assertions vérifiées en conditions réelles.

---

### CH-035 — Resynchronisation de la documentation frontend (`MATRICE_MODULE_API_ECRAN.md`)

- **Source** : `docs/audits/PHASE_11_FRONTEND_QUALITE.md` §3, §4.8
- **Description factuelle** : `docs/frontend-plan/MATRICE_MODULE_API_ECRAN.md` affichait encore 6 lignes 🔴 (police, audit, self-checkin staff, notifications, document-ocr, channel-manager) au moment de cet audit, alors que les six écrans correspondants sont ✅ depuis les chantiers CH-003/007/008/009/015/022 (session précédente) — personne n'avait remis ce document à jour après leur clôture, contrairement à `docs/governance/STATUT_MODULES.md`/`MATRICE_TRACABILITE.md`, qui portent une note explicite « à mettre à jour à chaque clôture de chantier ».
- **Pourquoi ce chantier existe** : un document de gouvernance désynchronisé du code réel reproduit exactement le problème que l'ensemble de ce référentiel `docs/` a été construit pour éliminer (`docs/governance/REGISTRE_DECISIONS.md`, RD-001 : « `docs/` existant se présente comme la source de vérité normative [...] mais l'audit a confirmé qu'elle est mesurablement désynchronisée du code réel »).
- **Modules concernés** : `docs/frontend-plan/MATRICE_MODULE_API_ECRAN.md` uniquement.
- **Priorité** : Secondaire · **Criticité** : Faible
- **Impact métier** : Aucun · **Impact sécurité** : Aucun · **Impact conformité** : Aucun · **Impact exploitation** : Faible (risque de décision basée sur une information périmée)
- **Dépendances** : aucune.
- **Livrable attendu** : les 6 lignes corrigées de 🔴 à ✅, synthèse de couverture recalculée (21/21 modules avec interface staff attendue, `booking-engine` restant `n/a` par conception).
- **Critères de validation** : chaque ligne corrigée cite le chantier qui l'a close.
- **Statut** : ✅ **Terminé** (session courante — corrigé directement dans le cadre de cette même session de documentation, pas laissé comme un chantier séparé à exécuter plus tard)
- **Estimation de charge** : Faible (moins de 0,5 jour — réalisée en quelques minutes).
- **Niveau de confiance de l'estimation** : élevée (a posteriori).
- **Lien(s) audit** : Phase 11 §3, §4.8.
- **Résolution** : `docs/frontend-plan/MATRICE_MODULE_API_ECRAN.md` corrigé (voir historique git) — les 6 lignes obsolètes sont désormais ✅, chacune citant le chantier de clôture correspondant.

---

## Chantiers — Plan de mise en production bêta (VPS Hostinger)

Issus de `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` (audit du 29 juillet 2026, comparatif avec `MakarimPMS_v2`), validé par l'utilisateur avant exécution. Phases A (rigueur frontend) → B (portage sélectif v2) → C (décisions/nettoyage doc) → D (infrastructure VPS réelle), un point de validation par phase.

### CH-036 — Socle de tests e2e automatisés frontend (Playwright)

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase A, demande explicite utilisateur (« le frontend de main doit être codé avec la même rigueur que le backend »).
- **Description factuelle** : le backend a 23 suites e2e réelles contre MySQL (`backend/test/*.e2e-spec.ts`). Le frontend n'a que 15 fichiers de tests unitaires de composants (Vitest, CH-028) — aucune des 16 pages `features/*/pages/*.tsx` n'est couverte, et aucun parcours utilisateur complet (connexion → action → résultat visible) n'est vérifié automatiquement. Toute vérification de chantier frontend cette session a été manuelle, en navigateur réel, non reproductible en CI.
- **Modules concernés** : transverse, ciblé sur les parcours à risque financier/légal en priorité.
- **Priorité** : Bloquant pour la bêta · **Criticité** : Élevée
- **Livrable attendu** : `@playwright/test` installé (Chromium pré-installé sur cet environnement, pas de nouveau téléchargement) ; configuration pointant vers le serveur de dev/preview + le backend réel (MySQL/Redis réels, données seedées, jamais de mock) ; 6 parcours critiques : connexion, création de réservation walk-in, check-in, check-out + paiement (jusqu'à solde à zéro), changement de statut ménage, déconnexion ; intégration au pipeline CI (`ci.yml`).
- **Critères de validation** : les 6 parcours passent en local et en CI contre une vraie base MySQL seedée ; un des tests reproduit un scénario métier réellement sensible (ex. blocage du check-out sur solde impayé, BR-SEJ-004) pour prouver la valeur du socle sur un cas réel.
- **Statut** : ✅ **Terminé** (session courante).
- **Résolution** : `@playwright/test` 1.62 installé (`frontend/playwright.config.ts`) ; `webServer` démarre backend réel (`npm run start:dev`) et frontend réel (`vite dev`) avant les tests — jamais de mock, MySQL/Redis réels, données seedées via `npx prisma db seed`. **5 scénarios** couvrant les 6 parcours prévus (réservation walk-in et check-in fusionnés : dans ce produit, un check-in walk-in *est* la création de la réservation, pas une étape séparée) : `01-auth.spec.ts` (connexion réussie, mot de passe incorrect, déconnexion + persistance de la session après reload) ; `02-checkin-checkout-paiement.spec.ts` (le scénario le plus sensible — check-in walk-in, check-out **refusé** tant que le solde HEBERGEMENT n'est pas réglé — preuve réelle de BR-SEJ-004/INV-SEJ-002 — paiement intégral via `RecordPaymentDialog`, check-out qui réussit ensuite) ; `03-housekeeping-statut.spec.ts` (ADR-003 — bascule manuelle LIBRE_PROPRE→À nettoyer→En nettoyage→LIBRE_PROPRE). Chaque scénario financier/métier sensible nettoie son propre état en fin de test (repasse la chambre à LIBRE_PROPRE) — vérifié rejouable deux fois de suite sans reseed intermédiaire (la matrice `ROOM_TRANSITIONS`, `rooms/utils/room-transitions.ts`, interdit `A_NETTOYER→OCCUPEE` : un premier essai sans ce nettoyage laissait la chambre inutilisable pour un rejeu, bug de test découvert et corrigé en vérifiant réellement une deuxième exécution, pas supposé). `ci.yml` : nouveau job `e2e-frontend` (services MySQL/Redis, migrate+seed, `playwright install --with-deps chromium`, upload du rapport HTML en cas d'échec) ; job `frontend` existant complété avec les étapes `lint`/`test` (vitest) qui manquaient totalement à la CI jusqu'ici (aucun test frontend n'était donc jamais exécuté automatiquement, même le socle Vitest de CH-028). `vitest.config.ts` exclut désormais `e2e/**` (Vitest tentait sinon d'interpréter les specs Playwright avec son propre `describe`/`it`, incompatibles). 5/5 verts, rejouable deux fois sans reseed.
- **Éléments testés** : suite complète rejouée deux fois consécutives sans reseed (idempotence vérifiée, pas supposée) ; `npm run build`/`lint`/`test` (unitaires) clean.

### CH-037 — Tests unitaires des pages frontend non couvertes

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase A.
- **Description factuelle** : complète CH-036 au niveau composant plutôt que parcours bout-en-bout — `ReservationsCalendarPage`, `CheckinPage`, `HousekeepingPage`, `GuestsPage` au minimum (pattern déjà établi par `BillingTabContent.test.tsx`/`PoliceRecordForm.test.tsx`).
- **Statut** : ✅ **Terminé (socle initial)** (session courante) — comme CH-028, pratique continue au-delà de ce socle, pas clos une fois pour toutes.
- **Résolution** : `HousekeepingPage.test.tsx` (3 tests) — seule règle métier non triviale de cet écran (ADR-003 : `RESERVEE`/`OCCUPEE`/`DEPART_PREVU` pilotés exclusivement par le système, jamais un `<Select>` manuel, remplacés par un texte explicatif), jamais vérifiée automatiquement jusqu'ici. `ReservationsCalendarPage`/`CheckinPage`/`GuestsPage` restent couverts par CH-036 (parcours e2e bout-en-bout) plutôt que dupliqués ici au niveau composant — priorité donnée à la page la moins déjà couverte plutôt qu'à une couverture exhaustive immédiate, même logique que CH-028.
- **Éléments testés** : 51/51 tests unitaires (48 précédents + 3 nouveaux), `npm run build`/`lint` clean.

### CH-038 — CRUD chambres/types de chambre (RD-024)

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase B, inspiré de `MakarimPMS_v2`.
- **Statut** : ✅ **Terminé** (session courante) — débloqué par le contournement de migration documenté en §2ter du plan bêta (`prisma migrate diff --from-url ... --to-schema-datamodel ... --script` + `prisma migrate deploy`, aucune commande destructive).
- **Écart avec v2 corrigé** (voir RD-024) : le CRUD de v2 fait une suppression physique (`prisma.room.delete()`) sans audit ni motif — violation ADR-005. `main` implémente un soft-delete (`Room.deletedAt`) avec motif obligatoire (`@MinLength(10)`) + `AuditLog` dans la même transaction, `RoomsController` dédié (`/rooms`, pas `/parameters/rooms` comme v2), clés RBAC `rooms:read`/`rooms:write` (`rooms.md` §7, jamais implémentées avant ce chantier faute de route de configuration — `rooms:read` accordé aux 6 rôles de seed, `rooms:write` réservé à l'Administrateur).
- **Livrable** : migration `20260729100706_ch038_rooms_crud` (`AuditEntity.RoomType`, `AuditAction.{CREATE,UPDATE,DELETE}_ROOM`/`{CREATE,UPDATE}_ROOM_TYPE`) ; 5 DTOs motif-obligatoire (`create-room`, `update-room`, `delete-room`, `create-room-type`, `update-room-type`) ; `RoomsService` étendu (`createRoom`/`updateRoom`/`deleteRoom`/`findAllRoomTypes`/`findRoomTypeByIdOrThrow`/`createRoomType`/`updateRoomType`, chaque mutation dans un `$transaction` avec `AuditService.writeLog`) ; `RoomsController` neuf (`GET/POST /rooms/types`, `PATCH /rooms/types/:id`, `POST /rooms`, `PATCH/DELETE /rooms/:id`) ; `RoomsModule` importe désormais `AuditModule` et déclare le controller. `GET /rooms`/`PATCH /rooms/:id/statut` restent volontairement sur `HousekeepingController` (écart RBAC assumé, `rooms.md` §16, pour ne pas casser le frontend existant). Pas de suppression de `RoomType` (dette technique documentée, `rooms.md` §16 : catégorie potentiellement référencée par des chambres/tarifs/restrictions existants). Pas d'écran frontend dédié : la ligne de plan bêta pour CH-038 (« CRUD chambres/types de chambre + route de configuration RBAC dédiée ») ne mentionne qu'une route backend, contrairement à CH-042/CH-043 qui listent explicitement des livrables frontend — une UI d'administration de l'inventaire chambres reste à cadrer séparément si le besoin se confirme.
- **Garde de suppression (soft delete)** : refusée tant que la chambre est `RESERVEE`/`OCCUPEE`/`DEPART_PREVU`, ou porte une `RoomNight` future verrouillée (réservation confirmée à venir même si la chambre est momentanément `LIBRE_PROPRE` entre deux séjours). Vérifié que le filet de sécurité soft-delete centralisé (`backend/src/prisma/soft-delete.extension.ts`, CH-006) couvre déjà `Room` sur toutes les lectures top-level (`findMany`/`findUnique`/…) utilisées par `findAllWithType`/`findById`/`findByIdWithPricing` (disponibilité réservations, check-in walk-in, planning) — aucune chambre supprimée ne peut redevenir réservable, vérifié en lisant le code plutôt que supposé.
- **Découverte réelle en testant (pas supposée)** : `prisma/seed.ts` faisait `invoice.deleteMany()` avant `invoiceDownloadToken.deleteMany()` (table introduite par CH-050, FK non-cascade `invoiceId`) — le reseed échouait en P2003. Ordre corrigé (même raison que `channelReservationImport`/`selfCheckinToken` déjà documentée juste au-dessus dans le fichier, avant `reservation.deleteMany()`).
- **Éléments testés** : `backend/test/rooms.e2e-spec.ts` (10 tests, réel MySQL) — lecture `rooms:read` pour tous les rôles de seed ; écriture `rooms:write` refusée à la Réception (403) ; motif < 10 caractères rejeté (400) ; création/modification de type de chambre avec `AuditLog` (`CREATE_ROOM_TYPE`/`UPDATE_ROOM_TYPE`) ; `roomTypeId` inexistant → 404 (type et chambre) ; création/modification/suppression (soft delete) de chambre avec `AuditLog` aux 3 étapes ; garde de suppression testée dans les deux directions (statut d'occupation actif, puis nuitée future verrouillée) avec preuve sabotage/restore (blocage `STATUTS_OCCUPATION_ACTIVE` commenté temporairement dans `RoomsService.deleteRoom` → le test « refuse la suppression tant que la chambre est engagée… » échoue bien, 200 au lieu de 409 attendu — confirmant qu'il est discriminant ; rétabli, suite revérifiée verte). `npm run build`/`lint`/`test` clean ; suite e2e complète relancée sans régression (24 suites, 172/172 tests verts, +10 par rapport à avant ce chantier).

### CH-039 — Mouvements de stock étendus (dotation chambre, linge)

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase B, inspiré de `MakarimPMS_v2`.
- **Statut** : ✅ **Terminé** (session courante) — non bloqué par la migration (aucun changement de schéma).
- **Code source de v2 vérifié avant tout portage (jamais supposé fiable), et très largement rejeté** : le module stock de v2 (`stock.service.ts`) fabrique un catalogue de 24 articles entièrement fictifs et sans rapport avec l'inventaire réel de Makarim (« Climatiseur Split Inverter 12000 BTU », « Machine Espresso (Suites) »…), calcule des dotations par type de chambre à partir de formules codées en dur (`isSuite`/`isDouble` déduits d'une recherche de sous-chaîne dans le nom), et maintient l'état de la lingerie sale en buanderie (`dirtyLinenInLaundry`) et les dotations personnalisées (`customDotations`) **en mémoire process, jamais persisté** — perdu à chaque redémarrage du serveur, incompatible avec un déploiement multi-instance. Même famille de défaut que le constat déjà fait sur les widgets v2 lors de CH-043 (données fabriquées présentées comme réelles) : rien de ce module n'a été porté tel quel.
- **Écart retenu, fondé sur le code déjà existant dans `main`** : `StockService.sortir()` (privée) et `StockMovement.roomId` existaient déjà, avec un commentaire explicite « et par une future sortie manuelle » — seul manquait un point d'entrée HTTP pour l'exposer. `docs/modules/stock.md` §8 (flux entrants, déjà rédigé avant ce chantier) décrit exactement ce besoin : « Sortie de draps ou de produits d'accueil pour la réfection d'une chambre », « Consommation d'un article de minibar signalée à l'inspection de départ », « Constat de perte ou de péremption » (BR-STK-003, motif écrit ≥10 caractères pour tout ajustement exceptionnel). Choix : une seule route générique `POST /stocks/sortie`, `roomId` optionnel (présent = consommation d'exploitation liée à une chambre ; absent = perte/casse/péremption), motif toujours ≥10 caractères (rigueur uniforme, même convention que le reste du projet plutôt que de distinguer deux DTOs pour une nuance que la spec elle-même ne sépare pas au niveau API).
- **RBAC inchangé** : `stock:write` reste Administrateur + Gouvernante uniquement (`RBAC_MATRIX.md`, déjà documenté dans `CLAUDE.md` — Maintenance explicitement exclu malgré `stock.md` §7, conflit déjà tranché avant ce chantier, non rouvert ici).
- **Livrable** : `ManualStockOutDto` (`stockItemId`, `quantite`, `motif` ≥10, `roomId?`) ; `StockService.manualStockOut()` (nouvelle méthode publique, valide `roomId` via la façade `RoomsService.findByIdOrThrow` — dépendance explicitement autorisée par `stock.md` §10 — avant de déléguer à `sortir()` existante, jamais de logique dupliquée) ; `POST /stocks/sortie` (`stock:write`) ; `StockModule` importe désormais `RoomsModule` (premier import d'un autre module métier pour ce module feuille, conforme à §10, toujours interdit d'importer housekeeping/billing/payments/guests/reservations/hr par §11).
- **Éléments testés** : 6 nouveaux tests dans `backend/test/stock.e2e-spec.ts` (réel MySQL, suite existante étendue) — sortie liée à une chambre réelle (décrément + `StockMovement.roomId` renseigné) ; sortie sans chambre pour un constat de perte/casse (`roomId` NULL) ; motif < 10 caractères rejeté (400, BR-STK-003) ; `roomId` inexistant → 404 via la façade `RoomsService` plutôt qu'une erreur de contrainte FK opaque (preuve sabotage/restore : validation commentée temporairement dans `StockService.manualStockOut` → le test échoue bien avec un 500 au lieu du 404 attendu, confirmant qu'il est discriminant ; rétabli, suite revérifiée verte) ; sortie qui rendrait la quantité négative refusée (400, INV-STK-001) sans altérer le stock ; la Réception ne peut pas déclarer de sortie (403). `npm run build`/`lint`/`test` clean ; suite e2e complète relancée sans régression (24 suites, 178/178 tests verts, +6 par rapport à CH-038).

### CH-040 — Annulation contrôlée d'une ligne de folio

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase B ; prérequis explicite de CH-049 (modification de durée de séjour) dans `PLAN_MODULE_FACTURATION.md`.
- **Statut** : ✅ **Terminé** (session courante) — non bloqué par la migration (seul ajout de schéma : 2 valeurs d'enum, `AuditAction.CANCEL_FOLIO_LINE` et `AuditEntity.FolioLine`).
- **Constat avant tout code (pas supposé)** : le schéma portait déjà `FolioLine.annulee`/`motifAnnulation` (jamais écrits nulle part), et toute la lecture en aval respectait déjà ces champs — `computeSoldeDu` (`backend/src/modules/stay/utils/solde.ts`) exclut déjà les lignes `annulee`, `BillingService.generateInvoice` filtre déjà `.annulee` avant de sommer l'hébergement. Seul manquait le point d'écriture HTTP, exactement comme pour `StockService.sortir()` en CH-039 — même schéma déjà préparé, jamais câblé.
- **Portée fixée par BR-AUD-002** (`docs/modules/billing.md` §5, déjà rédigée avant ce chantier) : « Justification écrite ≥10 caractères et écriture synchrone dans `AuditLog` de toute … soft-delete de ligne de folio **d'extras** ». Restreint donc explicitement aux lignes `EXTRA` — `HEBERGEMENT`/`TAXE_SEJOUR` sont générées par le système (jamais annulées à la main, une correction se fait par avoir total, CH-001) et `PAIEMENT` a son propre flux dédié (`payments:refund`).
- **Mêmes gardes que `addFolioLine`** (`ConflictException` si le séjour est déjà clôturé, ou si une facture `EMISE` existe déjà sur le folio) — sinon une ligne facturée disparaîtrait silencieusement du solde dû sans que la facture immuable déjà émise (INV-FAC-001) ne le reflète jamais, même défaut que le bug réel corrigé en CH-050 sur `addFolioLine` (voir plus bas dans ce fichier).
- **Livrable** : `CancelFolioLineDto` (`motif` ≥10) ; `BillingService.cancelFolioLine()` (garde idempotence — ligne déjà annulée refusée — puis garde de type EXTRA, puis les deux gardes ci-dessus, puis `$transaction` : `FolioLine.update({annulee:true, motifAnnulation})` + `AuditService.writeLog` avec la nouvelle cible `AuditEntity.FolioLine`) ; `DELETE /folios/lignes/:id` (`billing:write`, motif dans le corps — même convention que `DELETE /rooms/:id`/`DELETE /season-rates/:id`, soft delete jamais suppression physique, ADR-005).
- **Éléments testés** : 7 nouveaux tests dans `backend/test/billing.e2e-spec.ts` (réel MySQL) — annulation réussie d'une ligne EXTRA avec `motifAnnulation` persisté et `AuditLog` (`CANCEL_FOLIO_LINE`) ; motif < 10 caractères rejeté (400) ; annulation d'une ligne HEBERGEMENT refusée (409) ; annulation d'une ligne déjà annulée refusée (409, idempotence) ; `lineId` inexistant → 404 ; la Réception (`billing:read` seul) ne peut pas annuler (403) ; annulation refusée une fois une facture active émise sur le folio, avec preuve sabotage/restore (garde commentée temporairement dans `cancelFolioLine` → le test échoue bien avec 200 au lieu du 409 attendu, confirmant qu'il est discriminant ; rétabli, suite revérifiée verte). `npm run build`/`lint`/`test` clean ; suite e2e complète relancée sans régression (24 suites, 185/185 tests verts, +7 par rapport à CH-039).
- **UI frontend livrée séparément** : voir CH-053 ci-dessous (`docs/execution/PLAN_FRONTEND_PARITE_ADMIN.md`) — bouton d'annulation dans `BillingTabContent.tsx`, demande explicite de l'utilisateur d'amener le frontend à la même rigueur que le backend.

### CH-051 — Écran d'administration de l'inventaire chambres (frontend, CH-038)

- **Source** : `docs/execution/PLAN_FRONTEND_PARITE_ADMIN.md` §1, demande explicite de l'utilisateur (rigueur frontend = rigueur backend).
- **Statut** : ✅ **Terminé** (session courante).
- **Code source de `MakarimPMS_v2` vérifié avant tout portage** (`frontend/src/features/parameters/RoomsSection.tsx`, lu intégralement) — UX réutilisable (filtre par étage, onglets Chambres/Types, dialogs de création/édition) mais écarts corrigés : (1) v2 appelle `/parameters/rooms` (suppression physique, sans motif ni audit) — `main` appelle `/rooms`/`/rooms/types` (CH-038, soft delete + motif ≥10 obligatoire sur toute mutation) ; (2) v2 utilise `confirm()`/`alert()` natifs — `main` réutilise `toastManager` + erreurs inline déjà établis ; (3) v2 code en dur « 3 Étages — 24 Chambres » et des boutons `[1,2,3]` — RD-024 (inventaire configurable) impose un filtre d'étage dérivé dynamiquement des chambres réellement chargées ; (4) v2 n'a pas les champs de formule d'hébergement (`prixPetitDejeuner`/`prixDemiPension`/`prixPensionComplete`, Priorité 3, postérieurs à v2) — ajoutés au formulaire de type ; (5) pas de bouton de suppression de type (v2 en propose un qui échouerait contre notre backend — pas de route de suppression, dette technique assumée `rooms.md` §16).
- **Livrable** : `frontend/src/features/rooms/` (nouveau — `api.ts`, `types.ts` réexportant `Room`/`RoomType` de `reservations/types.ts` pour éviter toute divergence de schéma, `components/RoomsSection.tsx`) ; `reservations/types.ts` étendu (`Room.etage`, `RoomType.prix{PetitDejeuner,DemiPension,PensionComplete}`, tous optionnels pour ne casser aucun usage existant) ; nouvelle section « Chambres & types » dans `ParametersPage.tsx` (placement cohérent avec v2 et avec la portée `parameters:read` déjà gardée côté sidebar, `rooms:write` réservé à l'Administrateur comme le reste des mutations de cette page).
- **Vérification navigateur réelle** (Playwright, données seedées réelles, backend/frontend redémarrés après build) : connexion Admin → Paramètres → Chambres & types → création d'un type (« Suite Test Playwright ») → création d'une chambre à l'étage 9 avec ce type → filtre d'étage dynamique confirmé (« Étage 9 (1) » apparaît sans être codé en dur) → suppression réussie (motif ≥10) → chambre disparaît de la liste. Nettoyage post-vérification effectué (chambre et type de test purgés directement en base, aucune route de suppression de type n'existant côté backend — dette technique assumée, pas un oubli).
- **Éléments testés** : `npm run build`/`lint` clean (frontend) ; suite e2e backend inchangée (aucun changement d'API, seulement un nouveau consommateur frontend des routes déjà testées en CH-038).

### CH-052 — Sortie manuelle de stock (frontend, CH-039)

- **Source** : `docs/execution/PLAN_FRONTEND_PARITE_ADMIN.md` §2.
- **Statut** : ✅ **Terminé** (session courante).
- **Code source de v2 vérifié** : aucun bouton « sortie manuelle » n'existe dans `StockPage.tsx` de v2 (seulement réassort + les dialogs buanderie/dotation déjà rejetés en CH-039 comme données fabriquées) — rien à porter pour l'action elle-même. Seule la présentation du tableau des mouvements est reprise (v2 affiche le libellé réel de l'article et le numéro de chambre plutôt que des ID bruts).
- **Écart backend nécessaire** : `StockService.findMovements` (lecture seule, `stock.service.ts`) étendu avec `include: { stockItem: true, room: true }` — sans ce changement, le frontend n'aurait eu que des ID à afficher. Aucun changement de contrat d'écriture, suite e2e stock revérifiée verte après coup (13/13).
- **Livrable** : `ManualStockOutInput`/`StockMovement` étendu dans `stock/types.ts` ; `manualStockOut()` dans `stock/api.ts` (`POST /stocks/sortie`, déjà exposé côté backend depuis CH-039) ; `StockPage.tsx` — bouton « Sortie » symétrique du bouton « Réassort » existant, `ManualStockOutForm` (quantité, chambre optionnelle via `listRooms()` déjà réutilisé, motif ≥10 obligatoire), tableau des mouvements enrichi (libellé d'article et numéro de chambre lisibles au lieu d'ID).
- **Vérification navigateur réelle** (Playwright) : sortie manuelle de 1 unité sur le premier article, motif réel saisi, apparition immédiate dans l'onglet Mouvements avec le motif exact affiché.
- **Éléments testés** : `npm run build`/`lint` clean (frontend et backend) ; suite e2e backend complète relancée sans régression après le changement d'`include` (24 suites, 185/185).

### CH-053 — Annulation de ligne de folio EXTRA (frontend, CH-040)

- **Source** : `docs/execution/PLAN_FRONTEND_PARITE_ADMIN.md` §3.
- **Statut** : ✅ **Terminé** (session courante).
- **Aucun équivalent dans v2** (`BillingTabContent.tsx` de v2 n'a pas cette action, le backend correspondant n'y a jamais existé non plus) — conception entièrement nouvelle côté `main`, cohérente avec l'écran déjà livré en CH-050 (`AddFolioLineDialog.tsx`).
- **Livrable** : `cancelFolioLine()` dans `billing/api.ts` (`DELETE /folios/lignes/:id`) ; `BillingTabContent.tsx` — bouton « Annuler » affiché uniquement sur les lignes `EXTRA` non déjà annulées (reflet exact de la garde backend BR-AUD-002, jamais affiché sur HEBERGEMENT/TAXE_SEJOUR/PAIEMENT ni sur une ligne déjà annulée) ; motif saisi inline (même convention que `SeasonRatesSection`) ; ligne annulée affichée barrée avec son motif visible (`Annulée — <motif>`), jamais masquée (traçabilité).
- **Éléments testés** : 2 nouveaux tests dans `BillingTabContent.test.tsx` — bouton présent uniquement sur l'unique ligne EXTRA non annulée (absent sur HEBERGEMENT/TAXE_SEJOUR/PAIEMENT/EXTRA déjà annulée, motif de l'annulée déjà affiché) ; `cancelFolioLine` appelé avec le bon id et le bon motif après confirmation (`@testing-library/user-event`, déjà une dépendance du projet). `npm run build`/`lint`/`test` clean (62/62, +2). **Vérification navigateur réelle** (Playwright, séjour walk-in réel créé pour le test) : ajout d'une charge EXTRA réelle (75 MAD) → bouton Annuler → motif saisi → confirmation → ligne barrée avec motif affiché. Nettoyage post-vérification (check-out forcé du séjour de test, motif tracé dans `AuditLog`).

**Validation finale des 3 chantiers frontend (CH-051/052/053)** : build/lint/test clean sur les deux packages, suite e2e backend complète sans régression (24 suites, 185/185), vérification Playwright réelle des 3 écrans avec données seedées/créées réelles (aucune donnée fabriquée), nettoyage post-vérification systématique.

### CH-054 — Écrans reporting manquants (taxes, registre police, yield management)

- **Source** : audit comparatif `main`/`MakarimPMS_v2` demandé explicitement par l'utilisateur pour identifier des avancées réelles à reprendre. `docs/execution/PLAN_REPORTING_UI.md` (écrit avant le code).
- **Statut** : ✅ **Terminé** (session courante).
- **Trouvaille principale — pas dans v2, dans `main` lui-même** : le backend `reporting` de `main` expose déjà 4 endpoints réels et documentés « implémenté » (`docs/modules/reporting.md` §17) jamais appelés par le frontend : `GET /reporting/taxes`, `GET /reporting/police-register` (CSV+JSON), `GET /reporting/yield-forecast` (F3), `GET /reporting/export/async` + `/async/:jobId`. Même schéma déjà rencontré 3 fois cette session (CH-038/039/040) — capacité backend réelle, zéro consommateur frontend.
- **Portée retenue (3 des 4)** : `ReportingPage.tsx` gagne 3 nouvelles cartes, même convention de mise en page que l'existant (cartes empilées) — détail des taxes collectées avec section Trésor isolée (déclaration DGI), registre légal police en vue tabulaire + export CSV, prévision de revenu (Yield Management, tableau par type de chambre × jour avec badge de recommandation HAUSSE/BAISSE/MAINTIEN). `export/async` écarté : chemin technique alternatif vers le **même** CSV que l'export synchrone déjà consommé, aucune capacité métier nouvelle — dette technique documentée, pas un oubli.
- **Découvertes v2 explicitement écartées** (documentées dans `PLAN_REPORTING_UI.md` pour qu'aucun futur chantier ne les propose par erreur) : `HrAnalyticsChart.tsx` (12 mois de chiffres RH/paie fabriqués, export qui ne fait qu'un `alert()`), `CompanyDetailView.tsx` (rattachement client↔entreprise par correspondance de texte heuristique, solde de compte courant codé en dur à 0,00 MAD — `main` documente déjà cette absence de FK réelle), `specimenGenerator.ts` (fausses pièces d'identité avec noms inventés), `QrCheckinScannerDialog.tsx` (badges « Pièce ID ✓ / Fiche Police ✓ / Chambre Prête ✓ » jamais réellement vérifiés).
- **Livrable** : `reporting/types.ts` (`TaxesReport`, `PoliceRegisterEntry`, `YieldForecast` — types calqués exactement sur la réponse backend, aucune transformation/calcul dupliqué côté client) ; `reporting/api.ts` (`getTaxesReport`, `getPoliceRegister`, `exportPoliceRegister`, `getYieldForecast`) ; `ReportingPage.tsx` — 3 nouveaux composants (`TaxesReportCard`, `PoliceRegisterCard`, `YieldForecastCard`), chacun avec sa propre plage de dates indépendante (même convention que la carte « Rapport de police » déjà existante).
- **Éléments testés** : 4 nouveaux tests dans `ReportingPage.test.tsx` (nouveau fichier, la page n'avait jamais été testée) — section Trésor isolée du détail complet, état vide explicite du registre police, ligne du registre affichée fidèlement (client/chambre/pièce), recommandation tarifaire et prix suggéré affichés fidèlement par jour/type de chambre. `npm run build`/`lint`/`test` clean (66/66, +4). Suite e2e backend `reporting.e2e-spec.ts` revérifiée (5/5, aucun changement backend ce chantier). **Vérification navigateur réelle** (Playwright, données seedées/créées réelles de la session) : taxe de séjour réelle (6.00 MAD, section Trésor) affichée correctement ; registre police avec état vide réel (aucune fiche sur la période) ; prévision de revenu avec recommandation « Baisse suggérée » et prix suggéré calculé correctement (700→630 MAD, -10 %) sur un type de chambre à 0 % d'occupation réelle.

### CH-075 (Handoff final, lot 5) — Fiche client 360° : préférences éditables

- **Source** : `docs/design/design_handoff_final/screens/Clients.dc.html` (vue 360°).
- **Constat avant tout code** : `Guest.preferences` (`String?`) existe déjà côté backend et était déjà exposé dans le formulaire de **création** d'un client (`GuestsPage.tsx`, champ « Préférences »), mais jamais affiché ni modifiable une fois le client créé — aucune vue 360° ne l'exposait. Le mockup demande un onglet « type de chambre, étage, allergies/régime, notes libres », mais aucun champ structuré de ce type n'existe côté schéma (`preferences` reste un unique champ texte libre) — écart assumé : affichage en pastilles obtenues par simple découpage sur virgule du texte existant (comme le mockup lui-même le suggère avec ses 3 pastilles d'exemple, probablement dérivées du même texte libre), jamais de champs structurés inventés (pas de `typeChambrePreferee`/`etagePreferee`/`allergies` en base).
- **Livrable** : `GuestDetail` (`frontend/src/features/guests/pages/GuestsPage.tsx`) — nouvelle section « Préférences » entre l'en-tête et le formulaire de changement de catégorie : affichage en pastilles (`Badge variant="info"`) si renseigné, message explicite si vide, bouton « Modifier » → formulaire inline (`Input` + Enregistrer/Annuler) réutilisant `updateGuest()` (déjà exposé côté `guests/api.ts`, `PATCH /guests/:id`, `guests:write`, aucun nouvel endpoint).
- **Vérification navigateur réelle** (Playwright, backend/MySQL/Redis réels, client réel « TestBell Lot3 » créé lors de CH-073) : section Préférences visible et vide au départ ; édition réelle (« Étage élevé, oreiller ferme, régime sans gluten ») → 3 pastilles réelles affichées immédiatement ; **persistance vérifiée après rechargement complet de la page** (pas seulement l'état local React) — les 3 pastilles réapparaissent identiques après `page.reload()`. Nettoyage post-vérification : préférences du client de test remises à `null`.
- **Priorité** : Secondaire (confort CRM, pas une règle métier) · **Criticité** : Faible.
- **Statut** : **Terminé**.
- **Éléments testés** : `npm run build`/`lint`/`test` clean côté frontend (74/74, aucun test cassé). Vérification navigateur réelle décrite ci-dessus, y compris preuve de persistance réelle (rechargement de page, pas seulement l'état en mémoire).
- **Documents liés** : `docs/design/design_handoff_final/README.md`, CH-074 (même lot, précédent).

### CH-074 (Handoff final, lot 4) — Recherche globale ⌘K + drill-down KPI (constat)

- **Source** : `docs/design/design_handoff_final/screens/{RechercheGlobale,Dashboard}.dc.html`.
- **Drill-down KPI — constat, aucun code nécessaire** : le mockup `Dashboard.dc.html` présente des cartes KPI cliquables ouvrant un panneau de détail. Vérification du code existant avant tout travail : `DashboardPage.tsx` (`frontend/src/features/dashboard/pages/DashboardPage.tsx`) rend déjà les 4 cartes pertinentes (Taux d'occupation, Arrivées, Départs, Chambres à nettoyer) cliquables (`role="button"`, `onClick={() => onNavigate(...)}`, clavier Entrée/Espace géré) depuis un chantier antérieur (Lot #1, boutons d'action rapide). Seule la carte « Encaissé aujourd'hui » n'a pas de cible — cohérent avec `CLAUDE.md` (« billing n'a pas d'onglet dédié ici ») : aucun écran de destination honnête n'existe. Choix retenu : navigation directe vers l'écran complet plutôt que le panneau de prévisualisation tronqué du mockup (4 éléments avec un badge, sans action réelle) — un produit plus complet, pas une régression : l'utilisateur atterrit directement sur l'écran filtré et actionnable au lieu d'un aperçu qui exigerait de toute façon une seconde navigation pour agir. Aucune fiche de gouvernance dédiée pour cette partie : rien n'a été modifié.
- **Recherche globale ⌘K — livrable réel** : `frontend/src/components/layout/GlobalSearch.tsx` (nouveau), câblé dans `AppTopbar.tsx`. Raccourci `⌘K`/`Ctrl+K` global (écoute `keydown` sur `document`, fonctionne depuis n'importe quel écran) + bouton visible dans la topbar (`Rechercher…`, masqué sous `sm` faute de place). Recherche réellement cross-module sur des endpoints existants, jamais de données fabriquées : `searchGuests(q)` (recherche serveur réelle déjà exposée par `guests`, `guests:read`), `listRooms()`/`listTickets()`/`listStockItems()` (filtrage client sur des listes courtes déjà chargées ailleurs dans l'app, `reservations:read`/`maintenance:read`/`stock:read`) — chaque catégorie n'est interrogée que si la permission correspondante est présente. Résultats groupés par catégorie (Chambres/Clients/Tickets maintenance/Stock), navigation clavier (↑↓/Entrée) + clic, `Echap` ou clic sur le fond ferme la palette.
- **Écarts assumés vis-à-vis du mockup** : (1) sans routeur ni deep-linking dans ce projet (même limite déjà documentée pour self-checkin/F6), un résultat ne peut pas ouvrir « la fiche déjà ouverte » — seule la navigation vers l'onglet source est réellement câblable, cohérent avec le choix déjà fait pour `NotificationCenter` (CH-073, même lot) ; (2) pas de résultat « Actions rapides » du mockup (ex. « Créer une réservation pour la chambre 205 ») — aucun moyen de préremplir `CreateReservationDialog` depuis ce composant sans changement architectural plus large (état partagé entre `GlobalSearch` et `ReservationsCalendarPage`), hors périmètre de ce lot ; (3) pas de recherche sur les réservations elles-mêmes (le mockup affiche un résultat « RES-3391 ») — aucun endpoint de recherche texte sur `Reservation` n'existe côté backend (seules des requêtes par plage de dates), dette technique documentée plutôt que fabriquée avec une liste tronquée non représentative.
- **Vérification navigateur réelle** (Playwright, backend/MySQL/Redis réels, données réelles de la session : chambre 102 réelle, client « Lot3 TestBell » créé lors de CH-073) : bouton « Rechercher… » visible et fonctionnel ; requête « 102 » → résultat réel « Chambre 102 · Single — Libre & propre » ; requête « 101 » → chambre 101 + les 2 tickets de maintenance réels liés à cette chambre (dont ceux résolus lors de vérifications précédentes) ; requête « TestBell » → client réel « TestBell Lot3 » (recherche serveur réelle, pas un filtrage local) ; clic sur le résultat client → navigation réelle vers l'onglet Clients, palette fermée ; `Echap` ferme la palette ; `⌘K` la rouvre depuis n'importe quel écran.
- **Priorité** : Secondaire (confort de navigation, pas une règle métier) · **Criticité** : Faible.
- **Statut** : **Terminé**.
- **Éléments testés** : `npm run build`/`lint`/`test` clean côté frontend (74/74, aucun test cassé — `AppTopbar.test.tsx` déjà à jour depuis CH-073, aucun changement de props supplémentaire nécessaire ici). Vérification navigateur réelle décrite ci-dessus.
- **Documents liés** : `docs/design/design_handoff_final/README.md`, CH-073 (même lot, précédent de ce chantier).

### CH-073 (Handoff final, lot 3) — Centre d'alertes (cloche topbar) + micro-interaction check-in

- **Source** : `docs/design/design_handoff_final/screens/NotificationDetail.dc.html` et `MicroInteractionCheckin.dc.html` (dossier `design_handoff_final/`, suite de CH-071/CH-072).
- **Constat avant tout code** : le mockup `NotificationDetail` présente un flyout de cloche listant des alertes internes au personnel (ticket urgent, paiement refusé, envoi WhatsApp échoué, stock bas) avec statut lu/non-lu et « Tout marquer comme lu ». Recherche exhaustive côté backend : `NotificationLog` (F7, module `notifications`) ne journalise que les envois CRM **sortants vers les clients** (email/SMS/WhatsApp de confirmation, rappel J-1, etc.) — aucun concept d'inbox interne au personnel n'existe, donc aucun statut lu/non-lu persistable. Même famille d'écart que documentée dans CH-071/CH-072 : plutôt que fabriquer un flag de lecture fictif, agrégation en direct de 3 signaux réels déjà exposés par des endpoints existants et permission-gated côté client (`maintenance:read` → tickets urgents ouverts via `GET /maintenance-tickets?ouvert=true` ; `stock:read` → articles sous seuil via `sousSeuilAlerte`, champ déjà calculé par le backend, `GET /stocks` ; `notifications:read` → envois CRM en échec via `GET /notifications/logs`, filtrés `statut === 'ECHEC'`).
- **Écart assumé** : pas de « Tout marquer comme lu », pas de détail épinglé avec actions dédiées (« Voir le ticket »/« Marquer comme lu ») — remplacé par une navigation directe vers l'écran source réel au clic (Maintenance/Stock/Notifications, `onNavigate={setTab}`). Le badge n'est jamais un compteur figé : il reflète l'état opérationnel courant et disparaît de lui-même une fois le problème réellement résolu (ticket clos, stock réassorti, envoi retenté avec succès) — jugé plus honnête qu'un « lu » qui masquerait un problème encore actif. Aucun signal « paiement refusé » du mockup : aucun concept de paiement en échec n'est journalisé dans ce projet (`Payment` ne modélise que des règlements réussis) — non implémenté, pas simulé.
- **Micro-interaction check-in** : le mockup anime un toggle de démonstration (chambre Réservée ↔ Occupée, réversible en un clic, sans appel serveur). Le check-in réel de ce projet est une mutation non réversible en un clic (INV-SEJ-*) — traduit honnêtement par une confirmation immédiate après la mutation réelle (`toastManager`, même convention que le toast de réassort stock, CH-032 : « une confirmation dit ce qui s'est passé »), plutôt qu'un état togglable fictif. Appliqué aux deux chemins de check-in existants (`handleCheckin` depuis une réservation, `handleWalkinConfirm` pour un walk-in), avec le nom du client, le numéro de chambre réels et l'heure réelle de l'opération.
- **Livrable** : `frontend/src/components/layout/NotificationCenter.tsx` (nouveau) ; câblé dans `AppTopbar.tsx` (nouvelles props `onNavigate`/`permissions`) et `App.tsx` ; `CheckinPage.tsx` — fonction `notifyCheckinDone()` + appel dans les deux handlers de check-in.
- **Vérification navigateur réelle** (Playwright, backend/MySQL/Redis réels, données créées réellement pour le test) : cloche vide au départ (« Aucune alerte en ce moment ») ; création réelle d'un ticket de maintenance urgent (chambre 101) → badge apparaît, panneau affiche « Ticket urgent — Chambre 101 » avec le vrai libellé de panne → clic navigue réellement vers l'onglet Maintenance (titre de page vérifié) ; création réelle d'une réservation arrivant aujourd'hui (cliente « Lot3 TestBell », chambre 102) → clic sur « Check-in » dans Check-in & séjours → toast réel « Check-in effectué — Lot3 TestBell — Chambre 102 à 10:19 ». Nettoyage post-vérification effectué : ticket marqué résolu (état réaliste laissé en base, aucune route de suppression n'existe), séjour de test check-outé (forcé, motif tracé dans `AuditLog`), chambres 101/102 repassées à `LIBRE_PROPRE`.
- **Priorité** : Important · **Criticité** : Modérée.
- **Statut** : **Terminé**.
- **Éléments testés** : `npm run build`/`lint`/`test` clean côté frontend (74/74, `AppTopbar.test.tsx` mis à jour pour les nouvelles props obligatoires). Vérification navigateur réelle décrite ci-dessus.
- **Documents liés** : `docs/design/design_handoff_final/README.md`, CH-071/CH-072 (même dossier de handoff).

### CH-072 (Handoff final, lot 2) — Apps mobiles terrain : Housekeeping + Maintenance

- **Source** : `docs/design/design_handoff_final/screens/HousekeepingMobile.dc.html` et `MaintenanceMobile.dc.html` (dossier `design_handoff_final/`, 4ᵉ lot de handoff au-delà des batch1-3 déjà livrés). Suite directe de CH-071 (États UI réutilisables, même dossier).
- **Description factuelle** : deux points d'entrée mobiles autonomes, jamais rattachés à `App.tsx`/`AppSidebar` (pas de routeur dans ce projet — `frontend/src/main.tsx` teste `window.location.pathname` avant de choisir la racine React à monter). `/mobile/housekeeping` réutilise l'infrastructure F9 déjà existante et jamais consommée côté frontend (`MobileHousekeepingController`, jeton Bearer `scope: "mobile-housekeeping"`, sans refresh) via un client HTTP dédié (`housekeeping/mobile-api.ts`) plutôt que `apiRequest()` (qui suppose cookies + refresh, inapplicable à un jeton Bearer sans refresh). `/mobile/maintenance` n'a aucune infrastructure mobile dédiée côté backend — réutilise donc la session cookie/CSRF normale (`login()`, mêmes permissions `maintenance:read`/`write` que le desktop) plutôt que d'inventer un second mécanisme d'auth, cohérent avec le précédent `checkin`/`stay` (même domaine, transport différent, pas de nouveau module).
- **Écarts assumés vis-à-vis du mockup (discipline de scope honnête, aucune capacité fabriquée)** :
  1. Aucune bannière « hors ligne » sur les deux apps — aucune file de synchronisation différée n'existe côté client ni serveur.
  2. Housekeeping mobile : pas de regroupement par étage ni d'horodatage par chambre — `MobileRoomSummary` (`{id, numero, statut, typeChambre}`) ne porte ni l'un ni l'autre.
  3. Housekeeping mobile : pas de bouton « Signaler un problème » (création de ticket maintenance) — contrainte architecturale réelle, pas un oubli : `JwtAuthGuard` rejette tout jeton `scope: "mobile-housekeeping"` en dehors du préfixe `/api/mobile/housekeeping/*`, rendant un appel cross-module (`POST /maintenance-tickets`) impossible depuis cette session sans changement backend.
  4. Maintenance mobile : pas d'état intermédiaire « Prise en charge » — `MaintenanceTicket` n'a que deux états réels (ouvert / résolu via `resoluAt`), aucun champ pour un état intermédiaire en base. Seule l'action « Marquer résolu » (`PATCH /maintenance-tickets/:id/resoudre`, déjà existante) est réellement câblable.
  5. Maintenance mobile : « Assigné à vous » du mockup devient l'affichage simple du champ `assigneA` (texte libre déjà existant), sans lien vers l'utilisateur connecté — aucune FK réelle entre `MaintenanceTicket` et `User` côté schéma.
- **Bug découvert et corrigé en vérifiant en direct (pas supposé)** : `MobileLoginScreen` de `MaintenanceMobileApp.tsx` appelait `login()` mais n'appelait jamais `setCsrfToken()` sur le `csrfToken` retourné (contrairement à `LoginPage.tsx` desktop, la référence). Conséquence réelle observée : une fois connecté, l'action « Marquer résolu » (`PATCH`, mutation) échouait avec « Forbidden resource » — `CsrfGuard` ne s'auto-exempte que lorsqu'aucun cookie `makarim_access_token` n'est présent (cas Bearer pur de Housekeeping mobile F9), mais une session cookie normale comme celle-ci exige bien l'en-tête `X-CSRF-Token`. Corrigé en import `setCsrfToken` (`lib/token-storage.ts`) et en l'appelant immédiatement après connexion — revérifié en direct, l'action réussit.
- **Livrable** : `frontend/src/features/housekeeping/mobile-api.ts` (nouveau, client Bearer dédié) ; `frontend/src/features/housekeeping/pages/HousekeepingMobileApp.tsx` (nouveau, écran de connexion + liste filtrable par chip + détail avec sélection parmi `MANUAL_TARGETS`) ; `frontend/src/features/maintenance/pages/MaintenanceMobileApp.tsx` (nouveau, même structure, session cookie normale) ; `frontend/src/main.tsx` (routage par chemin, deux nouvelles racines).
- **Vérification navigateur réelle** (Playwright, backend/MySQL/Redis réels, données seedées + un ticket de test créé via curl en Administrateur pour la chambre 101, priorité URGENTE) : Housekeeping mobile — connexion via `maintenance@makarim.test`/`gouvernante@makarim.test` (jeton mobile dédié obtenu), changement de statut d'une chambre réelle via le sélecteur `MANUAL_TARGETS`, statut restauré après vérification. Maintenance mobile — connexion via session cookie normale, ticket réel visible en « Ouverts · 1 / Urgents · 1 », « Marquer résolu » d'abord en échec (« Forbidden resource », bug CSRF ci-dessus), puis succès après correctif (ticket passé en « Résolus · 1 », compteurs mis à jour immédiatement).
- **Priorité** : Important · **Criticité** : Modérée (pas de flux financier/légal, mais outil terrain quotidien réel pour Gouvernante/Maintenance).
- **Statut** : **Terminé**.
- **Éléments testés** : `npm run build`/`lint`/`test` clean côté frontend (74/74 tests unitaires, aucun test existant cassé — aucun fichier de test touché par ce lot). Vérification navigateur réelle décrite ci-dessus, y compris la preuve sabotage/restore naturelle fournie par le bug CSRF lui-même (échec réel observé avant correctif, succès réel observé après).
- **Documents liés** : `docs/design/design_handoff_final/README.md`, `docs/ADR-006-RBAC-Enforcement.md` (scope de jeton mobile), CH-071 (même dossier de handoff, lot précédent).

### CH-071 (Lot final #1) — Handoff de design final : États UI réutilisables (vide/erreur/chargement)

- **Source** : premier chantier du lot 4 (`docs/design/design_handoff_final/`, `EtatsUI.dc.html`) — dossier consolidé remplaçant/complétant `design_handoff_batch3/` avec un 4e lot de patterns transverses (vues mobiles terrain, documents imprimables, états vides/erreur/chargement, drill-down KPI, recherche globale, fiche client 360°, RBAC). `EtatsUI.dc.html` n'est pas un écran de prod isolé mais un catalogue de référence des 3 états à appliquer aux tables/listes existantes.
- **Livrable** : 3 composants réutilisables dans `@/components/ui` — `EmptyState` (icône + titre + description + action optionnelle, jamais de bouton inventé vers une capacité qui n'existe pas côté serveur), `ErrorState` (bandeau `destructive`/8% + bouton de reprise optionnel + action secondaire optionnelle), `Skeleton` (`animate-pulse` Tailwind natif plutôt que le dégradé « shimmer » CSS custom du mockup — même effet perçu, sans keyframes à maintenir).
- **Application à deux écrans réels** (même convention que les lots B1-B4 : un nouveau composant partagé s'accompagne toujours d'au moins une application réelle, jamais livré nu) : `StockPage.tsx` — squelette de chargement (4 cartes KPI + lignes de table, largeurs de barres variées comme le mockup) et état vide de la table des articles (sans bouton « + Ajouter un article » : `stock/api.ts` n'a aucun endpoint de création d'article, un tel bouton mentirait sur une capacité inexistante) ; `DocumentOcrPage.tsx` — état d'erreur du scan (remplace le simple texte rouge existant par le bandeau + bouton « Reprendre le scan », qui relance `handleScan()` sur le même fichier) et squelette du sélecteur de séjour pendant son chargement.
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, aucune régression). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux, compte `admin@makarim.test`) : chargement réel de `StockPage` avec les 3 articles seedés affichés correctement (table, KPI) ; tentative de scan réelle d'un fichier image invalide (contenu non-image renommé `.png`) sur `DocumentOcrPage` → échec réel du moteur `tesseract.js`, bandeau d'erreur affiché avec le message serveur exact (« Image illisible par le moteur OCR… ») et bouton « Reprendre le scan » fonctionnel.
- **Priorité** : Secondaire (fondations réutilisables, pas un correctif de règle métier) · **Criticité** : Faible (aucun changement de comportement backend, additif pur côté frontend).
- **Statut** : ✅ **Terminé** (ce lot). `EmptyState`/`ErrorState`/`Skeleton` restent à appliquer progressivement au reste des écrans existants au fil des chantiers futurs, pas en un seul passage exhaustif (même discipline que les composants partagés des lots B1-B4).
- **Documents liés** : `docs/design/design_handoff_final/README.md` (index des lots 1-4), `docs/design/design_handoff_final/screens/EtatsUI.dc.html`.

### CH-070 (Lot #7) — Handoff de design batch 3 : ReservationForm (nouvelle réservation)

- **Source** : suite directe de CH-064 à CH-069 — septième et dernier lot du dossier `design_handoff_batch3/`, sur `ReservationForm.dc.html`. Contrairement aux six lots précédents (reprise fidèle d'un écran existant), le README du dossier signale explicitement cet écran comme « une suggestion de nouvelle fonctionnalité produit, pas une simple reprise » — validation produit demandée avant implémentation (utilisateur : « yes build »).
- **Trouvaille — le formulaire de création existant était figé, sans édition possible** : `CreateReservationDialog.tsx` (avant ce lot) ne permettait de créer une réservation que depuis une sélection glisser-déposer sur le planning (`ReservationsCalendarPage`) — chambre et dates entièrement pré-figées par le drag, aucun sélecteur de type de chambre/chambre/dates dans le formulaire lui-même, aucun choix de canal, aucun ajustement de prix. Le mockup exige les trois. Ajout d'un point d'entrée manquant : bouton « + Nouvelle réservation » dans la barre d'outils du planning (`ReservationsCalendarPage.tsx`), seul autre point d'entrée existant restant le glisser-déposer (comportement inchangé, le formulaire est désormais préremplissable par les deux voies).
- **Trouvaille — bug réel découvert en cours de route** : `CanalReservation` (frontend, `reservations/types.ts`) était resté figé à `WALK_IN | DIRECT | BOOKING_COM` alors que F10 (channel-manager) a étendu l'enum Prisma backend avec `EXPEDIA`/`AIRBNB` depuis plusieurs chantiers déjà — `POST /reservations` (staff) accepte déjà n'importe quelle valeur de l'enum. Les `Record<CanalReservation, …>` de `ReservationsCalendarPage.tsx` (légende/couleur de barre) et `CheckinPage.tsx` (avatar/texte) auraient silencieusement affiché `undefined` pour une réservation importée via webhook OTA Expedia/Airbnb. Corrigé : type élargi, deux nouvelles entrées ajoutées à chaque `Record` (`warning` pour Expedia — même convention que le badge déjà existant dans `ParametersPage.tsx`, `violet` pour Airbnb — token déjà présent depuis CH-063, jamais utilisé pour un canal). Les 3 pills de canal du formulaire de création restent volontairement limitées à Walk-in/Direct/Booking.com (mêmes que le mockup) : Expedia/Airbnb n'arrivent que par webhook OTA, jamais saisis manuellement par un réceptionniste.
- **Écart backend assumé, résolu par composition plutôt que par nouvelle capacité serveur** : le mockup présente l'ajustement manuel du prix final dans la même fiche que la création, mais `CreateReservationDto` n'a pas de champ `prixTotalFinal` — seul `PATCH /reservations/:id` (`UpdateReservationDto`, motif ≥ 10 caractères) le permet. Résolu sans aucun changement backend : `ReservationsCalendarPage.handleConfirmCreate` enchaîne `createReservation()` puis `updateReservation()` quand la case est cochée, en réutilisant les deux endpoints déjà existants. Échec du second appel géré explicitement (réservation déjà créée non ajustée) : message d'erreur distinct (« Réservation créée mais ajustement du prix échoué : … ») plutôt qu'un échec silencieux ou trompeur, et rafraîchissement du planning malgré tout pour ne pas cacher la réservation réellement créée.
- **Écart de copie assumé, capacité non implémentée pas revendiquée** : le mockup affiche « Un client Liste noire déclenche une alerte de blocage à la confirmation — dérogation possible, réservée à l'Administrateur, journalisée dans l'audit. » — or `GuestsService.assertNotBlacklisted` rejette la catégorie `BLACKLIST` sans aucune option de dérogation côté serveur (contrairement à `checkin:force-checkout`, qui en a une). Cette phrase n'a pas été reprise : `GuestPicker` (composant partagé, inchangé) affiche déjà son propre avertissement factuel (« Ce client est en liste noire — l'opération sera refusée. ») et le formulaire ne bloque pas côté client la sélection d'un client blacklisté (cohérent avec le composant partagé, qui ne le fait pas non plus) — le blocage réel remonte du serveur (`ConflictException`) et s'affiche dans la zone d'erreur existante du formulaire. Vérifié en live : aucune fausse promesse de dérogation affichée nulle part.
- **Livrable** : `CreateReservationDialog.tsx` réécrit — pills de canal, `GuestPicker` (inchangé, réutilisé), sélecteurs en cascade type de chambre → chambre (dérivés de `rooms`, déjà chargé par le parent, aucun nouvel appel API), dates éditables, pills de formule (4, libellés déjà existants depuis CH-061), bloc Tarification (ligne de détail, total en gras, case à cocher d'ajustement manuel qui déverrouille prix final + motif obligatoire ≥ 10 caractères, valeur par défaut = prix calculé au moment de la coche). `ReservationsCalendarPage.tsx` : bouton d'entrée manuelle, `handleConfirmCreate` réécrit pour la séquence create-then-patch.
- **Priorité** : Important (demande produit directe, avec validation explicite avant implémentation) · **Criticité** : Faible (nouvel écran additif, aucune règle métier serveur modifiée — RBAC inchangé, `reservations:write`/`guests:write` déjà couverts par les rôles existants).
- **Statut** : ✅ **Terminé** (ce lot). Dossier `design_handoff_batch3/` intégralement traité (7/7 écrans).
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, aucun test dédié préexistant pour `CreateReservationDialog.tsx` — changement de comportement principalement couvert par la vérification navigateur ci-dessous, pas de nouveau fichier de test unitaire pour ce lot). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux, compte `reception@makarim.test`) : ouverture manuelle du formulaire via le nouveau bouton ; 3 pills de canal visibles et sélectionnables ; saisie d'un nouveau client via `GuestPicker` ; cascade type de chambre → chambre fonctionnelle ; dates éditées ; formule Demi-pension sélectionnée ; estimation de prix réelle chargée (820 MAD/nuit Single+Demi-pension, 3 nuits → 2 550,00 MAD, valeur serveur réelle, jamais recalculée côté client) ; case d'ajustement manuel cochée → champ prix final préempli avec le prix calculé (2 550), motif initialement vide → bouton de soumission désactivé (validation ≥ 10 caractères confirmée à la limite exacte : une saisie de 10 caractères exacts est acceptée, cohérent avec `@MinLength(10)` côté backend, ni plus ni moins strict) ; soumission réussie → réservation réellement créée en base avec **`prixTotalCalcule: "2550"` (inchangé) et `prixTotalFinal: "999"` (valeur ajustée manuellement) et `ajustementManuel: true`** confirmés par relecture directe de l'API — preuve que la séquence create-then-patch fonctionne réellement, pas seulement côté UI ; barre de réservation visible sur le planning avec l'astérisque `*` signalant un ajustement manuel. **Test du blocage liste noire réel** : client de test créé et basculé en `BLACKLIST` via l'API admin (`PATCH /guests/:id/categorie`), recherché et sélectionné dans le formulaire → avertissement affiché, bouton de soumission non désactivé côté client (comportement assumé, cf. écart ci-dessus) → tentative de soumission réellement rejetée par le serveur, message d'erreur exact du backend (« … est en liste noire : réservation et check-in impossibles. ») affiché dans le formulaire. Données de test nettoyées après vérification : réservation de test annulée (soft delete, `ANNULEE`), client blacklisté restauré en catégorie `STANDARD` ; le client créé lors du test de réservation (`PlaywrightTest Lot7`) reste en base en catégorie `STANDARD` — aucun endpoint de suppression physique pour `Guest` (cohérent avec ADR-005), même limite déjà assumée en CH-069 pour les données de test de comptes.
- **Documents liés** : `docs/design/design_handoff_batch3/README.md`, CH-064 à CH-069 (lots précédents du même dossier), `docs/modules/reservations.md` (DTOs de création/mise à jour), F10 (`docs/governance/REGISTRE_CHANTIERS.md` — chantier channel-manager, source de l'écart `CanalReservation` corrigé ici), F8 (`GET /reservations/availability`, non câblé dans ce formulaire — le mockup n'affiche que le statut statique de la chambre déjà chargée, pas une pré-vérification de conflit de dates ; resterait à faire dans un futur chantier si un besoin réel émerge).

### CH-069 (Lot #6) — Handoff de design batch 3 : Mot de passe oublié

- **Source** : suite directe de CH-064 à CH-068 — sixième lot du dossier `design_handoff_batch3/`, sur `MotDePasse.dc.html`. Restait uniquement ReservationForm après ce lot (nécessite une validation produit explicite avant implémentation, cf. CH-064 — non traité ici).
- **Trouvaille — pas un écran à créer, un écran à finir de reprendre** : contrairement aux autres écrans du dossier, `ForgotPasswordPage.tsx` existait déjà et était pleinement fonctionnel (CH-002, flux demande→réinitialisation→terminé, endpoints `POST /auth/forgot-password`/`POST /auth/reset-password` réels avec `PasswordResetToken`, anti-énumération déjà en place côté backend) — vérifié par lecture de `auth.controller.ts`/`auth.service.ts` avant tout code, aucun endpoint manquant à improviser. Seul l'habillage visuel restait en formulaire nu, sans la carte de marque déjà appliquée à `LoginPage.tsx` (CH-062).
- **Livrable** : `ForgotPasswordPage.tsx` reprend exactement la carte de `LoginPage.tsx` (liseré dégradé marine→or, logo/raison sociale via `GET /parameters/branding`, désormais transmis depuis `App.tsx` — absent avant ce lot) sur ses 3 états. Ajouts fonctionnels côté client uniquement (aucune nouvelle route serveur) : champ de confirmation du nouveau mot de passe avec validation de correspondance en direct (le backend ne reçoit toujours qu'un seul champ `nouveauMotDePasse`, `ResetPasswordDto` inchangé) ; encart distinct (teinte destructive) pour le cas « lien expiré ou déjà utilisé », séparé des autres erreurs génériques.
- **Écart assumé, texte corrigé plutôt que copié du mockup** : `MotDePasse.dc.html` affiche un indice « majuscule, chiffre et caractère spécial obligatoire » — `ResetPasswordDto` (CH-026(d)) n'exige que minuscule + majuscule + chiffre, aucun caractère spécial. Le texte affiché reflète la règle serveur réelle vérifiée dans le DTO, pas celle (plus stricte) du mockup — même discipline que les écarts de libellé déjà assumés en CH-064 (statuts de facture) et CH-067 (devise/format de date).
- **Priorité** : Important (demande produit directe, sixième lot d'un mandat de refonte déjà validé) · **Criticité** : Faible (cosmétique/UX + validation client additive — `AuthService.forgotPassword`/`resetPassword` strictement inchangés, aucun changement de RBAC : ces deux routes restent `@Public()`).
- **Statut** : ✅ **Terminé** (ce lot). Reste du dossier batch 3 : ReservationForm (validation produit requise avant implémentation).
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers — aucun test dédié préexistant pour `ForgotPasswordPage.tsx`, changement principalement visuel + validation client déjà couverte manuellement). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux) : cycle complet réel sur le compte seedé `reception@makarim.test` — demande de réinitialisation réelle (jeton `PasswordResetToken` récupéré en base pour piloter le test, aucun SMTP configuré en local donc email dégradé en simple log, comportement déjà attendu de `MailerService`) ; validation de correspondance des mots de passe confirmée (encart rouge, bouton désactivé) ; réinitialisation réelle réussie (« Mot de passe mis à jour ») ; **connexion réelle avec le nouveau mot de passe confirmée** ; réutilisation du même jeton (déjà consommé) confirmée rejetée avec l'encart « lien expiré ou déjà utilisé » distinct. Compte de test restauré après vérification (hash bcrypt de `Password123!` réappliqué, `PasswordResetToken` résiduels supprimés) — aucune donnée de démonstration laissée altérée.
- **Documents liés** : `docs/design/design_handoff_batch3/README.md`, CH-064 à CH-068 (lots précédents du même dossier), CH-002 (implémentation backend/frontend d'origine du flux mot de passe oublié), CH-026(d) (règle de complexité de mot de passe).

### CH-068 (Lot #5) — Handoff de design batch 3 : Scan pièce d'identité

- **Source** : suite directe de CH-064/065/066/067 — dernier écran « configuration/exploitation » du dossier `design_handoff_batch3/`, sur `ScanIdentite.dc.html`. Restent hors périmètre après ce lot : MotDePasse (nouvel écran auth) et ReservationForm (validation produit explicite requise avant implémentation, cf. CH-064).
- **Combine deux capacités backend déjà réelles, jusqu'ici jamais reliées entre elles côté frontend** : le scan OCR (F5, `document-ocr`, purement consultatif) et la fiche de police (CH-003, `police`, jusqu'ici uniquement accessible séjour par séjour depuis l'onglet « Police » de `StayDetailsDialog`, `checkin/components/`). La maquette montre un écran unique scanner → relire → reporter → enregistrer ; `DocumentOcrPage.tsx` (fichier et export conservés tels quels — seul l'onglet `Scan pièce d'identité` existant, déjà correctement placé/étiqueté dans `nav-items.ts`, pointait dessus) est réécrit pour intégrer un sélecteur de séjour en cours (`GET /stays/en-cours`, déjà utilisé par `CheckinPage`) et embarque directement `PoliceRecordForm` (composant réutilisé tel quel, pas dupliqué) pour le séjour choisi.
- **Nouvelle capacité ciblée sur `PoliceRecordForm`, additive et rétrocompatible** : prop optionnelle `ocrPrefill` (nouveau type exporté `OcrPrefill`) — un nouvel objet à chaque clic explicite sur « Reporter dans la fiche de police » (jamais automatique, contrairement au pré-remplissage self check-in existant qui se déclenche passivement au chargement) écrase les champs correspondants du formulaire via un effet dédié. `StayDetailsDialog` (seul autre appelant du composant) ne passe pas cette prop — comportement strictement inchangé pour lui. Mapping `DocumentOcrResult.formatDetecte` (`TD1_CIN`/`TD3_PASSEPORT`, énumération document-ocr) → `TypePiece` (`CIN`/`PASSEPORT`, énumération police) — deux énumérations restent volontairement distinctes (déjà le cas avant ce lot, document-ocr n'a jamais géré SEJOUR/AUTRE).
- **Aucun nouveau chemin d'écriture** : `upsertPoliceRecord` reste le seul point d'écriture de `PoliceRecord` (inchangé), le scan OCR reste purement consultatif (aucune image persistée, confirmé par relecture du composant réécrit) — seule l'ergonomie de saisie change (un report explicite pré-remplit des champs texte, l'utilisateur reste libre de tout corriger avant l'enregistrement réel).
- **Priorité** : Important (demande produit directe, cinquième et dernier lot « configuration/exploitation » d'un mandat de refonte déjà validé) · **Criticité** : Faible (cosmétique/ergonomie — aucun changement de RBAC : le sélecteur de séjour appelle `GET /stays/en-cours`, `checkin:read`, déjà couvert par les rôles ayant accès à l'onglet via `guests:write`, confirmé par lecture de `prisma/seed.ts` avant d'écrire le code ; le formulaire de police reste gouverné par `checkin:read`/`checkin:write`, inchangés).
- **Statut** : ✅ **Terminé** (ce lot).
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers — aucun test dédié préexistant pour `DocumentOcrPage.tsx`/`PoliceRecordForm.tsx`, changement principalement d'assemblage entre composants déjà testés/utilisés en production). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux, admin réel) : séjour walk-in réel créé pour le test (chambre 101, cliente « Verification ScanTest ») ; écran Scan pièce d'identité confirmé conforme à la maquette (3 sections, sous-titre dynamique « — Verification ScanTest, chambre 101 ») ; scan réel via le pipeline OCR tesseract.js existant (image de test sans zone MRZ réelle — comportement honnête confirmé : « Checksum invalide ou absent », avertissement « Aucune zone de lecture automatique détectée », tous les champs à `—`, aucune donnée inventée) ; bouton « Reporter » correctement désactivé tant qu'aucun séjour n'est sélectionné ; fiche de police remplie et enregistrée avec succès (`PoliceRecord` réel confirmé en base, id 4, `numeroPiece=TEST123456`) ; sélecteur de séjour mis à jour avec la coche `✓` après enregistrement, boutons « Mettre à jour la fiche »/« Télécharger le PDF » apparus comme attendu. Données de test nettoyées après vérification (`DELETE` direct sur `PoliceRecord`/`FolioLine`/`Folio`/`Stay`/`Guest` créés pour le test, `Room` 101 remise à `LIBRE_PROPRE`).
- **Documents liés** : `docs/design/design_handoff_batch3/README.md`, CH-064/CH-065/CH-066/CH-067 (lots précédents du même dossier), CH-003 (fiche de police), F5/CH-022 (scan OCR).

### CH-067 (Lot #4) — Handoff de design batch 3 : Paramètres

- **Source** : suite directe de CH-064/065/066 — même dossier `design_handoff_batch3/`, quatrième lot sur `Parametres.dc.html`. Explicitement annoncé comme exclu des 3 premiers lots (« sa taille et sa complexité justifient un lot dédié », CH-066) : `ParametersPage.tsx` faisait déjà 1000+ lignes avant ce lot.
- **Changement structurel, pas seulement visuel** : le commutateur d'onglets à 5 boutons (`identite`/`taxes`/`saisons`/`chambres`/`channel-manager`, un seul rendu à la fois) est remplacé par une page unique à sections empilées avec sous-navigation ancrée sticky (`<nav>` de liens `#id`, `position: sticky`), fidèle à la maquette. Seuls les **4 sous-modules couverts par la maquette** (Identité de l'hôtel, Taux & taxes, Grille tarifaire, Canaux OTA) suivent ce nouveau layout — « Chambres & types » (`RoomsSection`, absent de `Parametres.dc.html`) reste une 5ᵉ section empilée avec son propre encart « hors périmètre de la maquette — inchangé », toujours accessible via un 5ᵉ lien de sous-navigation plutôt que silencieusement retiré (même discipline que le rapport « arrivées du jour » conservé en CH-066) — `RoomsSection.tsx` lui-même n'a reçu aucune modification.
- **Chaque section redevient sa propre carte autonome** (`bg-card rounded-lg border`, en-tête `text-xs font-bold uppercase`), même pattern que `NotificationsPage.tsx`/`AuditPage.tsx` (CH-066) plutôt qu'un unique conteneur partagé.
- **`TaxRatesSection`** : table dense (Type/Taux/Applicable à/Actif depuis/Action) remplace la pile de cartes par taux ; un unique panneau d'édition partagé (`editingId`) apparaît sous la table au clic sur « Modifier », même convention que le panneau d'édition de template dans `NotificationsPage.tsx`.
- **`SeasonRatesSection` — capacité backend réelle raccordée pour la première fois** : `updateSeasonRate` (`PATCH /season-rates/:id`) existe côté API (`parameters/api.ts`) depuis l'introduction du module mais n'était jamais appelé par le frontend — seuls `createSeasonRate`/`deleteSeasonRate` étaient câblés (même schéma « capacité backend réelle, zéro consommateur frontend » que CH-054/038/039/040). La maquette montre un lien « Modifier » par ligne : ajouté avec un panneau d'édition partagé (libellé/dates via `DateRangeField`/prix, motif) réutilisant cette méthode jusque-là orpheline. Le lien « Supprimer » (capacité déjà existante avant ce lot, absente de la maquette) n'a pas été retiré — conservé comme second lien par ligne avec son propre panneau de confirmation partagé, pour ne pas régresser une fonctionnalité déjà réelle. Le formulaire de création (`+ Nouveau tarif`, jusqu'ici une `Dialog` modale) devient un panneau « Nouvelle période tarifaire » toujours visible en bas de carte, fidèle à la maquette (aucune modale sur cet écran) — les 3 panneaux (édition/suppression/création) sont mutuellement exclusifs dans le même emplacement, jamais superposés.
- **`ChannelManagerSection`** : même bascule `Dialog` → panneau « Nouvelle correspondance » toujours visible, badges de canal colorés par variant sémantique déjà existant (`info`=Booking.com, `warning`=Expedia, `destructive`=Airbnb — aucun nouveau token, 3 teintes déjà en place les plus proches des couleurs exactes de la maquette) au lieu du texte brut précédent. « Retirer » (suppression, seule action d'écriture existante sur les mappings — pas de `updateChannelMapping` côté backend) ouvre un panneau de confirmation partagé au lieu d'un champ motif toujours affiché par ligne.
- **`HotelIdentitySection` — deux champs réels exposés pour la première fois** : `devise`/`formatDate` existent sur `HotelConfig`/`UpdateHotelConfigInput` et sont acceptés sans contrainte d'énumération par `UpdateHotelConfigDto` (`@IsString()` simple, aucun `@IsIn`) depuis l'origine du module, mais le formulaire ne les affichait jamais (ni lecture ni écriture) — confirmé par lecture de `update-hotel-config.dto.ts` avant d'écrire le code. Ajoutés en `Input` texte simple (pas un `<select>` à options fixes comme le mockup MAD/EUR/USD, faute de contrainte serveur réelle à refléter — inventer une liste fermée aurait été plus restrictif que ce que le backend autorise réellement).
- **Priorité** : Important (demande produit directe, quatrième et dernier lot « configuration » d'un mandat de refonte déjà validé) · **Criticité** : Faible (cosmétique/UI + raccordement d'une capacité déjà réelle — aucun changement de RBAC ni de règle métier serveur ; `updateHotelConfig`/`updateTaxRate`/`createSeasonRate`/`updateSeasonRate`/`deleteSeasonRate`/`createChannelMapping`/`deleteChannelMapping` tous inchangés côté service).
- **Statut** : ✅ **Terminé** (ce lot). Reste du dossier batch 3 : ScanIdentite, MotDePasse (nouvel écran auth), ReservationForm (validation produit requise avant implémentation, cf. CH-064).
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers — aucun test dédié préexistant pour `ParametersPage.tsx`, changement principalement visuel + raccordement d'un endpoint déjà testé côté backend). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux, admin réel) : les 5 sections empilées affichées avec les données réelles (10 `SeasonRate` seedés sur 2 saisons × 5 types de chambre, 3 `TaxRateConfig`) ; modification réelle d'un taux TVA (10 %→16 %→retour 10 %, confirmé en base après chaque étape) via le panneau d'édition partagé ; création réelle d'un mapping OTA (Booking.com, `RT-VERIF-001` → Single) via le panneau toujours visible, badge coloré confirmé, puis retrait réel via le panneau de confirmation (base revérifiée vide après nettoyage) ; section Chambres & types confirmée fonctionnelle et inchangée (24 chambres, 5 types, formulaires de configuration existants intacts).
- **Documents liés** : `docs/design/design_handoff_batch3/README.md`, CH-064/CH-065/CH-066 (lots précédents du même dossier).

### CH-066 (Lot #3) — Handoff de design batch 3 : Reporting + Notifications + Audit

- **Source** : suite directe de CH-064/CH-065 — même dossier `design_handoff_batch3/`, troisième lot sur `Reporting.dc.html`/`Notifications.dc.html`/`Audit.dc.html`. `Paramètres` (`ParametersPage.tsx`, déjà 1000+ lignes, 5+ onglets : HotelConfig, Taxes, SeasonRates, RateRestrictions, Channel Manager, Logo) volontairement **exclu de ce lot** — sa taille et sa complexité justifient un lot dédié plutôt que d'être compressé en fin de session, cohérent avec la discipline de dimensionnement déjà appliquée (RH+Stock séparé de Clients+Entreprises en CH-064/065).
- **`ReportingPage.tsx` — changement comportemental, pas seulement visuel** : les 4 sections (résumé financier, taxes, registre police, prévision yield) avaient chacune leur propre plage de dates et leur propre bouton de calcul — 4 formulaires indépendants. Le mockup montre une plage de dates unique en tête d'écran (« Actualiser les rapports ») qui rafraîchit tout à la fois ; implémenté avec `Promise.allSettled` (un rapport en échec n'empêche pas les 3 autres de s'afficher) et une valeur par défaut sensée (mois en cours à aujourd'hui) qui déclenche un chargement automatique au montage — écran qui n'était plus jamais vide à l'ouverture, contre 4 formulaires vides avant. Le rapport de police « arrivées du jour » (un seul jour, capacité réelle distincte du registre sur plage de dates) n'apparaît pas dans le mockup mais a été conservé en action secondaire compacte plutôt que silencieusement supprimé — rien ne justifiait de retirer une capacité déjà réelle.
- **`NotificationsPage.tsx`** : commutateur Templates/Journal remplacé par une page à sections empilées (KPI, Modèles, Journal), panneau d'édition inline pour un seul template à la fois (au lieu de chaque ligne pré-dépliée en formulaire complet en permanence, illisible au-delà de 3-4 templates). **Bug réel détecté et corrigé pendant la vérification navigateur** (pas seulement un ajustement cosmétique) : deux lignes de templates réels s'affichaient avec une colonne « Événement » vide — `EvenementNotification` (type frontend) et `EVENEMENT_LABEL` n'incluaient que 4 des 5 valeurs d'énumération réellement utilisées côté backend (`FACTURE_EMISE` manquant, confirmé par `SELECT DISTINCT evenement FROM NotificationTemplate` en base réelle) ; écart pré-existant avant ce lot (l'ancien layout aurait affiché le même vide, simplement moins visible dans une carte pré-remplie que dans une colonne de tableau dense), corrigé ici puisque découvert pendant la vérification de ce chantier.
- **`AuditPage.tsx`** : 4 cartes KPI ajoutées (Entrées période / Utilisateurs distincts / Changements Blacklist / Dernière entrée), toutes calculées côté client depuis les résultats déjà chargés par la recherche (jamais un second appel réseau) ; liste de cartes remplacée par une table dense (Horodatage/Utilisateur/Action/Entité/ID/Motif/Détail) avec un unique panneau `DiffViewer` partagé pour la ligne sélectionnée (au lieu d'un `DiffViewer` par ligne potentiellement dépliée) ; recherche déclenchée automatiquement au montage avec la même plage par défaut (mois en cours) que Reporting, pour la cohérence entre les deux écrans du même lot. **Écart assumé documenté dans le code** : le mockup affiche « #3 (Admin) » (rôle entre parenthèses) pour l'utilisateur d'une entrée — `AuditLogEntry` n'expose que `userId` (aucune jointure vers un nom/rôle côté backend), donc seul `#<id>` est affiché, jamais un rôle fabriqué.
- **Priorité** : Important (demande produit directe, troisième lot d'un mandat de refonte déjà validé) · **Criticité** : Faible pour Reporting/Audit (consultatif, `INV-REP-001`/`INV-AUD-001` inchangés) — légèrement plus élevée mais toujours maîtrisée pour Notifications (le chemin d'écriture `updateTemplate`/`createTemplate` reste strictement inchangé, seule l'UI de déclenchement change).
- **Statut** : ✅ **Terminé** (ce lot). Reste du dossier batch 3 : Paramètres (lot dédié à venir), ScanIdentite, MotDePasse (nouvel écran auth), ReservationForm (validation produit requise avant implémentation, cf. CH-064).
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers) — `ReportingPage.test.tsx` entièrement réécrit (4 tests, mêmes scénarios métier que CH-054 mais adaptés au fetch automatique sans saisie de dates manuelle, plus plus besoin de cibler une carte par sélecteur d'ID puisqu'il n'y a plus qu'une seule plage de dates partagée) ; aucun test dédié pour Notifications/Audit (pas de fichier de test préexistant, changement principalement visuel + le bug `FACTURE_EMISE` corrigé). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux, admin réel, 2157 entrées d'audit réelles accumulées sur les sessions précédentes) : Reporting avec résumé financier/taxes/yield chargés automatiquement au montage (données réelles, ex. solde brut encaissé 10 000 MAD) ; Notifications avec 6 templates réels affichés (dont la ligne `FACTURE_EMISE` correctement libellée après correctif) et panneau d'édition inline fonctionnel ; Audit avec KPI réelles (2157 entrées, 308 changements blacklist cumulés, dernière entrée réelle) et bascule « Voir diff » confirmée fonctionnelle sur une ligne réelle (`VALIDATE_PAYSLIP`, résidu traçable de la vérification du Lot #2 — cohérent avec la nature append-only du registre, la suppression du `PaySlip` de test en CH-065 n'efface jamais son entrée d'audit).
- **Documents liés** : `docs/design/design_handoff_batch3/README.md`, CH-064/CH-065 (lots précédents du même dossier).

### CH-065 (Lot #2) — Handoff de design batch 3 : RH + Stock

- **Source** : suite directe de CH-064 (Lot #1, Clients/Entreprises) — même dossier `design_handoff_batch3/`, deuxième lot resserré sur la catégorie sidebar « Ressources & stocks » (RH.dc.html, Stock.dc.html).
- **`HrPage.tsx` — remplacement du commutateur d'onglets par une page unique à sections empilées** (KPI, Employés, Historique de pointage, Paie), même convention que GuestsPage/CompaniesPage/StockPage plutôt qu'une navigation par onglets. `employees` chargé une seule fois au niveau de la page et partagé aux 3 sections — nécessaire pour résoudre le nom d'un employé sur l'historique de pointage (`TimeShift` n'embarque que `employeeId`, jamais un nom) sans dupliquer un appel réseau. Panneau d'ajustement de segment transformé d'un `Dialog` modal en panneau inline en bas de la carte « Historique de pointage » (reproduit fidèlement le mockup, qui n'utilise aucune modale sur cet écran).
- **KPI partiellement omises, pas fabriquées** : le mockup affiche 4 cartes (Employés actifs, Masse salariale, **En service maintenant**, **Bulletins à valider**) — seules les deux premières sont calculables honnêtement côté client depuis les données déjà chargées (`employees.filter(actif)`, somme des `salaireBase`). Les deux autres exigeraient un endpoint d'agrégation qui n'existe pas côté backend : `statutCourant()` ne renvoie que le statut de l'utilisateur courant (pas un décompte global de tous les employés en service), et `PayrollController` n'expose aucune route listant les `PaySlip` en brouillon (`findSlipsValides()` ne renvoie que `estValide: true`) — confirmé par lecture directe de `payroll.controller.ts`/`payroll.service.ts` avant d'écrire le code. Omises plutôt qu'inventées, même discipline que le bloc « Compte courant » de `CompaniesPage` (CH-064).
- **`StockPage.tsx` — même refonte en page unique à sections empilées**, remplace le commutateur d'onglets Articles/Mouvements (composant `Tabs`, introduit en Lot B2/CH-032, retiré ici — pas de régression, le composant reste utilisé ailleurs, ex. `ParametersPage`). KPI réelles calculées côté client (`Articles suivis`, `Sous seuil critique` depuis `StockItem.sousSeuilAlerte`, `Entrées`/`Sorties aujourd'hui` filtrées sur `StockMovement.createdAt` du jour courant, jamais un chiffre du mockup recopié tel quel). Table articles dense avec colonne « Kit accueil » (Oui/Non, `StockItem.kitAccueil`, déjà en base mais jamais affiché avant ce lot) et actions « Réassort »/« Sortie » en lien inline plutôt qu'en boutons de carte — déclenchent les mêmes `Dialog`s de formulaire déjà existants et testés (`ReplenishForm`/`ManualStockOutForm`), aucun changement de comportement au-delà du déclencheur visuel.
- **Écart corrigé en cours de route** : le mockup utilise des codes d'article courts (`SAV-001`, 7 caractères) qui ont dicté une colonne « Code » de 70px — les codes réels seedés (`AMEN-SOAP-01`, `LINGE-DRAP-01`, jusqu'à 13 caractères) s'y tronquaient en `AMEN-SOA…`. Colonne élargie à 110px après vérification navigateur réelle avec les données seedées, plutôt que de garder la largeur du mockup basée sur des données d'exemple non représentatives.
- **Priorité** : Important (demande produit directe, deuxième lot d'un mandat de refonte déjà validé) · **Criticité** : Faible (cosmétique/UI, aucun changement de RBAC, de règle métier serveur ou de chemin d'écriture — `createEmployee`/`ajusterSegment`/`calculerPaie`/`validerPaie`/`replenishStock`/`manualStockOut` tous inchangés).
- **Statut** : ✅ **Terminé** (ce lot). Reste du dossier batch 3 (Reporting, Notifications, Audit, Paramètres, ScanIdentite, MotDePasse, ReservationForm) hors périmètre.
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers — aucun test dédié à HrPage/StockPage n'existait avant ce lot, aucune régression introduite). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux, admin réel) : écran RH testé bout-en-bout avec un employé réellement créé (compte `rh@makarim.test`, salaire 7200 MAD) — KPI mises à jour immédiatement (1 actif, 7200 MAD), calcul de bulletin réel (retenue CNSS 268,80 MAD, AMO 162,72 MAD, net 6768,48 MAD — cohérent avec les taux CNSS/AMO configurés), validation réelle du bulletin confirmée en base (`estValide=1`) malgré un rafraîchissement de liste asynchrone pas capturé à temps par la capture d'écran. Écran Stock vérifié avec les 3 articles et 8 mouvements seedés réels (KPI sous seuil critique à 0, sorties du jour à 20 reflétant le décompte automatique du kit d'accueil déclenché par des ménages validés lors de sessions antérieures). Données de test nettoyées après vérification (`DELETE` sur `PaySlip`/`Employee` créés pour le test).
- **Documents liés** : `docs/design/design_handoff_batch3/README.md`, CH-064 (Lot #1 du même dossier).

### CH-064 (Lot #1) — Handoff de design batch 3 : Clients + Entreprises

- **Source** : demande explicite de l'utilisateur — troisième dossier de handoff déposé (`design_handoff_batch3/`, 11 écrans : Clients, Entreprises, RH, Stock, Reporting, Notifications, Audit, Paramètres, ScanIdentite, MotDePasse, ReservationForm), ajouté au dépôt tel quel via PR #31 (`docs/design/design_handoff_batch3/`). Portée de ce lot volontairement resserrée aux deux premiers écrans de la catégorie sidebar « Clients & partenaires » plutôt qu'aux 11 d'un coup — cohérent avec la pacing « par lots successifs » déjà en place depuis CH-059/060/061.
- **Nouveau variant de badge — `brand`** : le mockup (`Clients.dc.html`) attribue à la catégorie CRM `ENTREPRISE` une pastille teintée dans `--primary` (navy), distincte des pastilles déjà existantes `success`/`warning`/`info`/`destructive` (dont les hue exactes matchent respectivement le vert/or/bleu-violet/rouge du mockup et ont donc été réutilisées telles quelles pour `VIP`→`warning`, `AGENCE`→`info`, `BLACKLIST`→`destructive`). Ajouté à `badge.tsx` (`bg-primary/10 text-primary`, même formule `bg-X/10 text-X` que les variantes existantes) — seul ajout de token/variant de tout ce lot, même discipline que l'ajout du token `--violet` en CH-063 (n'introduire une nouvelle couleur que quand aucune existante ne convient).
- **`GuestsPage.tsx` — refonte visuelle complète** : liste de clients en cartes arrondies (`gcard`, bordure teintée + anneau `ring-primary/20` sur la carte sélectionnée, au lieu du simple `border-primary` précédent) au lieu de boutons à bordure plate ; fiche client réorganisée avec chips de catégorie CRM cliquables (boutons, pas un `<Select>`) reproduisant l'interaction du mockup, motif obligatoire tracé dans l'audit (comportement `updateGuestCategorie`/`GuestCategoryLog` strictement inchangé, uniquement l'UI de sélection change) ; historique des séjours et factures affichés en tables denses à en-tête (au lieu de listes à puces) avec badges de statut (`En cours`/`Terminé` pour un séjour, `Émise`/`Avoir émis` pour une facture — **valeurs réelles de l'énum `Invoice.statut`** vérifiées dans `billing/types.ts`, pas les libellés du mockup qui utilisait un statut « Soldée » n'existant pas côté backend, corrigé avant même la vérification navigateur). Formulaire de création réorganisé en grille 2 colonnes (inchangé fonctionnellement), avertissement de doublon informatif inchangé (`useDuplicateWarning`).
- **`CompaniesPage.tsx` — refonte visuelle complète** : même traitement de carte que Clients, badge `ICE renseigné`/`ICE manquant` (`success`/`warning`) reflétant directement la présence de `company.ice`, fiche entreprise avec bandeau « Entreprise partenaire depuis <mois/année> » (`company.createdAt`), contacts affichés en lignes bordées avec lien « Retirer » en rouge (au lieu d'un bouton outline), bloc « Compte courant » honnête inchangé (toujours 0,00 MAD statique — aucun rattachement séjours/factures réel côté backend, cf. commentaire déjà présent avant ce lot). **Ajustement post-vérification navigateur** : le premier jet du formulaire d'ajout de contact utilisait une grille CSS (`grid-cols-[repeat(auto-fit,...)]`) qui repoussait le bouton « + Contact » sur sa propre ligne à la largeur du panneau réel (900px) — corrigé en un `flex flex-wrap` à largeurs fixes (retour au pattern déjà existant avant ce lot), qui reproduit fidèlement la disposition en ligne du mockup.
- **Priorité** : Important (demande produit directe, troisième lot d'un mandat de refonte déjà validé) · **Criticité** : Faible (cosmétique/UI, aucun changement de RBAC, de règle métier serveur ou de chemin d'écriture — `updateGuestCategorie`/`createGuest`/`updateCompany`/`addCompanyContact`/`removeCompanyContact` tous inchangés).
- **Statut** : ✅ **Terminé** (ce lot). Reste du dossier batch 3 (RH, Stock, Reporting, Notifications, Audit, Paramètres, ScanIdentite, MotDePasse, ReservationForm) hors périmètre — à traiter en lots suivants, `ReservationForm` nécessitant en outre une validation produit explicite avant implémentation (le README du handoff le classe lui-même comme « suggestion de nouveauté produit, pas une simple reprise de l'écran existant »).
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers — aucun test dédié ajouté, changement purement visuel sans nouvelle logique testable, même convention que CH-062). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux, admin réel) : écran Clients avec les 2 clients seedés réels (liste, sélection, fiche avec chips de catégorie et tables historique/factures) ; dialogue de création de client ; écran Entreprises testé sur son chemin complet avec une entreprise réellement créée via l'UI (`Voyages Atlas SARL`, ICE 0012345678, 30 jours, 50 000 MAD) — badge `ICE renseigné` confirmé après création, contact ajouté et affiché avec son lien « Retirer », bloc compte courant affiché. Données de test nettoyées après vérification (`DELETE` direct sur `Company`/`CompanyContact` créés pour le test, aucune donnée de production affectée).
- **Documents liés** : `docs/design/design_handoff_batch3/README.md` (référence de design à tenir à jour pour les 11 écrans du dossier, dont seuls 2 sont couverts par ce lot), CH-062/CH-063 (lots précédents de la même refonte design, palette et composants réutilisés à l'identique).

### CH-063 — Handoff de design haute-fidélité : Réservations, Check-in, Housekeeping, Maintenance

- **Source** : demande explicite de l'utilisateur — second dossier de handoff déposé (`f91fa71c-MakarimPMS_project_review.zip`), suite directe de CH-062. Contient `design_handoff_exploitation_hotel/` (`Reservations.dc.html`, `Checkin.dc.html`, `Housekeeping.dc.html`, `Maintenance.dc.html`, `README.md`) — les 4 écrans de la catégorie sidebar « Exploitation hôtel ». Même instruction que CH-062 : « ajoute ce dossier au projet [...] et le tiens à jour », dossier ajouté tel quel à `docs/design/design_handoff_exploitation_hotel/`.
- **Nouveau token sémantique — `--violet`** : le README du handoff distingue visuellement `EN_NETTOYAGE` (ménage en cours) de `A_NETTOYER` (en attente) sur la grille Housekeeping — deux statuts manuels adjacents qui partageaient jusqu'ici le seul token `--warning`, indistinguables au premier coup d'œil sur la nouvelle table dense. Ajouté à `index.css` (`--violet: oklch(0.5 0.14 300)` clair / `oklch(0.62 0.14 300)` sombre, valeur exacte du handoff) et à `badgeVariants` (`badge.tsx`, variante `violet`) — seul ajout hors palette « Marine & Or » existante de tout ce chantier.
- **`ReservationsCalendarPage.tsx`** : les barres de réservation étaient toutes colorées `bg-primary` sans distinction, alors que `Reservation.canal` (`DIRECT`/`WALK_IN`/`BOOKING_COM`) existe déjà côté données. Couleur de barre désormais dérivée du canal (`CANAL_BAR_CLASS`, une seule source de vérité jamais recalculée ailleurs) + légende cliquable dans la toolbar (pastille + libellé par canal, cohérent avec la légende du mockup) + tinte de la colonne « aujourd'hui »/week-end sur l'en-tête de dates (absente jusqu'ici).
- **`CheckinPage.tsx`** : ajout d'une rangée de 4 chips de statistiques cliquables (arrivées/départs/séjours en cours/alerte fiches police manquantes — cette dernière désactivée visuellement si son compte est nul), chaque clic fait défiler jusqu'à la section correspondante (`scrollIntoView`) plutôt que de filtrer (les 3 listes restent visibles simultanément, cohérent avec l'écran existant). Avatars à initiales colorés par canal (`CANAL_AVATAR_CLASS`, résolu via `resolveCanal()` — un séjour walk-in, `stay.reservation === null`, retombe sur `WALK_IN` : seul `StayService.checkinWalkIn` crée un séjour sans réservation associée, donc ce repli n'est jamais une valeur inventée). Barre de recherche (`Input`, filtre nom/prénom/numéro de chambre sur les 3 listes simultanément) et pill « ⚠ Fiche police manquante » remplacée par `Badge variant="warning"` (au lieu d'un simple texte inline) — même icône `AlertTriangle` que le mockup.
- **`HousekeepingPage.tsx` — refonte la plus large des 4** : grille de cartes remplacée par une table dense groupée par étage (`Room.etage`, colonnes Chambre/Type/Statut/Action), avec 4 chips de stats cliquables en filtre (toggle) au-dessus. **Écart assumé et documenté, pas un oubli** : les colonnes « Équipier » et « Mis à jour » du mockup nécessitent un modèle `HousekeepingTask` (Équipier assigné, horodatage par utilisateur) qui **n'existe pas** dans le backend (`docs/modules/housekeeping.md` le décrit mais confirmé absent de `schema.prisma`/`backend/src` par grep) — `RoomStatusLog.userId`/`createdAt` existent bien en base mais aucune route n'expose une vue « dernier log par chambre » jointe à un nom d'utilisateur. Ces deux colonnes sont omises plutôt que peuplées de données inventées. Vues Kanban et mobile équipier du mockup également omises (même README qui les classe V2, gate explicite sur le même `HousekeepingTask` manquant) — aucune duplication du chemin d'écriture existant (`updateRoomStatus`/`HousekeepingService.updateStatus` inchangés, toujours l'unique point d'écriture de `Room.statut`, ADR-003). Le tri par étage retombe sur un groupe « Sans étage renseigné » pour les chambres sans `Room.etage` (champ nullable depuis CH-038 — confirmé réellement vide sur les 24 chambres seedées à ce jour, aucune régression de données).
- **`MaintenancePage.tsx`** : `PRIORITE_BADGE_VARIANT` corrigé pour distinguer réellement les 4 niveaux — l'ancien mapping (`BASSE→outline, MOYENNE→secondary, HAUTE→default, URGENTE→destructive`) ne différenciait visuellement que Urgente des trois autres, `Haute` et `Moyenne` étant visuellement équivalentes (gris/bleu shadcn générique). Réaligné sur l'échelle sémantique déjà en place ailleurs (Housekeeping, Réservations) : `BASSE→secondary, MOYENNE→info, HAUTE→warning, URGENTE→destructive`, conforme aux couleurs exactes du mockup. Liste plate remplacée par une table dense (icône/pastille de priorité, ticket, priorité, assigné, statut, action) avec 4 chips de stats cliquables en filtre — les chips comptent les tickets **ouverts** par priorité (pas tous les tickets, résolus inclus) : reflète la charge de travail réelle du jour plutôt qu'un historique cumulé, décision engineering assumée face à des données d'exemple du mockup elles-mêmes incohérentes sur ce point précis (vérifié en comptant à la main les 7 lignes du mockup — les totaux affichés ne correspondent ni à un décompte « ouverts seuls » ni à un décompte « tous confondus » de façon cohérente entre les 4 priorités). Les tickets résolus affichent désormais leur date de résolution (`DD/MM`) à la place du bouton « Résoudre », auparavant simplement masqué sans rien à la place.
- **Priorité** : Important (demande produit directe, second lot d'un mandat de refonte déjà validé) · **Criticité** : Faible (cosmétique/UI, aucun changement de RBAC, de règle métier serveur ou de chemin d'écriture — `updateRoomStatus`/`resolveTicket`/`checkinFromReservation`/`checkinWalkIn`/`checkoutStay` tous inchangés).
- **Statut** : ✅ **Terminé**. Le dossier de handoff reste la référence à tenir à jour pour ces 4 écrans (même convention que CH-062) ; V2 explicitement hors périmètre (Kanban/mobile équipier Housekeeping, gate `HousekeepingTask`).
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers — 2 assertions de `HousekeepingPage.test.tsx` mises à jour pour les nouveaux libellés de texte système,« Occupée au check-in »/« Libérée au check-out », sans changement du comportement testé) ; 2 specs e2e Playwright (`02-checkin-checkout-paiement.spec.ts`, `03-housekeeping-statut.spec.ts`) mises à jour pour cibler la nouvelle structure DOM (table dense au lieu de cartes à bordure `border-l-4`) sans changer l'assertion métier vérifiée — suite complète rejouée deux fois, verte les deux fois (la première tentative avait échoué sur une chambre 101 dans un état résiduel d'une session de test antérieure, pas une régression de ce chantier — confirmé en reseedant puis en rejouant). **Vérification navigateur réelle** (Playwright, backend + MySQL + Redis locaux, reseedés) : capture des 4 écrans avec données réelles (légende de canal sur Réservations, chips + avatars colorés + pill police sur Check-in avec 3 séjours réels sans fiche police, table groupée « Sans étage renseigné » + 23 chambres Libre & propre sur Housekeeping, chips de priorité + table dense sur Maintenance avec 4 tickets de test réels) ; transition manuelle réelle À nettoyer→En nettoyage confirmant le rendu du nouveau badge `violet` (chip de stat mise à jour de 0→1 « En nettoyage », dot violet) ; ouverture du sélecteur de statut confirmant la résolution correcte des libellés français (pas la valeur brute de l'énumération) ; filtre de chip Housekeeping confirmé fonctionnel (23→liste filtrée, chip activée visuellement) ; rejet réel d'une transition invalide (LIBRE_PROPRE→EN_NETTOYAGE, ADR-003) affiché en message d'erreur — état remis à Libre & propre après vérification.
- **Documents liés** : `docs/design/design_handoff_exploitation_hotel/README.md` (référence de design à tenir à jour pour ces 4 écrans), CH-062 (premier lot du même mandat, Connexion/Tableau de bord), `docs/modules/housekeeping.md` (écart `HousekeepingTask` déjà documenté comme absent avant ce chantier, confirmé toujours vrai).

### CH-062 — Handoff de design haute-fidélité : Connexion + Tableau de bord

- **Source** : demande explicite de l'utilisateur — dépôt d'un dossier `MakarimPMS_project_review.zip` (`/goal` du 2026-07-30) contenant un handoff de design haute-fidélité (`Login.dc.html`, `Dashboard.dc.html`, `README.md`) pour les écrans Connexion et Tableau de bord, dans l'identité « Marine & Or » déjà en place. Dossier ajouté tel quel à `docs/design/design_handoff_login_dashboard/` (référence versionnée, pas seulement un fichier joint éphémère) — but explicite « ajoute ce dossier au projet [...] et le tiens à jour ».
- **Nature des fichiers `.dc.html`** : prototypes HTML/CSS inline montrant l'intention visuelle, pas du code à copier — le README du handoff le précise explicitement. Recréés avec les composants `@/components/ui` et les tokens `index.css` existants (aucun nouveau token créé), jamais de style inline collé tel quel.
- **`LoginPage.tsx` — refonte complète** : carte blanche centrée (max-width 420px, coins arrondis, ombre `--shadow-elevated` via `shadow-[var(--shadow-elevated)]`, `--shadow-elevated`/`--shadow-card` n'étaient définis dans `index.css` que depuis CH-057 mais jamais réellement consommés par un composant jusqu'ici), liseré dégradé marine→or en tête de carte (seul élément sans équivalent shadcn/ui, implémenté en style inline ciblé plutôt qu'un nouveau composant pour un usage unique), badge logo "M" 34×34 (fallback quand `logoUrl` n'est pas configuré, inchangé du comportement existant), pastilles de rôles actifs recolorées (`bg-primary/10 text-primary` plutôt que `variant="outline"` générique, plus de contraste "produit"), champs et bouton principal redimensionnés (40px/42px) pour matcher la référence pixel-près. Comportement (soumission, état `submitting`, erreurs, `GET /auth/roles-actifs`) strictement inchangé.
- **`DashboardPage.tsx` — cartes KPI enrichies** : chaque carte gagne une icône `lucide-react` illustrant la métrique (jamais la couleur seule comme indicateur, cohérent avec l'accessibilité déjà travaillée aux lots précédents), un effet de survol (`translateY(-1px)` + ombre `--shadow-card` + bordure teintée `--primary`) et un focus clavier visible (`outline-gold`, absent auparavant sur ces cartes cliquables faites de `<div role="button">`, jamais de régression sur les vrais boutons shadcn qui gardent leur style de focus existant `ring`). La carte « Taux d'occupation » gagne une mini barre de progression (`--primary`/15% de fond, remplissage = `tauxOccupation`%), jamais recalculée côté client — reflet direct de la valeur déjà renvoyée par `GET /dashboard/resume`. Boutons d'action rapide redimensionnés à 44px de hauteur (cible tactile explicite du handoff). **Icônes choisies par sens métier plutôt que copiées trait pour trait du SVG du prototype** (ex. `Banknote` pour « Encaissé aujourd'hui » plutôt que l'icône d'horloge du mockup, qui ne correspond à aucune icône `lucide-react` sémantiquement pertinente pour un montant encaissé) — déviation assumée, cohérente avec la consigne du README (« recréer, pas coller »).
- **`AppSidebar.tsx`/`AppTopbar.tsx`` — aucune modification nécessaire** : comparaison visuelle directe contre `Dashboard.dc.html` (catégories repliables, badge de compte par catégorie, item actif avec liseré `inset 2px 0 0 var(--sidebar-primary)`, hauteur minimale 40px, bouton « Replier ») confirme une conformité déjà réalisée au Lot #1 (CH-059, 2026-07-30 même journée) — rien à refaire. Écart mineur assumé et documenté ici plutôt que « corrigé » : `AttendanceWidget.tsx` (pointage RH, ADR-007) reste un badge de statut **avec boutons d'action** (Démarrer/Pause/Terminer) plutôt que la pastille passive « En service depuis HH:MM » du mockup — le prototype simplifie une fonctionnalité interactive réelle en élément décoratif ; reproduire le mockup à l'identique aurait supprimé la seule façon de pointer depuis la barre de navigation. Le README du handoff invite explicitement à adapter, pas à copier aveuglément.
- **Priorité** : Important (demande produit directe, référence de design formelle jamais reçue jusqu'ici) · **Criticité** : Faible (cosmétique, aucun changement de logique métier, RBAC ou endpoint).
- **Statut** : ✅ **Terminé**. Le reste des écrans (au-delà de Connexion/Tableau de bord) n'est pas couvert par ce handoff — aucune extension de portée non demandée.
- **Éléments testés** : `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers) — aucun test dédié ajouté (changement purement visuel sans nouvelle logique testable, cohérent avec les lots précédents qui ne testent pas le CSS). **Vérification en navigateur réelle** (Playwright, backend + MySQL + Redis locaux) : capture de l'écran de connexion (carte, liseré, badge logo, pastilles de rôles réelles via `GET /auth/roles-actifs`, y compris RESTAURATEUR ajouté par F11) et du tableau de bord connecté (icônes KPI, barre de progression à 11.5%, widgets ménage/maintenance avec badges de priorité réels) — les deux captures confirment une fidélité visuelle élevée à la référence.
- **Documents liés** : `docs/design/design_handoff_login_dashboard/README.md` (référence de design à tenir à jour pour ces deux écrans — toute évolution future de Connexion/Tableau de bord doit rester cohérente avec ce handoff ou le faire évoluer explicitement), CH-057/CH-059 (palette et sidebar déjà en place, réutilisées ici).

### CH-061 (Lot #3) — Formulaires enrichis : formule d'hébergement + prix estimé en direct

- **Source** : demande explicite de l'utilisateur (`/goal` du 2026-07-30, suite des Lots #1/#2) — « formulaires enrichis par module ». Portée délibérément resserrée sur un gap concret plutôt qu'un ravalement cosmétique des ~14 écrans : « Formule d'hébergement » (`FormuleHebergement`) est supportée par le backend depuis Priorité 3 (`CreateReservationDto.formule`/`WalkinCheckinDto.formule`) mais n'était exposée nulle part côté saisie — seule `ParametersPage` (grille tarifaire des types de chambre) l'utilise en lecture. Aucun des deux formulaires de création (nouvelle réservation, check-in walk-in) n'affichait de prix avant confirmation.
- **Nouveau point d'entrée backend authentifié** : `GET /reservations/estimation-prix` (`reservations:read`), réutilise tel quel `ReservationsService.estimatePrixTotal()` — déjà une façade publique existante pour le Booking Engine (F4) — sans dupliquer la logique de calcul ni créer de réservation. Route statique déclarée avant `@Get(':id')` pour ne jamais y être capturée par erreur.
- **Formulaires enrichis** : `CreateReservationDialog.tsx` (calendrier des réservations) et `WalkinCheckinDialog.tsx` (check-in walk-in) gagnent tous deux un sélecteur de formule (Logement seul / Petit-déjeuner / Demi-pension / Pension complète) et un encart « Prix total estimé » recalculé en direct à chaque changement de formule ou de dates — jamais calculé côté client, uniquement affiché depuis la réponse serveur (mêmes règles de tarification saisonnière que la création réelle). Le walk-in utilise la date du jour comme arrivée (implicite, pas de champ à saisir) et seule la date de départ déjà présente dans le formulaire.
- **Bug réel détecté et corrigé pendant la vérification en navigateur** (pas seulement en test unitaire) : le sélecteur de formule affichait la valeur brute de l'énumération (`BED_AND_BREAKFAST`) au lieu du libellé humain (« Petit-déjeuner ») une fois une option choisie — `Select.Value` de `@base-ui/react` n'auto-résout pas un libellé à partir des `SelectItem`, il faut lui passer explicitement une fonction de rendu (`<SelectValue>{(value) => label}</SelectValue>`, documenté dans l'API du composant). Corrigé dans les deux formulaires. **Dette identique repérée mais non corrigée ailleurs** (hors périmètre de ce lot, aucun changement de ce fichier) : le sélecteur de chambre de `StockPage.tsx` (sortie manuelle) a la même limite latente.
- **Priorité** : Important (demande produit directe, capacité backend existante enfin exploitée) · **Criticité** : Faible (purement consultatif avant confirmation, aucune règle de calcul dupliquée côté client).
- **Statut** : ✅ **Terminé** (ce lot). Portée restante de « formulaires enrichis » (au-delà de ce gap précis) à cadrer séparément si l'utilisateur le souhaite — trop large pour un seul lot au sens de la pacing « par lots successifs » déjà convenue.
- **Éléments testés** : 3 nouveaux scénarios e2e backend (`reservations-pricing.e2e-spec.ts`) — estimation correcte à cheval sur deux saisons sans créer de réservation (vérifié : aucune ligne `Reservation`/`RoomNight` créée), 404 sur type de chambre inexistant, 400 sur plage de dates invalide ; suite complète non régressée (24 suites, 194/194). `npm run build`/`lint`/`test` backend et frontend clean des deux côtés (74/74 frontend, 39/39 backend). **Vérification en navigateur réelle** (Playwright, backend + MySQL + Redis locaux) : glisser-déposer réel sur le calendrier des réservations → dialogue ouvert avec prix estimé initial (2250.00 MAD, Petit-déjeuner) → changement de formule vers Pension complète → prix recalculé en direct (2760.00 MAD) confirmé par capture d'écran ; check-in walk-in réel (chambre 102, +3 jours) → même comportement (2250.00 → 2100.00 MAD en repassant à Logement seul) — c'est cette vérification qui a révélé et permis de corriger le bug d'affichage du libellé de formule ci-dessus.
- **Documents liés** : `docs/modules/reservations.md` (facade `estimatePrixTotal` déjà documentée pour F4, réutilisation notée ici), CH-057/CH-059/CH-060 (lots précédents de la même refonte design).

### CH-060 (Lot #2) — Couleurs sémantiques cohérentes + écran frontend Restaurant (F11)

- **Source** : demande explicite de l'utilisateur (`/goal` du 2026-07-30, suite du Lot #1/CH-059) — « enchaîner sur le lot suivant (formulaires enrichis par module, couleurs sémantiques, écran Restaurant F11) ».
- **Couleurs sémantiques — constat avant code** : les tokens `--success`/`--warning`/`--info` et le composant `Badge` (variantes `success`/`warning`/`info`) existaient déjà (CH-057) et étaient déjà utilisés par la majorité des écrans. Un balayage exhaustif (`grep` sur les classes Tailwind `bg-*-NNN`/`text-*-NNN` codées en dur) a trouvé **8 occurrences résiduelles** de amber/emerald non migrées vers les tokens : `DocumentOcrPage.tsx`, `GuestPicker.tsx`, `GuestsPage.tsx` (avertissements doublons), `SelfCheckinPanel.tsx`, `StayDetailsDialog.tsx`, `CheckinPage.tsx` (×2, alertes fiche police manquante), `PoliceRecordForm.tsx`, `ParametersPage.tsx` (confirmation d'enregistrement), `diff-viewer.tsx` (valeur modifiée) — toutes remplacées par `text-warning`/`bg-warning`/`text-success`, aucun changement de sens visuel, seulement de source de vérité (un futur changement de palette ne les manquera plus).
- **Écran frontend Restaurant (F11)** — le seul module backend-prêt sans aucune UI dédiée (confirmé par audit avant ce lot, `grep -rln "restaurant" frontend/src/`, un seul résultat hors périmètre). Nouveau `frontend/src/features/restaurant/` (`types.ts`/`api.ts`/`pages/RestaurantPage.tsx`), consommant les 4 endpoints déjà livrés en backend (`GET /restaurant/stays-in-house`, `POST /restaurant/charges`, `PATCH /restaurant/charges/:folioLineId`, `GET /restaurant/charges/rapport`) — aucun nouveau code backend. Deux vues (même convention `Tabs` que `StockPage.tsx`) : « Séjours en cours » (ajout de note par séjour) et « Rapport du jour » (double vérification a posteriori, RD-F11-03, avec correction inline — motif ≥ 10 caractères, RD-F11-02). Nouvel onglet `restaurant` dans `App.tsx`/`nav-items.ts`, catégorie « Exploitation hôtel », gaté sur `restaurant:write` (seule permission existante sur ce module, même exception à la convention `:read` que `document-ocr`, CLAUDE.md).
- **Badge « Restaurant » sur le folio (clôture #156)** — **écart corrigé en cours de route** : la première version ajoutait un badge « Restaurant » à côté du libellé de type existant, qui affichait déjà le texte « Restaurant » (`TYPE_LIGNE_LABEL`) — doublon visuel repéré par le test automatisé écrit dans la foulée (`screen.getByText` ambigu). Corrigé en remplaçant le libellé texte muted par un badge coloré (`variant="info"` actif, `variant="secondary"` + barré si annulée) **uniquement** pour `TypeLigneFolio.RESTAURANT`, les autres types (`Hébergement`/`Extra`/`Taxe de séjour`/`Paiement`) gardant leur libellé texte inchangé — une seule source d'affichage par ligne, jamais deux textes redondants.
- **Priorité** : Important (demande produit directe, complète le périmètre F11 côté frontend) · **Criticité** : Faible (cosmétique + nouvel écran consultatif/saisie, aucune règle métier nouvelle côté serveur).
- **Statut** : ✅ **Terminé** (ce lot). Reste hors périmètre (F11 encore 🚧, voir CH-056) : listener notification réception, suite `restaurant.e2e-spec.ts`, `docs/modules/restaurant.md` — travail backend/QA, pas de la refonte design.
- **Éléments testés** : 3 nouveaux tests (`RestaurantPage.test.tsx` — état vide explicite, liste des séjours en cours, ajout de note avec les bons paramètres transmis à l'API) + 1 nouveau test (`BillingTabContent.test.tsx` — badge coloré distinct entre ligne active/annulée, absence de doublon de texte) ; suite existante non régressée. `npm run build`/`lint`/`test` frontend clean (74/74, 22 fichiers). **Vérification en navigateur réelle** (Playwright, backend + MySQL + Redis locaux, reseedé pour disposer du compte `restaurant@makarim.test` ajouté par CH-056) : walk-in réel créé par un compte Administrateur → connexion `RESTAURATEUR` confirmée strictement scopée à la seule catégorie/onglet Restaurant dans la sidebar (RBAC CH-011 vérifié en conditions réelles, pas seulement en unitaire) → séjour visible dans « Séjours en cours » → note ajoutée (toast de confirmation réel) → présente dans le « Rapport du jour » groupée par chambre → reconnexion Administrateur → badge « Restaurant » coloré confirmé visible sur le folio réel, distinct des autres lignes (`Hébergement`/`Extra` restés en texte muted). Nettoyage post-vérification : scripts Playwright temporaires supprimés avant commit, serveurs de dev arrêtés.
- **Documents liés** : `docs/modules/restaurant.md` (à venir, CH-056), `docs/governance/REGISTRE_DECISIONS.md` (RD-025, RD-026).

### CH-059 (Lot #1) — Refonte de la navigation : sidebar par catégories + actions rapides tableau de bord

- **Source** : demande explicite de l'utilisateur (`/goal` du 2026-07-30, après la première connexion réussie sur `hotelmakarim.cloud`) — refonte visuelle globale du frontend, exécutée par lots successifs. Priorité confirmée par l'utilisateur (`AskUserQuestion`) pour ce premier lot : « Navigation + logo + dashboard ». Palette confirmée : conserver Marine & Or (CH-057), enrichir uniquement la structure.
- **Constat déclencheur** : la sidebar (`AppSidebar.tsx`, `NAV_ITEMS`) affichait les 14 onglets en liste plate sans regroupement — reproche explicite de l'utilisateur : « le module audit est à la fin » (Audit, module d'administration, perdu en toute fin d'une liste sans hiérarchie visuelle avec les modules opérationnels quotidiens).
- **Inspiration** : structure `NAV_CATEGORIES`/`NAV_ITEMS` de `makarimpms_v2` (`AppSidebar.tsx`), dont l'utilisateur avait explicitement jugé le design « plus joli » — reprise du **mécanisme** (catégories repliables, badge de comptage, catégorie active toujours dépliée), adapté aux 14 onglets réels de `main` (v2 a deux onglets supplémentaires, `billing`/`police`, qui n'ont pas d'équivalent séparé ici — restés embarqués dans check-in/folio, CLAUDE.md — non repris).
- **Livrable** :
  - `nav-items.ts` — nouveau type `NavCategoryKey` + `NAV_CATEGORIES` (6 catégories : Pilotage, Exploitation hôtel, Clients & partenaires, Ressources & stocks, Statistiques & rapports, Administration) ; chaque `NavItem` gagne un champ `category`. Audit rejoint Administration aux côtés de Paramètres/Notifications (au lieu de fin de liste plate).
  - `AppSidebar.tsx` — regroupement par catégorie repliable indépendamment (chevron, compteur d'items visibles après filtrage RBAC), la catégorie de l'onglet actif reste toujours dépliée même si l'utilisateur l'a repliée manuellement ; une catégorie entièrement vidée par le filtrage RBAC (CH-011) n'est pas rendue. Logo/RBAC/tiroir mobile (CH-034) inchangés (déjà livrés, CH-057/CH-034).
  - `DashboardPage.tsx` — nouvelle rangée « Actions rapides » (4 boutons : Nouvelle réservation, Check-in walk-in, Chambres à nettoyer, Signaler une panne), chacun navigue directement vers l'onglet concerné (même mécanisme que le clic sur une carte KPI existante). **Portée volontairement limitée à la navigation** — ouvrir directement le formulaire de création exigerait de faire transiter un état d'ouverture jusqu'à `ReservationsCalendarPage`/`CheckinPage` (aucun des deux n'accepte de props aujourd'hui), hors périmètre de ce lot.
- **Logo (partie du mandat de ce lot)** : vérifié — aucun bug de code. `GET /branding` (`@Public()`, CH-057/RD-027) et sa consommation dans `App.tsx`/`AppSidebar.tsx`/`LoginPage.tsx` fonctionnent tels quels ; le badge « M » de repli s'affiche simplement parce qu'aucune image n'a encore été enregistrée dans `HotelConfig.logoUrl` sur la base de production (champ vide par défaut, comportement attendu — voir CH-057 : le logo réel fourni par l'utilisateur n'avait pas pu être récupéré après une compaction de contexte lors d'une session précédente). **Reste bloqué sur une action utilisateur** : uploader le logo réel via Paramètres → Identité de l'hôtel (fonctionnalité déjà livrée et vérifiée, CH-057).
- **Priorité** : Important (demande produit directe) · **Criticité** : Faible (purement cosmétique/navigation, aucune donnée métier ni RBAC serveur affecté).
- **Statut** : ✅ **Terminé** (ce lot — lots suivants du chantier design restant à cadrer : formulaires enrichis par module, tokens sémantiques success/warning/info, écran Restaurant F11).
- **Éléments testés** : suite Vitest existante (`AppSidebar.test.tsx`, 10 scénarios RBAC/tiroir mobile déjà couverts, aucune régression) — `npm run build`/`lint`/`test` frontend clean (70/70, 21 fichiers). **Vérification en navigateur réelle** (Playwright, backend + MySQL + Redis démarrés localement, données déjà seedées d'une session précédente) : connexion réelle (`admin@makarim.test`), sidebar affichée avec les 6 catégories et leurs compteurs, Audit confirmé regroupé sous Administration ; repli d'une catégorie (« Exploitation hôtel ») confirmé fonctionnel (chevron, items masqués, autres catégories inchangées) ; clic sur le bouton d'action rapide « Nouvelle réservation » confirmé naviguer vers l'onglet Réservations (`aria-current="page"` vérifié par sélecteur). Nettoyage post-vérification (serveurs de dev arrêtés).
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-026 pour la palette, réutilisée telle quelle), CH-057 (logo/palette), CH-034 (tiroir mobile, inchangé).

### CH-058 — Préparation de la mise en production réelle (domaine hotelmakarim.cloud)

- **Source** : demande explicite de l'utilisateur — déploiement réel sur son VPS et le domaine `hotelmakarim.cloud`, avec mandat de « finaliser ce qui empêche le déploiement ».
- **Bug bloquant réel détecté** (pas seulement de la documentation) : `VITE_API_URL` est une variable **Vite résolue au build** (`import.meta.env`, jamais lisible au runtime du conteneur nginx qui ne sert que du HTML/JS statique) — `frontend/Dockerfile` ne déclarait aucun `ARG`/`ENV` pour la relayer, et `.github/workflows/deploy.yml` construisait l'image sans jamais la passer. Toute image de production aurait donc silencieusement embarqué le fallback de développement (`http://localhost:3000/api`, voir `lib/api-client.ts`) — le frontend déployé n'aurait jamais pu atteindre l'API réelle. Corrigé : `ARG VITE_API_URL` + `ENV VITE_API_URL=$VITE_API_URL` avant `RUN npm run build` dans le Dockerfile, `build-args: VITE_API_URL=https://api.hotelmakarim.cloud/api` dans l'étape de build de l'image frontend de `deploy.yml`. Vérifié réellement (pas seulement lu) : build Vite local avec la variable positionnée, `grep` du bundle généré confirme l'URL de production effectivement embarquée dans `api-client-*.js` (Docker non disponible dans cet environnement d'exécution pour un test de build d'image complet).
- **Domaine réel renseigné** partout où `docs/operations/CH-026E_DEPLOIEMENT_DOMAINE.md` §1 laissait un gabarit `<domaine>`/`mondomaine.ma` à remplacer : `infra/nginx/pms-hotel-makarim.conf` (schéma `pms.hotelmakarim.cloud`/`api.hotelmakarim.cloud`, certificat SAN Certbot), `docs/execution/GO_LIVE_CHECKLIST.md`, `docs/operations/OPERATIONS_RUNBOOK.md` (schéma Mermaid + section 1.1 réseau), `infra/README.md`.
- **Écart de documentation corrigé en passant** : `OPERATIONS_RUNBOOK.md` §1.1 présentait le durcissement des ports (`127.0.0.1` uniquement) comme une « action requise avant le premier déploiement réel » — en réalité déjà livré par CH-046 (`docker-compose.prod.yml`, tags `!override`/`!reset null`) et déjà utilisé par `deploy.yml`. Reformulé pour ne pas faire croire à un travail restant qui n'existe plus.
- **Secrets de production générés** (`openssl rand`, jamais des valeurs par défaut) : `JWT_ACCESS_SECRET`/`JWT_REFRESH_SECRET` (48 octets), `ENCRYPTION_KEY` (32 octets), `CHANNEL_WEBHOOK_SECRET` (32 octets), identifiants MySQL — remis directement à l'utilisateur (fichier `.env` complet prêt à copier sur le VPS, jamais commit dans le dépôt, conforme à `OPERATIONS_RUNBOOK.md` §1.2).
- **Second bug bloquant réel détecté, plus grave que les précédents** : `.github/workflows/deploy.yml` ne se déclenche que sur `workflow_run` avec `conclusion == 'success'` du workflow `CI` sur `main` — or l'historique des 30 derniers runs `CI` sur `main` (vérifié via l'API GitHub, pas supposé) montre une conclusion `failure` systématique, toujours sur le même job `e2e-frontend` (`backend`/`frontend` passent systématiquement). Autrement dit, même une fois le VPS et les secrets GitHub en place, le pipeline de déploiement automatique ne se serait **jamais** déclenché. Cause racine identifiée : `frontend/playwright.config.ts` lançait le serveur de dev Vite (`npm run dev -- --port 5173 --strictPort`) sans `--host`, donc lié à la résolution système de `localhost` — sur les runners GitHub Actions Ubuntu, `localhost` peut préférer `::1` (IPv6), alors que le healthcheck du `webServer` et `baseURL` sondent explicitement `http://127.0.0.1:5173` (choix délibéré du projet, cohérent avec `FRONTEND_URL`/CORS dans `ci.yml`) — décalage silencieux, jamais d'erreur explicite, juste un timeout après 60000ms identique sur chaque run. Non reproductible dans cet environnement d'exécution (résolution `localhost`→`127.0.0.1` déjà cohérente ici), mais la correction (`--host 127.0.0.1` explicite dans la commande du `webServer` frontend) est sans risque et directement alignée sur la convention 127.0.0.1 déjà utilisée partout ailleurs dans le projet (backend `app.listen()` sans host, donc déjà multi-interface, non affecté).
- **Hors périmètre de ce qu'un agent sans accès VPS/DNS/GitHub-admin peut finaliser seul** (RD-028) : provisionnement du VPS, enregistrements DNS A pour les deux sous-domaines, secrets GitHub Actions (`VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY`/`VPS_SSH_PORT`, environnement `production`), génération/pose du certificat Certbot, premier `docker compose up` réel, plan de sauvegarde en crontab, exercice de restauration réel (`GO_LIVE_CHECKLIST.md` points 1/5/9/11/12/13 restent `[ ]`, action humaine ou accès à donner explicitement). **Confirmé également hors de portée pendant ce chantier** : l'environnement d'exécution de l'agent n'a pas d'accès réseau sortant vers des hôtes arbitraires (politique d'égress, confirmée par un 403 systématique du proxy en tentant de joindre l'IP du VPS de l'utilisateur) — un accès SSH direct au VPS n'aurait donc de toute façon pas pu être exploité même fourni ; le chemin retenu est un script de provisionnement complet (`vps-bootstrap.sh`, remis à l'utilisateur) qu'il exécute lui-même.
- **Troisième bug CI découvert et corrigé en creusant le second** : une fois le webServer démarré, la suite échouait différemment (`login()` : `#nav-dashboard` introuvable). Cause : sans `VITE_API_URL` explicite, le frontend retombe sur `http://localhost:3000/api` (`lib/api-client.ts`) alors que la page est servie sur `http://127.0.0.1:5173` — "localhost" et "127.0.0.1" ne partagent pas de domaine enregistrable, donc ne sont pas "same-site" pour un navigateur : le cookie d'authentification host-only (CH-026e) posé par `POST /auth/login` est silencieusement refusé sur l'appel suivant (`GET /auth/me`, cross-site de son point de vue), les permissions retombent à `[]`, aucun onglet de navigation ne s'affiche jamais. Corrigé : `VITE_API_URL=http://127.0.0.1:3000/api` explicite dans le `webServer` frontend de `playwright.config.ts`. **Confirmé réellement vert** (pas seulement supposé) : les 3 jobs `backend`/`frontend`/`e2e-frontend` du run CI sur le commit du correctif sont passés avec succès (vérifié via l'API GitHub Actions, historique consultable).
- **Quatrième bug découvert au tout premier déclenchement réel de `deploy.yml`** (jamais exécuté avant la fusion de ce chantier sur `main`, faute de CI verte) : `docker/build-push-action` échouait à l'étape même de build de l'image frontend, avant d'atteindre le SSH vers le VPS — bug resté totalement invisible tant que ce pipeline n'avait jamais tourné (voir bug n°2 ci-dessus). **Premier diagnostic erroné, corrigé par la suite** : `frontend/Dockerfile` épinglait `node:20-alpine` alors que `ci.yml` utilise `node-version: 22` — hypothèse plausible (le job `frontend` de la CI, Node 22, installe sans erreur), corrigée en alignant le Dockerfile sur `node:22-alpine` (PR `claude/fix-frontend-docker-node-version`, fusionnée). **Le nouveau run de `deploy.yml` a échoué exactement pareil malgré ce correctif** — preuve que le diagnostic Node 20/22 était une fausse piste, pas la cause réelle (discipline du projet : ne jamais s'arrêter à une correction qui « semble » logique sans revérifier en conditions réelles). **Cause racine effective** : `frontend/.npmrc` (`legacy-peer-deps=true`, ajouté par CH-029 précisément pour tolérer ce même conflit `eslint@^10.7.0`/`eslint-plugin-jsx-a11y@6.10.2`) n'était **jamais copié dans l'image Docker** — `COPY package.json package-lock.json ./` (avant `RUN npm ci`, optimisation classique de cache de layers) omettait `.npmrc`, contrairement au job `frontend` de la CI qui checkoute le dépôt complet avant tout `npm ci`. Corrigé : `.npmrc` ajouté à la liste des fichiers copiés avant `npm ci`. **Vérifié réellement** (sabotage/restore, pas seulement supposé) : reproduction exacte hors Docker (répertoire temporaire avec uniquement `package.json`/`package-lock.json`, sans `.npmrc`) — `npm ci` échoue avec le même message `ERESOLVE`/`eslint-plugin-jsx-a11y` que dans les logs GitHub Actions ; avec `.npmrc` ajouté, `npm ci` réussit (618 packages installés).
- **Statut** : 🚧 **En cours** — tous les blocages applicatifs/infra-as-code/CI/Docker identifiés et corrigés (cause racine du bug Docker confirmée par reproduction locale, pas seulement par lecture des logs) ; premier déploiement réel en attente d'exécution du script de provisionnement par l'utilisateur (accès VPS hors de portée du présent environnement d'exécution) et des secrets GitHub Actions.
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-028), `docs/operations/CH-026E_DEPLOIEMENT_DOMAINE.md`, `docs/execution/GO_LIVE_CHECKLIST.md`.

### CH-057 — Refonte visuelle « Marine & Or » + logo configurable de l'hôtel

- **Source** : demande explicite de l'utilisateur, avec trois pièces jointes fournies simultanément — l'image du logo réel « Hôtel Makarim Tétouan » (navy `#283379` + étoiles or), `bookingDESIGN1.md` (tokens de design extraits de Booking.com) et `tasteSKILL1.md` (skill de design pour landing pages, jugé non applicable à ce dashboard admin — voir RD-026). Double besoin : (1) appliquer une nouvelle identité visuelle cohérente avec la marque réelle de l'hôtel, (2) rendre le logo configurable depuis Paramètres et le propager à toutes les surfaces qui affichent une identité visuelle (application, factures, tableau de bord, page de connexion).
- **Décisions architecturales tranchées avant code** : voir `docs/governance/REGISTRE_DECISIONS.md` RD-026 (arbitrage entre les 3 sources de palette — structure Booking.com, couleurs du logo réel, ni Ardoise & Laiton ni le bleu littéral de Booking.com) et RD-027 (endpoint public dédié `GET /parameters/branding`, plutôt qu'un relâchement RBAC sur `GET /hotel-config`).
- **Palette** : conversion colorimétrique exacte sRGB→OKLCH (script Python) du navy du logo — `oklch(0.355 0.119 272.1)` en thème clair, `oklch(0.445 0.136 273)` en thème sombre — plus un nouveau token `--gold`/`--gold-foreground` (`oklch(0.728 0.138 89.7)`) pour les accents secondaires, absent de la palette Ardoise & Laiton précédente. `--radius` réduit à `0.5rem`, nouvelles variables de durée/easing (`--duration-fast/base/slow`, `--ease-out-brand`) et ombres teintées vers la teinte de marque (`--shadow-card`/`--shadow-elevated`) — repris de la structure Booking.com.
- **Logo configurable** : `HotelConfig.logoUrl` (déjà présent en base et dans le DTO depuis un chantier antérieur, mais jamais exposé dans l'UI — champ mort découvert en préparant ce chantier) élargi en `@db.LongText` (migration Prisma), validé par regex data-URI + taille max (même convention que `MaintenanceTicket.photoUrl`, CH-055). Champ d'upload avec miniature ajouté à `HotelIdentitySection` (`ParametersPage.tsx`, réservé `parameters:write`).
- **Propagation** (4 surfaces, un seul point d'accès par contexte) : nouvel endpoint `@Public() GET /parameters/branding` (`{raisonSociale, logoUrl}` uniquement, jamais les champs fiscaux) consommé une fois dans `App.tsx` et propagé par props à `LoginPage` (logo avant authentification) et `AppSidebar` (logo à la place du badge « M » une fois connecté) ; favicon dynamique (`<link rel="icon">` réécrit par un `useEffect`) ; `buildInvoicePdf()` (backend, décodage direct du data URI en `Buffer`, dégradation silencieuse vers l'en-tête texte seul si le contenu base64 n'est pas une image valide) et `InvoicePrintModal.tsx` (aperçu imprimable, réutilise `HotelConfig.logoUrl` déjà chargé par la modale).
- **Priorité** : Important (image de marque + demande produit directe) · **Criticité** : Faible (aucune donnée sensible, aucun chemin métier bloquant).
- **Statut** : ✅ **Terminé** (session courante).
- **Éléments testés** : 2 nouveaux tests unitaires backend (`invoice.pdf.spec.ts` — PDF plus volumineux avec logo fourni, dégradation silencieuse sur contenu base64 corrompu malgré préfixe data-URI valide ; **preuve de rigueur sabotage/restore réalisée** : retrait temporaire du bloc `try/catch` autour de `doc.image()` → le test échoue bien avec l'exception réelle pdfkit « Unsupported image format » au lieu d'un PDF valide, confirmant qu'il est discriminant et n'est pas déjà bloqué par le filtre de préfixe testé séparément ; restauré, revérifié vert). `npm run build`/`lint`/`test` backend (39/39) et frontend (70/70, 21 fichiers) clean des deux côtés. **Vérification en navigateur réel** (Playwright, cycle complet) : upload d'une image de test via le champ Paramètres → miniature affichée + message de succès → rechargement de la page → logo effectivement affiché dans la barre latérale (remplace le badge « M ») → déconnexion → logo affiché sur la page de connexion avant authentification → favicon dynamique confirmé (`<link rel="icon">` en data URI) → `GET /api/branding` confirmé renvoyer le logo réellement enregistré. **Limite assumée** : le logo réel de l'hôtel fourni par l'utilisateur (image collée dans la conversation) n'a pas pu être récupéré comme fichier après une compaction de contexte — la vérification a été faite avec une image de test générique, puis le champ a été explicitement remis à `null` en fin de session pour ne pas laisser une image de test en configuration ; l'utilisateur doit uploader le logo réel via Paramètres (fonctionnalité livrée et vérifiée).
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-026, RD-027).

### CH-056 (F11) — Module Restaurant : interception du flux « note restaurant → facture hôtel »

- **Source** : demande explicite de l'utilisateur — l'hôtel exploite un POS de caisse restaurant indépendant du PMS ; quand un client de l'hôtel veut ajouter sa note restaurant à sa facture, le restaurant envoie aujourd'hui un ticket papier à la réception. Ce chantier intercepte ce geste : un compte restaurant saisit directement la note dans le PMS, sans ressaisie côté réception.
- **Décisions architecturales tranchées avant code** : voir `docs/governance/REGISTRE_DECISIONS.md` RD-025 (RD-F11-01 : écriture directe en `FolioLine`, aucune validation réception intermédiaire ; RD-F11-02 : correction = annulation soft + recréation, jamais de mutation directe ; RD-F11-03 : note non acquittée bloque le check-out, même mécanique que CH-005/RD-008).
- **Modules concernés** : nouveau module `restaurant` (feuille côté écriture propre — aucune table Prisma dédiée, seulement une nouvelle valeur d'enum `TypeLigneFolio.RESTAURANT`), dépend en façade de `stay` (lecture, séjours `EN_COURS` uniquement), `billing` (écriture, `addFolioLine`/`cancelFolioLine`), `audit`, `notifications` (listener consommateur, pas d'import réciproque).
- **Écart technique détecté avant d'écrire le code métier (préparation du chantier, pas une découverte a posteriori)** : `BillingService.addFolioLine()` n'acceptait pas de `tx` externe (contrairement à `creditFolioLine`/`assertFolioWritable`) — incompatible avec ADR-005 (audit dans la même transaction que l'écriture métier). `cancelFolioLine()` existait déjà et implémentait exactement le pattern d'annulation soft demandé par RD-F11-02, mais restreint à `type === EXTRA`. Les deux méthodes sont étendues (paramètre `tx?` optionnel, garde de type élargie à `EXTRA | RESTAURANT`) plutôt que dupliquées — cohérent avec la règle CLAUDE.md « un seul chemin d'écriture par champ sensible ».
- **Endpoints** (`restaurant:write` sauf mention contraire) : `GET /restaurant/stays-in-house` (projection stricte `{stayId, roomNumber, guestName, checkoutDate}`, jamais de solde/folio/donnée financière) ; `POST /restaurant/charges` (crée `FolioLine RESTAURANT` + `AuditLog` + `emitAsync('restaurant.charge.ajoutee', …)`) ; `PATCH /restaurant/charges/:folioLineId` (annulation + recréation dans une même transaction, bloqué 409 si `Stay.statut === TERMINE`) ; `GET /restaurant/charges/rapport?date=` (regroupement par chambre, pour impression/email/WhatsApp — double vérification a posteriori, jamais bloquante).
- **Nouveau rôle** `RESTAURATEUR` (`prisma/seed.ts`, compte `restaurant@makarim.test` / `Password123!`), nouvelle permission dédiée `restaurant:write`/`restaurant:read` (pas de réutilisation d'une permission existante, contrairement à `police`/`companies`).
- **Priorité** : Important (demande produit directe) · **Criticité** : Modérée.
- **Statut** : 🚧 **En cours** (session courante).
- **Reste à faire** : listener notification réception, suite `restaurant.e2e-spec.ts` (8 scénarios), extension `docs/modules/stay.md` §BR-SEJ-004, nouveau `docs/modules/restaurant.md`. Migration Prisma, extension `StayService.checkout()`, module backend complet et écran frontend (page + badge folio) sont désormais livrés — voir CH-060.
- **Documents liés** : `docs/governance/REGISTRE_DECISIONS.md` (RD-025), `docs/modules/stay.md` (extension à venir), `docs/DEPENDENCY_GRAPH.md` (à mettre à jour avec le nouveau module).

### CH-042 — Impression de facture (aperçu imprimable + PDF)

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase B, inspiré de `MakarimPMS_v2` (`InvoicePrintModal.tsx`) — non bloqué par la dérive de migration (aucun changement de schéma, `FolioLine`/`Invoice`/`HotelConfig` existent déjà tels quels).
- **Écarts corrigés par rapport à v2** (code source de v2 vérifié avant portage, jamais supposé fiable) : (1) v2 recalculait la TVA côté client avec des taux **codés en dur** (×1.1 hébergement, ×1.2 extra/restauration) — `main` n'affiche que le montant HT par ligne et le `Invoice.montantTotal` déjà figé côté serveur au moment de l'émission (ADR-004, immuable), aucun recalcul client ; (2) v2 affichait des coordonnées légales **entièrement fabriquées** (téléphone, email, et surtout un ICE/RC/Patente/CNSS factices dans le pied de page, incohérents avec les valeurs `hotelConfig` déjà utilisées dans l'en-tête du même fichier) — `main` n'affiche que les champs réellement présents dans `HotelConfig` (`raisonSociale`/`adresse`/`ice`/`rc`/`identifiantFiscal` ; ni téléphone ni patente/CNSS, absents du schéma, jamais affichés).
- **Livrable** : `features/billing/components/InvoicePrintModal.tsx` (nouveau), bouton « Imprimer » par facture dans `BillingTabContent.tsx` (nouvelles props optionnelles `guest`/`room`, transmises depuis `StayDetailsDialog.tsx` qui les a déjà via `stay` — pas de nouvel appel réseau). Impression via iframe masquée (copie des feuilles de style Tailwind, déclenchement de `window.print()`), pattern conservé de v2 (légitime, pas de défaut identifié sur cette partie).
- **Statut** : ✅ **Terminé** (session courante).
- **Éléments testés** : 3 tests unitaires (`InvoicePrintModal.test.tsx` — absence de champs légaux fabriqués quand `HotelConfig` ne les porte pas, affichage fidèle quand ils existent, total unique non recalculé, lignes `PAIEMENT`/annulées exclues du tableau imprimable) ; `npm run build`/`lint`/`test` clean (54/54). **Vérification en navigateur réel** (Playwright, données seedées réelles) : check-in walk-in → génération de facture → impression → contenu affiché fidèle aux données réelles de `HotelConfig`/`Guest`/`Room`/`FolioLine` du backend de dev, aucune valeur fabriquée.

### CH-043 — Widgets tableau de bord (chambres à nettoyer, tickets de maintenance ouverts)

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase B, inspiré de `MakarimPMS_v2` (`AlertsPanel.tsx`, `BookingTrendsChart.tsx`, `HousekeepingTasksWidget.tsx`).
- **Écart majeur découvert en lisant le code source de v2 avant tout portage** : les 3 widgets de v2 sont **entièrement des données fictives codées en dur** — `AlertsPanel` (`defaultAlerts`) invente une arrivée VIP (« M. Bennani, Président Groupe Maghreb »), des arrivées non attribuées, des fiches de police manquantes et un ticket de maintenance chambre 203, avec des horodatages statiques (« Il y a 10 min ») jamais recalculés ; `BookingTrendsChart` (`mockTrendData`) invente 12 mois de chiffre d'affaires/occupation, année « 2026 » codée en dur dans l'infobulle ; `HousekeepingTasksWidget` (`initialTasks`) invente 4 tâches sur des chambres précises, réutilise le même client VIP fictif, et « marquer terminé » ne modifie aucune donnée réelle (état local React uniquement, jamais d'appel à `PATCH /rooms/:id/statut`). **Aucun des trois n'a été porté tel quel** — afficher de fausses informations opérationnelles à du personnel réel (arrivée VIP inexistante, tendance de revenus inventée) serait une régression grave, pas une fonctionnalité, et contredit directement le constat déjà acquis sur `main` (Phase 11 : « zéro donnée simulée »).
- **Portée retenue** : seuls 2 widgets à données 100 % réelles, construits sur des endpoints déjà existants (aucun changement de schéma, non bloqué par la dérive de migration de CH-038) : chambres à nettoyer (`GET /rooms`, filtré `A_NETTOYER`/`EN_NETTOYAGE`, déjà utilisé par `HousekeepingPage`) et tickets de maintenance ouverts (`GET /maintenance-tickets?ouvert=true`, déjà utilisé par `MaintenancePage`). Un graphique de tendances réel nécessiterait d'abord d'étudier la forme exacte de `GET /reporting/financial-summary` — hors périmètre de cette itération, à cadrer séparément si besoin.
- **Livrable** : `features/dashboard/components/RoomsToCleanWidget.tsx` et `OpenMaintenanceWidget.tsx` (nouveaux), intégrés à `DashboardPage.tsx` sous les cartes KPI existantes. Dégradation silencieuse (composant non affiché, pas de plantage) pour un rôle sans `housekeeping:read`/`maintenance:read` (Comptable, RH) — cohérent avec le fait que `dashboard:read` seul ne garantit pas ces deux permissions.
- **Statut** : ✅ **Terminé** (session courante).
- **Éléments testés** : 6 tests unitaires (3 par widget — filtrage correct par statut/tickets ouverts, état vide explicite, dégradation silencieuse sans crash sur échec réseau/403) ; `npm run build`/`lint`/`test` clean (60/60). **Vérification en navigateur réel** (Playwright, données seedées réelles) : état initial vide confirmé cohérent avec la carte KPI « Chambres à nettoyer : 0 » ; ouverture d'un vrai ticket de maintenance (chambre 103, priorité Urgente) + passage réel de la chambre 103 en À nettoyer → les deux widgets reflètent immédiatement ces changements réels (capture d'écran), aucune donnée fabriquée à aucun moment ; nettoyage post-vérification (ticket résolu, chambre repassée Libre & propre).

### CH-045 — Resynchronisation de documentation obsolète (dette découverte pendant l'audit du 29 juillet 2026)

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase C.
- **Statut** : ✅ **Terminé** (session courante).
- **Résolution** : `docs/governance/FONCTIONNALITES_INCOMPLETES.md` (la ligne « Journal d'audit transverse consultable côté interface » était restée « Aucune UI de consultation » alors que CH-015/CH-032 l'avaient déjà résolue — corrigée) ; `docs/execution/EXECUTION_MASTER_PLAN.md` §5.1/5.2 (décrivait une politique GitFlow `main`/`develop`/`staging`/`feature/*` jamais suivie en pratique — corrigé pour refléter la pratique réelle : une seule branche de travail fusionnée dans `main` chantier par chantier).

### CH-046 — Infrastructure VPS réelle (runbook, Go-Live checklist, pipeline de déploiement)

- **Source** : `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` Phase D — choisi après CH-045 car non bloqué par les deux questions utilisateur en attente (reset DB local, arbitrage multi-folio) : documentation et scaffolding CI/CD purs, aucun code métier ni migration Prisma.
- **Écart corrigé** : `docs/operations/OPERATIONS_RUNBOOK.md` et `docs/execution/GO_LIVE_CHECKLIST.md` décrivaient tous deux une architecture GCP générique (Cloud Run, Cloud SQL, Load Balancer managé, Cloud Logging/Datadog, Secret Manager) qui ne correspond à aucun composant réel de ce projet (VPS Hostinger unique, Docker Compose, Nginx hôte, Certbot — écart déjà signalé sans être corrigé dans `docs/operations/CH-026E_DEPLOIEMENT_DOMAINE.md` §6). Les deux documents ont été intégralement réécrits section par section contre le stack réel vérifié (`docker-compose.yml`, `backend/Dockerfile`, `backend/.env.example`, `.github/workflows/ci.yml`).
- **Bug réel détecté et corrigé pendant l'écriture (pas seulement documenté)** : `EXECUTION_MASTER_PLAN.md` §4 et une première version de ce runbook décrivaient la révocation de session par « incrémentation de `tokenVersion` » — cette colonne **n'existe pas** dans `backend/prisma/schema.prisma` (vérifiée par grep avant d'écrire la procédure, conformément à la discipline « vérifier le code réel, jamais de suppositions »). Le mécanisme réel est `RefreshToken.revokedAt` + `User.actif`, avec une limite honnête documentée : l'access token JWT est strictement stateless (`JwtAccessStrategy`, aucune requête base par appel) donc un jeton déjà émis reste valide jusqu'à 15 minutes après une révocation, pas instantanément. Corrigé dans les deux documents.
- **Bug d'infrastructure réel détecté et corrigé avant commit** (pas seulement écrit puis supposé correct) : la première version de `docker-compose.prod.yml` utilisait `ports:` sans tag de fusion — `docker compose -f docker-compose.yml -f docker-compose.prod.yml config` (exécuté réellement, Docker Compose v5.1.1 disponible dans l'environnement) a révélé que Compose **fusionne** les listes par défaut au lieu de les remplacer : le port non restreint de `docker-compose.yml` restait exposé **en plus** du port restreint à `127.0.0.1`, annulant silencieusement tout le durcissement recherché. Corrigé avec le tag `!override` (Compose Specification) sur `ports:` des 4 services ; reconfirmé par une nouvelle exécution de `docker compose config` montrant un seul mapping par service, tous liés à `127.0.0.1`.
- **Livrables** :
  - `docs/operations/OPERATIONS_RUNBOOK.md` — réécriture complète des 6 sections (architecture VPS réelle avec schéma Mermaid à jour, pipeline `deploy.yml`, migrations hors `CMD` du conteneur, rollback par tag `stable`/`sha-<commit>`, monitoring sans outil géré, incidents avec le mécanisme de révocation réel).
  - `docs/execution/GO_LIVE_CHECKLIST.md` — grille de 15 points réécrite contre le stack réel, plus plusieurs points explicitement reformulés pour ne jamais faire cocher un contrôle qui prétendrait l'existence d'un outil non installé (ex. point 9, monitoring).
  - `docker-compose.prod.yml` (nouveau, racine du dépôt) — override production : images GHCR au lieu de `build:`, ports liés à `127.0.0.1` uniquement.
  - `infra/nginx/pms-hotel-makarim.conf` (nouveau) — reprend le schéma déjà validé dans CH-026E, ajoute les en-têtes HSTS/`X-Frame-Options`/`X-Content-Type-Options` exigés par le Go-Live checklist point 6 (jusque-là non matérialisés dans aucun fichier), plus un bloc de bascule en mode maintenance.
  - `infra/scripts/backup-mysql.sh` / `restore-mysql.sh` (nouveaux) — `mysqldump --single-transaction` quotidien + restauration interactive avec confirmation explicite ; limite assumée et documentée (stockage local uniquement tant que l'utilisateur n'a pas confirmé une destination externe).
  - `.github/workflows/deploy.yml` (nouveau) — pipeline réel déclenché après succès de `ci.yml` sur `main` : build+push GHCR, SSH, migrations, bascule `--no-deps`, healthcheck 10×3s, marquage `stable`, rollback automatique en job séparé si le déploiement échoue.
  - `.claude/skills/deploiement-vps/SKILL.md` — corrigé (référençait un `deploy.yml` et un `docs/plan-execution-claude-code.md` qui n'existaient pas encore ; présentait aussi l'infrastructure VPS comme « déjà en place » sans confirmation, reformulé en prérequis à provisionner).
  - `docs/execution/EXECUTION_MASTER_PLAN.md` §4 — corrigé au passage (même bug `tokenVersion` que ci-dessus).
- **Non fait dans ce chantier, explicitement hors périmètre** : provisionnement réel du VPS, achat/DNS du domaine, génération des secrets GitHub (`VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY`), premier déploiement réel — toutes ces actions nécessitent des informations que seul l'utilisateur détient (domaine choisi, accès SSH réel) et ne doivent jamais être supposées ou inventées.
- **Statut** : 🚧 **Partiellement terminé** (session courante) — documentation, infra-as-code et pipeline livrés et validés autant que possible sans accès réel (`docker compose config`, YAML, bash -n). **Non conforme au critère de complétion défini par `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md` §2bis** : « configuration Nginx hôte committée et **testée** » et « un exercice de restauration **effectivement testé** » exigent tous deux un accès réel au VPS (domaine exact, méthode de connexion SSH) — confirmé disponible par l'utilisateur le 29 juillet 2026 mais **pas encore recueilli dans cette session**, conformément à l'instruction explicite du plan de ne jamais le supposer. Reste à faire dès réception : déployer `infra/nginx/pms-hotel-makarim.conf` avec le vrai domaine, générer le certificat Certbot, exécuter un vrai cycle sauvegarde→restauration, puis exécuter `deploy.yml` une première fois en conditions réelles (bascule effective depuis `MakarimPMS_v2`, déjà déployé en test sur ce VPS selon le plan).
- **Éléments vérifiés** : `docker compose -f docker-compose.yml -f docker-compose.prod.yml config` (résolution réelle, deux fois — avant et après le correctif `!override` — pour prouver le bug puis la correction) ; `docker-compose.yml` seul reste valide (`docker compose config`, non affecté par l'ajout de l'override) ; YAML de `deploy.yml` validé ; syntaxe bash des deux scripts validée (`bash -n`) ; `GET /api/health` confirmé exister réellement (`backend/src/app.controller.ts`) avant d'être cité comme contrôle de santé du pipeline.

### CH-050 — Diffusion de facture (PDF + UI d'ajout de charge extra + email/WhatsApp)

- **Source** : demande explicite de l'utilisateur (module de facturation complet), cadrée dans `docs/execution/PLAN_MODULE_FACTURATION.md`.
- **Livrables (1ʳᵉ tranche, sans migration)** :
  - `backend/src/modules/billing/utils/invoice.pdf.ts` (nouveau, pur, pdfkit — même bibliothèque/convention que `police/utils/police-record.pdf.ts`) + `BillingService.generateInvoicePdf()` + `GET /invoices/:id/pdf` (`billing:read`).
  - `frontend/src/features/billing/components/AddFolioLineDialog.tsx` (nouveau) — type figé à `EXTRA` (pas de sélecteur : HEBERGEMENT/PAIEMENT ont leur propre chemin d'écriture canonique, TAXE_SEJOUR est déjà matérialisée automatiquement par `generateInvoice()`) ; bouton "Ajouter une charge" et bouton de téléchargement PDF dans `BillingTabContent.tsx`.
  - **Bug réel détecté et corrigé en câblant l'UI (pas seulement documenté)** : `BillingService.addFolioLine` ne vérifiait jamais si une facture `EMISE` existait déjà sur le folio — une charge ajoutée après coup n'aurait jamais pu apparaître sur cette facture immuable (INV-FAC-001), sans qu'aucun signal n'avertisse la réception (montant silencieusement non facturé). Garde ajoutée (`ConflictException`, même style que `excludeTaxes`), reflétée côté frontend (bouton désactivé, `title` explicatif). Non appliquée à `creditFolioLine` (paiements/acomptes, qui doivent rester possibles après émission — cas normal, pas un bug).
- **Livrables (suite, débloquée une fois le consentement de migration DB obtenu — voir §2ter de `docs/execution/PLAN_MISE_EN_PRODUCTION_BETA.md`)** :
  - Migration Prisma (générée via `prisma migrate diff --from-url ... --to-schema-datamodel ... --script` + `prisma migrate deploy`, contournement fiable de la limite `migrate dev` documentée ci-dessous — pas de `migrate reset`) : `InvoiceDownloadToken` (jeton public à durée limitée, 7 jours, un par demande d'envoi, jamais réutilisé), `EvenementNotification.FACTURE_EMISE`, et au passage la colonne `Room.etage` (CH-038) qui traînait depuis la préparation RD-024.
  - **Approche WhatsApp retenue** (réponse à la question ouverte Q3 du plan) : pièce jointe **native** via `mediaUrl` Twilio plutôt qu'un simple lien texte — Twilio récupère lui-même le PDF sur `GET /invoices/download/:token` (`@Public()`, throttlé 20/min comme self-checkin), donc le coût d'ingénierie du lien à token est identique dans les deux cas ; autant offrir la vraie pièce jointe dans la conversation WhatsApp plutôt qu'un lien à cliquer. Email : pièce jointe PDF directe (pas de lien, `MailerService.send()` étendu avec un paramètre `attachments` nodemailer natif).
  - `BillingService.requestDelivery()` (émet `facture.envoi-demande`, `emitAsync`) → `FactureEnvoiDemandeListener` (nouveau, `notifications/listeners/`, façade `BillingService.getInvoiceDeliveryContext()`/`createDownloadToken()`) → `NotificationsService.notify()` étendu avec des `options` génériques (`emailAttachment`/`whatsappMediaUrl`, jamais spécifiques à un évènement — n'importe quel futur évènement pourra les réutiliser) → jobs BullMQ étendus (`SendEmailJobData.attachment`, `SendWhatsappJobData.mediaUrl`) → `TwilioService.sendWhatsapp()` accepte désormais un `mediaUrl` optionnel. `POST /invoices/:id/envoyer` (`billing:write`) + `GET /invoices/download/:token` (`@Public()`) dans `BillingController`. 2 templates de seed (`FACTURE_EMISE`/EMAIL et /WHATSAPP, `{{numero}}`/`{{montant}}`/`{{lien_facture}}`) — même mécanique multi-canal que le reste de F7, aucun code de dispatch dupliqué.
  - Bouton "Envoyer par email/WhatsApp" (icône `Send`) dans `BillingTabContent.tsx`, confirmation par toast (`toastManager`, même composant que le réassort de stock) — résultat réel par canal consultable dans le journal de notifications existant (F7), pas un second écran.
- **Découverte réelle en débloquant la migration (pas supposée)** : `npx prisma migrate resolve --rolled-back` (autorisé explicitement par l'utilisateur ce tour-ci) a bien nettoyé la ligne fantôme de `_prisma_migrations`, confirmé par `migrate status` — mais `prisma migrate dev` continue de refuser toute nouvelle migration sur cette base (même erreur qu'avant, but re-triggered), semble ignorer `rolled_back_at` dans sa logique de détection de dérive côté shadow database. Contournement fiable identifié et utilisé pour cette migration et documenté pour les suivantes (CH-038/039/040/041/048/049) : `prisma migrate diff --from-url $DATABASE_URL --to-schema-datamodel prisma/schema.prisma --script` (diff direct contre la base réelle, sans shadow database) puis `prisma migrate deploy` (chemin de contrôle déjà utilisé en CI/production) — aucune commande destructive, migration versionnée normale dans `prisma/migrations/`.
- **Statut** : ✅ **Terminé** intégralement (session courante).
- **Éléments testés** : 4 tests unitaires (`invoice.pdf.spec.ts`) ; 3 nouveaux tests e2e (`billing.e2e-spec.ts`, vraie base MySQL) — rejet 409 de `addFolioLine` post-émission (sabotage/restore), `GET /invoices/:id/pdf` (`%PDF-` réel), `POST /invoices/:id/envoyer` créant un `NotificationLog` par canal configuré (EMAIL + WHATSAPP, destinataires réels vérifiés) et un jeton de téléchargement résolvable en vrai PDF via la route publique (jeton invalide → 404, sabotage/restore) ; suite e2e complète (23 suites/162 tests) relancée deux fois, verte les deux fois. **Vérification en navigateur réel** (Playwright, backend/frontend redémarrés à chaque itération) : check-in walk-in réel, ajout de charge, génération de facture, désactivation du bouton "Ajouter une charge" post-émission, téléchargement PDF réel, puis **clic réel sur "Envoyer"** → toast "Envoi demandé" → confirmation en base que `NotificationLog` contient bien 2 lignes `ENVOYE` (EMAIL vers l'email réel saisi au check-in, WHATSAPP vers le téléphone réel saisi) — aucune régression console imputable à ce chantier.
- **Incident annexe rencontré et résolu pendant la validation** : la suite e2e complète a d'abord échoué sur `stock.e2e-spec.ts` (quantité de shampoing inattendue) — enquête complète (mouvements de stock réels interrogés en base, pas supposés) a montré qu'il s'agissait d'une déplétion cumulative des articles de stock partagés (`AMEN-SOAP-01`/`AMEN-SHMP-01`) après de très nombreuses vérifications réelles en navigateur au fil de cette session, pas d'une régression du code — confirmé en reseedant (`prisma db seed`) puis en relançant : suite verte. Un processus `jest` orphelin d'une exécution antérieure (lancée avant la résolution de la dérive de migration, jamais arrêtée) tournait aussi en parallèle et a été identifié et arrêté au passage — deux causes distinctes, toutes deux étrangères au code de ce chantier.

*(CH-039, CH-040, CH-041, CH-044, CH-047, CH-048, CH-049 : fiches à détailler au fil de l'exécution — CH-039/040/041/048/049 ne sont plus bloqués par la migration DB (résolue ci-dessus) mais restent en attente de décisions produit pour CH-048 (répartition manuelle vs automatique) et CH-049 (politique de départ anticipé) — voir les questions ouvertes de `docs/execution/PLAN_MODULE_FACTURATION.md` ; CH-044 (décision multi-folio) nécessite le même arbitrage utilisateur avant tout code ; CH-047 (smoke tests + passe finale Go-Live) dépend de CH-046 et de l'accès VPS réel ; voir le plan pour la liste complète et les critères de chaque chantier.)*

---

## Résumé quantitatif

| Priorité | Nombre de chantiers | Charge cumulée estimée (ordre de grandeur) | Terminés |
|---|---|---|---|
| Bloquant | 4 (CH-001 à CH-004) | ~7–11 jours développeur | 4 (CH-001, CH-002, CH-003, CH-004) — tous terminés |
| Important | 8 (CH-005 à CH-012) | ~11–16 jours développeur | 8 (CH-005, CH-006, CH-007, CH-008, CH-009, CH-010, CH-011, CH-012) — tous terminés |
| Secondaire | 14 (CH-013 à CH-026) | ~18–28 jours développeur (plusieurs sous conditions d'arbitrage) | 12 (CH-013, CH-014, CH-015, CH-018, CH-019, CH-020, CH-021, CH-022, CH-023, CH-024, CH-025, CH-026 — CH-020/021/023/024 fermés sans changement de code : écarts assumés, statu quo actés, ou blocage technique MySQL vérifié pour CH-024 ; CH-026 désormais 6/6 sous-points, voir fiche) |
| Hors audit — cadré, non planifié | 1 (CH-027) | ~11–13,5 jours développeur (~15,5–19,5 j combiné avec le socle portail de connexion) | 0 — cadrage validé en principe (RD-017), timing d'insertion dans le backlog volontairement pas encore tranché |
| Frontend — audit qualité (Phase 11) | 8 (CH-028 à CH-035) | ~14–22 jours développeur (dont ~5-8 j pour CH-032 seul) | 7 (CH-035 — resync documentaire ; CH-031, CH-028 — Lot A ; CH-032 — Lot B, 4 sous-lots B1-B4 ; CH-034, CH-029 — Lot C ; CH-030 + CH-026(e) — Lot D, session courante) — Lots A/B/C/D clos, Lot E (CH-033) à faire, exécution par lot en cours (`docs/frontend-plan/LOTISSEMENT_CHANTIERS_QUALITE.md`) |

*Ces charges sont des ordres de grandeur de développement pur (hors tests e2e étendus, hors stabilisation, hors documentation) — voir `docs/planning/ESTIMATION_CHARGE.md` pour l'estimation consolidée par scénario.*

## Suivi d'avancement

| Chantier | Statut | Date | Résumé |
|---|---|---|---|
| CH-002 | ✅ Terminé | Session courante | Reset password sécurisé — voir fiche ci-dessus pour le détail et l'écart de conception (MailerService vs NotificationsService.notify()) |
| CH-001 | ✅ Terminé | Session courante | Avoir total sur facture émise — voir fiche ci-dessus (garde de régénération + correctif double-taxe) |
| CH-004 | ✅ Terminé | Session courante | Chiffrement AES-256-GCM de Guest.pieceIdentite — voir fiche ci-dessus (extension Prisma au niveau du client, pas du service, pour couvrir les lectures imbriquées) |
| CH-003 | ✅ Terminé | Session courante | UI de saisie du registre de police (nouvel onglet « Police » dans StayDetailsDialog) — voir fiche ci-dessus. Les 4 chantiers bloquants du registre sont désormais tous terminés. |
| CH-012 | ✅ Terminé | Session courante | Remboursement d'un acompte imputé — voir fiche ci-dessus (l'avoir est un préalable vérifié par lecture seule, pas une action déclenchée par la route, RD-007). |
| CH-005 | ✅ Terminé | Session courante | Blocage dur du check-out sur solde impayé, avec échappatoire de check-out forcé réservée à checkin:force-checkout — voir fiche ci-dessus (RD-008). |
| CH-011 | ✅ Terminé | Session courante | Gating RBAC frontend (granularité onglet entier) — GET /auth/me + filtrage de NAV_ITEMS, voir fiche ci-dessus (RD-009). |
| CH-006 | ✅ Terminé | Session courante | Filtrage soft-delete centralisé — extension Prisma `$extends`, voir fiche ci-dessus (RD-010). |
| CH-007 | ✅ Terminé | Session courante | Interface frontend self-checkin (staff) — `SelfCheckinPanel.tsx` sur le détail de réservation, voir fiche ci-dessus. Corrige au passage un bug latent de `lib/api-client.ts` (corps de réponse vide non géré hors 204) et la dette technique #6 (`seed.ts`, `DETTE_TECHNIQUE.md`). |
| CH-009 | ✅ Terminé | Session courante | Interface frontend channel-manager (mappings OTA) — 4e onglet dans `ParametersPage.tsx`, voir fiche ci-dessus. Vérifié par un appel webhook réel bout-en-bout (import résolu puis rejeté après suppression du mapping). |
| CH-008 | ✅ Terminé | Session courante | Interface frontend notifications (templates/journal) — nouvel onglet dédié `features/notifications/`, voir fiche ci-dessus. Preuve RBAC serveur réelle (403 pour Réception en écriture). |
| CH-013 | ✅ Terminé | Session courante | Retrait de `StatutSejour.ANNULE` (enum mort) — arbitrage utilisateur (RD-012), vérification exhaustive avant retrait. |
| CH-010 | ✅ Terminé | Session courante | Déduplication client — contrainte dure sur `Guest.pieceIdentite` (index aveugle HMAC, RD-011) + détection souple email/téléphone (`GET /guests/check-duplicate`, non bloquante). Vague 2 désormais close (8/8). Conflit technique avec le chiffrement CH-004 (IV aléatoire) détecté et résolu avant code. Corrige au passage un bug latent MySQL de `AllExceptionsFilter` sur P2002. |
| CH-020 | ✅ Fermé | Session courante | Numérotation de facture — statu quo acté (séquence continue), aucun développement (RD-013, EA-003). |
| CH-021 | ✅ Fermé | Session courante | City ledger / `Company` — dépriorisé formellement, écart assumé (RD-014, EA-001). |
| CH-023 | ✅ Fermé | Session courante | Recouvrement pénalité annulation/no-show — reste hors PMS par choix, écart assumé (RD-015, EA-002). |
| CH-026 | ✅ Terminé (6/6) | Session courante | Durcissement sécurité : helmet, comparaison à temps constant du secret webhook, verrouillage de compte, complexité mot de passe, rotation/révocation refresh token, migration cookies httpOnly + CSRF double-submit — voir fiche ci-dessus. (e) livré cette session (RD-016, RD-022), correctif post-vérification live sur la livraison du jeton CSRF cross-origine. |
| CH-014 | ✅ Terminé | Session courante | Consultation `RoomStatusLog` — `GET /rooms/:id/historique-statuts` (`RoomsService.findStatusHistory`, `housekeeping:read`) + `RoomHistoryDialog.tsx` côté frontend, voir fiche ci-dessus. Résout aussi le 4e des « quatre cas confirmés » de `CRITERES_STABILITE_LONG_TERME.md`. |
| CH-015 | ✅ Terminé | Session courante | Consultation `AuditLog` — backend déjà complet (`GET /audit-logs`), nouvel écran frontend `features/audit/` (filtres + détail avant/après), nouvel onglet « Audit », voir fiche ci-dessus. |
| CH-018 | ✅ Terminé | Session courante | Resync `MODULES_INDEX.md` (14→21 modules réels + `accounting` marqué non implémenté) et `CLAUDE.md` (17→21) ; 8 fiches créées (`auth`, `police`, `self-checkin`, `notifications`, `booking-engine`, `document-ocr`, `channel-manager`, `dashboard`) — scope réel bien plus large que l'estimation d'origine, validé avec l'utilisateur avant exécution, voir fiche ci-dessus. |
| CH-022 | ✅ Terminé | Session courante | Interface frontend document-ocr (`features/document-ocr/`, onglet « Scan pièce d'identité », `guests:write`) — corrige au passage un bug bloquant réel découvert en vérifiant l'écran en navigateur (une image corrompue faisait planter tout le process backend, pas seulement la requête), voir fiche ci-dessus. Premier upload multipart du frontend (`lib/api-client.ts` étendu pour les corps `FormData`). Premier fichier e2e pour ce module (`document-ocr.e2e-spec.ts`, gap pré-existant comblé). |
| CH-019 | ✅ Terminé | Session courante | Renommage `housekeeping/utils/room-transitions.ts` → `manual-status-targets.ts` — lève la collision de nom avec `rooms/utils/room-transitions.ts` (`DETTE_TECHNIQUE.md` point 4), aucune logique modifiée. |
| CH-024 | ✅ Fermé | Session courante | Exclusivité `RoomNight` — l'audit suggérait un XOR strict, mais `StayService.checkin()` fait légitimement cohabiter `reservationId`+`stayId` après un check-in réservé ; même la moitié valide de l'invariant (jamais les deux nulles) est techniquement bloquée en `CHECK` MySQL par `onDelete: Cascade` (erreur 3823) — fermé sans code, invariant vérifié par audit exhaustif des 4 sites d'écriture, voir fiche ci-dessus (RD-019). |
| CH-025 | ✅ Terminé | Session courante | 4 contraintes `CHECK` MySQL en défense en profondeur (`Reservation.dateDepart > dateArrivee`, `Payment.montant > 0`, `FolioLine.montant >= 0`, `TimeShiftSegment.fin >= debut`) — voir fiche ci-dessus pour l'écart avec la formulation initiale de l'audit et la régression détectée/corrigée sur `billing.e2e-spec.ts` (`> 0` trop strict pour `FolioLine`) avant clôture. |
| CH-027 | 🗒️ Cadré (non planifié) | Session courante | Personnel/Planning/Attendance — cadrage `docs/planning/CADRAGE_PLANNING_ATTENDANCE_STAFF.md` validé en principe par l'utilisateur (RD-017), documentation intégrée à la gouvernance (cette fiche, `hr.md` §17, `reporting.md` §16, `MATRICE_TRACABILITE.md`). Aucun code écrit — timing d'insertion dans le backlog volontairement laissé ouvert, voir `BACKLOG_PRIORISE.md`. |
| CH-028 | ✅ Terminé (socle initial) | Session courante | Socle de tests automatisés frontend (Vitest + Testing Library) — Lot A, 4 tests ciblés sur des régressions réelles (RBAC, refresh token/corps vide, upload multipart, affichage financier). Pratique continue au-delà. |
| CH-029 | ✅ Terminé | Session courante | Accessibilité (a11y) frontend — plugin `jsx-a11y` actif (9 violations réelles corrigées), focus trap/restauration sur `dialog.tsx` prouvé déjà fourni par le primitif (3 tests), 3 parcours prioritaires vérifiés au clavier en navigateur réel (10/10 assertions) — issu de la Phase 11, clôture Lot C. |
| CH-030 | ✅ Terminé | Session courante | Découpage de bundle (code splitting) par onglet via `React.lazy`+`Suspense` — chunk principal réduit de 571 kB à 237 kB, vérifié en navigateur réel (9/9 assertions, onglet réseau) — issu de la Phase 11, Lot D. |
| CH-031 | ✅ Terminé | Session courante | Error boundary transverse — Lot A, `components/ErrorBoundary.tsx`, preuve sabotage/restore réalisée en navigateur réel (crash provoqué et confiné dans `StockPage.tsx`, sabotage retiré, comportement normal revérifié). |
| CH-032 | ✅ Terminé (4/4 sous-lots, 7/7 composants) | Session courante | Composants partagés — dette Lot 0 résorbée. B1 : `table`+`form`/`FormField`. B2 : `tabs`+`date-picker`/`DateRangeField`. B3 : `select-search`/`SelectSearch`+`toast`/`toastManager`. B4 : `diff-viewer`/`DiffViewer` (AuditPage, vraie comparaison champ par champ) + `file-upload`/`FileUpload` (DocumentOcrPage, glisser-déposer réel). |
| CH-033 | 📋 À faire | Session courante | Branding et finitions d'identité visuelle (titre, favicon, `lang`, logo sidebar) — non prioritaire par choix explicite de l'utilisateur, issu de la Phase 11. |
| CH-034 | ✅ Terminé | Session courante | Responsive/mobile tranché (arbitrage utilisateur : investir, RD-021) — tiroir mobile `AppSidebar`/`AppTopbar` (backdrop, Escape, fermeture auto à la navigation), vérifié en navigateur réel à 375×812 et 1440×900, 14/14 assertions. |
| CH-035 | ✅ Terminé | Session courante | Resynchronisation de `MATRICE_MODULE_API_ECRAN.md` (6 lignes obsolètes corrigées) — corrigé dans la même session que son versement, issu de la Phase 11. |
